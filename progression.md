# Progression du projet

Dernière mise à jour : 16 septembre 2026
Branche de référence : `main`
Référence du cycle de l'étape 3 : `6130c99` — `fix: wire transactional match commits`
Référence du cycle de l'étape 4 : `9968878` — `fix: restore worker dispatcher and quiz judgments`, poussé sur `origin/main` et vérifié
Référence du cycle de l'étape 5 : `4d19233` — `fix: correct game rules projections and metrics`, poussé sur `origin/main` et vérifié
Référence du cycle de l'étape 6 : `55687d0` — `fix: unify game presence and network recovery`, poussé sur `origin/main` et vérifié

Ce fichier décrit la réalité du dépôt et non les seules capacités prévues dans les spécifications. Il complète [AGENTS.md](AGENTS.md), [docs/README.md](docs/README.md) et [docs/07-implementation-status.md](docs/07-implementation-status.md). Les statuts utilisés sont :

- ✅ documenté ou disponible comme contrat ;
- 🟢 présent dans le code ;
- 🟡 partiel ou à vérifier ;
- ⏳ à faire ;
- ⚠️ dépendance ou décision externe non vérifiée.

**Diagnostic du 13 septembre : le code des neuf jeux est présent, mais leur disponibilité fonctionnelle n'est pas acquise.** L'[audit complet](docs/audit-code-2026-09-13.md) et le [plan pas à pas](docs/plan-correction-2026-09-13.md) identifient 29 défauts de code/produit et 6 observations d'infrastructure : blocages du worker, abandon/forfait, présence, finalisation, réseau, sécurité et parcours incomplets. L'étape 1 est appliquée au harnais de tests ; le contrat commun de l'étape 2 est intégré à `main` (`d885079`), le raccordement SQL de l'étape 3 est versionné dans `6130c99` et sa validation PostgreSQL isolée ainsi que sa concurrence à deux sessions sont désormais démontrées localement. L'inspection Supabase du 16 septembre rapporte 33 migrations distantes, alignées avec les fichiers locaux, mais elle reste strictement en lecture seule. Quatre parties actives (trois TTMC et une Géographie), dix jobs échus en attente et deux joueurs engagés dans plusieurs parties doivent être préservés en production. Aucun secret, traitement de donnée, migration distante, écriture Vault, déploiement ou opération de reprise n'a été effectué dans la présente session. Les observations datées ci-dessous restent historiques ; l'audit et le préflight Étape 10 prévalent pour les limitations actuelles.

### 13/09/2026 — Clause de transmission des diagnostics et plans

- Demande : ajouter à `AGENTS.md` une sortie en deux niveaux pour les diagnostics, plans, revues et comptes rendus destinés à un modèle d'exécution plus petit.
- Décision : chaque document concerné doit séparer une `Synthèse pour l'humain` d'un bloc `Instructions d'exécution pour l'agent`, ce dernier listant les fichiers, symboles, causes ou hypothèses, changements attendus, invariants, ordre des dépendances et vérifications. Le bloc décrit la passation directement et ne demande pas à l'agent suivant de déléguer à son tour.
- Contradiction : aucune avec `AGENTS.md` ; il s'agit d'une précision de méthode documentaire, sans changement de périmètre produit ni de règle métier.
- Vérification : contrôle de cohérence Markdown et lecture du diff effectués ; aucun code, test applicatif, migration, commit ou déploiement n'a été engagé.

### 13/09/2026 — Abandon de la connexion GitHub pour les migrations Supabase

- Demande : ne plus utiliser la connexion GitHub/Supabase pour appliquer les migrations ; privilégier le connecteur Supabase dans Codex et, en cas d'erreur, fournir à l'utilisateur la commande ou le SQL exact dans un bloc `bash` copiable tel quel dans Supabase.
- Décision : GitHub reste le dépôt, le support de revue et de CI, mais aucun push, merge, déploiement GitHub, action CI ou build Vercel ne doit appliquer le schéma de production. Les inspections et applications distantes passent par le connecteur Supabase ; un échec impose l'arrêt avant mutation et la remise du texte exact avec l'erreur exacte.
- Documents réalignés : `AGENTS.md`, `README.md`, `docs/06-delivery-testing.md`, `docs/plan-correction-2026-09-13.md` et `docs/audit-code-2026-09-13.md`.
- Contradiction : oui, avec l'ancienne règle d'application automatique via l'intégration GitHub native Supabase ; ancienne règle et nouvelle procédure ajoutées au journal des décisions ci-dessous. Aucun changement de code, migration distante, commit ou déploiement n'a été effectué.

### 13/09/2026 — Clarification du secours Supabase, du statut et de la livraison

- Demande : appliquer les clarifications 1, 3, 4, 5 et 7 du retour de lecture d'`AGENTS.md`.
- Décision : le secours Supabase doit préciser sa destination — commande terminal dans un bloc `bash` ou SQL dans un bloc `sql` pour l'éditeur Supabase — sans mélanger les formats. « Privé » décrit l'accès aux parties, profils et données, pas une inscription obligatoirement sur invitation. `progression.md` est la référence de disponibilité réelle. Une inspection distante peut être en lecture seule ; toute mutation distante exige une autorisation explicite dans la tâche active.
- Livraison : toute tâche produisant une modification réelle du dépôt se termine désormais par un commit puis un push vers `main`, sauf instruction explicite contraire ; une tâche purement documentaire sans modification ne committe ni ne pousse.
- Documents réalignés : `AGENTS.md`, `README.md`, `docs/06-delivery-testing.md`, `docs/plan-correction-2026-09-13.md` et `docs/audit-code-2026-09-13.md`.
- Contradiction : oui, avec l'ancienne règle implicite de ne pas committer automatiquement et avec les formulations obsolètes de statut et de secours Supabase ; aucune contradiction avec les invariants métier. Aucun changement distant Supabase n'a été effectué ; le commit documentaire `408a96d` a été poussé sur `origin/main` et vérifié.

## Vue d'ensemble du site

| Domaine | Statut | Réalité et prochaine étape |
|---|---:|---|
| Cadrage produit et architecture | ✅ | Neuf jeux, V1 à deux joueurs, contrats d'architecture, base, API et moteurs documentés. |
| Dépôt et branche de travail | 🟢 | Remote GitHub configuré ; `main` suit `origin/main` et constitue la branche de production déclarée. Ne pas forcer ni réécrire son historique. |
| Shell Next.js et accueil | 🟢 | App Router, layout, header, accueil et rail responsive des neuf jeux présents. Homepage refondue en DA violette sobre : sélection prioritaire visible, jeux secondaires en rail, palette unifiée et responsive validé en E2E. Les jeux non prêts restent désactivés. |
| Authentification et admission privée | 🟢 | Inscription libre, provisionnement, callback, profil et accès invité présents. La session anonyme complète atteint `/profil` sur Chromium desktop et iPhone 13 mobile dans Supabase local ; le fixture permanent Alice/Bob et le tiers désactivé restent séparés. Recette de production non revendiquée. |
| Salons et lancement de partie | 🟢 | Le salon monté dans `src/app/salons/[roomId]/` couvre heartbeat, garde de version, Broadcast/relecture, `READY`/`START`, configuration, transfert d'hôte, départ, fermeture et reprise. La gate Étape 8 valide ce parcours à deux contextes sur desktop/mobile ; l'Étape 9 complète reste à exécuter. |
| PostgreSQL/Supabase | 🟡 | Inspection connecteur en lecture seule le 16 septembre : projet `ACTIVE_HEALTHY`, 33 migrations distantes jusqu'à `20260915210754`, alignées avec les fichiers locaux. Les agrégats de préservation comptent 4 parties actives (8 sièges), 10 jobs `pending` échus et aucun job `running`/`failed` ; deux joueurs ont plusieurs engagements actifs (maximum 3). Aucun traitement, changement de schéma, écriture Vault, contrainte ou donnée distante n'a été modifié. |
| Transactions de partie | 🟡 | Le contrat pur commun est intégré à `main` et raccordé localement à une migration additive, au repository, à la route de commandes et aux assertions SQL. PostgreSQL isolé, rejeu concurrent à deux sessions et finalisation unique sont validés localement ; la recette de production et les parcours multijoueurs restent à faire. |
| Realtime | 🟡 | Le helper partagé couvre maintenant heartbeat, relecture, garde de version, reconnexion, retour d'onglet, réponses HTTP obsolètes et reprise d'intention ; la validation Supabase distante et la recette à deux comptes restent à faire. |
| Jobs et échéances | 🟡 | Le claim/dispatcher Étape 3 produit exactement les couples `jobId`/`leaseToken` ; l'endpoint interne, le bail, la relecture privée et les reprises de bail sont validés par tests de contrat. Le runtime local des jugements Étape 4 valide aussi le budget, les tentatives, le cache et la libération durable des réservations expirées. En lecture seule, le Cron Supabase est actif et compte des exécutions `succeeded`, mais les 10 jobs `pending` échus restent sans transition vérifiée ; le worker réel, Vault et toute reprise idempotente sont arrêtés par la gate Étape 8 non démontrée (audit D01–D07). |
| Profils, statistiques et historique | 🟡 | Routes/repository d'historique existent ; le parcours complet profils, stats et agrégats de duo reste à achever. |
| Contenus | 🟡 | Packs publiés vérifiés à distance : Géographie 380, Trou Noir 120, TTMC 440/22 thèmes, Compatibilité 160, Longueur d'onde 80. Les manifests portent provenance, version, quantité, couverture et checksum ; `content:validate` est désormais une validation locale en lecture seule. Trou Noir reste sous le seuil 300/60 par catégorie et TTMC sous 30 thèmes ; seules les configurations TTMC 20/15 et 30/20 sont disponibles. Benchmark DeepSeek et publication de contenu restant à établir. |
| Tests et CI | 🟡 | Étape 8 démontrée localement : Vitest 60 fichiers / 432 succès + 2 sentinelles attendues, pgTAP 190 assertions et gate Playwright 10/10 sans retry sur Chromium desktop/iPhone 13. La suite E2E complète a terminé sans échec final avant correction de deux attentes instables ; leurs relances ciblées passent 6/6. L'Étape 9 reste `not-run`, donc le statut global demeure partiel. |
| Déploiement Vercel/Supabase | 🟡 | Le projet Vercel `thib.fun` et son dernier déploiement de production observé `READY` (`dpl_2h6H8pCc2VdrMvCRHbauVX8uy9Sa`) correspondaient au commit applicatif préflight `9f4aba3…`; les commits documentaires `b27ce0f` et `79a5200` n'ont pas été déployés. Les logs de build n'ont qu'un avertissement Node et les erreurs runtime des dernières 24 h sont vides. Le connecteur expose les domaines mais pas les noms de variables ni le réglage d'origine autorisée ; aucun déploiement ou changement Vercel n'a été effectué. |

## Progression par jeu

| Jeu | Slug | Statut actuel | Ce qui existe | À faire avant de le déclarer réellement prêt |
|---|---|---:|---|---|
| Trou Noir | `trou-noir` | 🟢 | Moteur answering/judging/reveal, correction déterministe + DeepSeek serveur, projections sans fuite, UI, pack 120 questions, RPC `server_get_quiz_content` (`20260911130000_trou_noir_quiz_rpc.sql`), `contestsAccepted` réel dans perPlayer/ResultSpec. Le `judgeJob` porte l'`attemptId` et la phase judging, le worker relit le contexte privé avant et après l'appel, et les budgets/tentatives/cache sont validés en base locale. Commits `11f3662` et `ec9e4ee` présents sur `main` et poussés selon la recette précédente. | Recette à deux sessions, worker/dispatcher réel et benchmark DeepSeek daté. |
| TTMC | `ttmc` | 🟢 | Moteur pur (choose_level/answering/judging/reveal/finished), types/config, projection sans fuite, correction déterministe + DeepSeek serveur, UI setup/match, pack 22 thèmes/440 questions via `server_get_ttmc_content()`, worker durable, registry `ready`, routes create/start/commands/get match. Le `judgeJob` et les tentatives sont liés à la phase suivante attendue ; les budgets/tentatives/cache passent les tests SQL locaux. Commits `b5a49b5` et `3de4688` intégrés dans `main` ; push groupé vérifié avec le jalon Skyjo. | Recette à deux sessions, worker/dispatcher réel et benchmark DeepSeek daté. |
| Géographie / HexaPoint | `geographie` | 🟢 | Moteur pur, types/config, projection privée, scoring Haversine, carte, UI, API, worker, pack local, tests unitaires et E2E. Étape 5 corrige l'attribution du forfait, l'interruption de préparation, la transformation de viewport, le curseur clavier et l'erreur/rejeu de carte ; l'étape 6 ajoute le transport partagé, le heartbeat et la reprise réseau ; le registre TS le marque `ready`. | Activation et contenu distants vérifiés, partie complète à deux comptes restante. |
| Skyjo | `skyjo` | 🟢 | Moteur pur (setup simultané, sources, remplacements, colonnes, dernier tour, pénalité, timeouts, recyclage, cap 200, short/full), projection sans fuite, config salons, routes create/start/commands/get, worker durable, UI setup/match violette sobre, page `/jeux/skyjo`, migration `20260911180344_skyjo_activate.sql` créée avec la CLI (appliquée à distance, vérification du 13 septembre). Registre TS `ready`. Cycles 1+2 relus et corrigés par Muse ; commit code `2745261`, documentation `a2ec9e4`, push vérifié sur `origin/main`. | Corriger les blocages communs de l’audit, puis recette à deux sessions. |
| UNO / Dernière carte | `uno` | 🟢 | Moteur, deck, types/config, projection de main secrète, UI, API de commandes, worker, migration `20260910100000_uno_ready.sql`, tests moteur/projection et E2E. Étape 5 couvre forfait/abandon précoce, joker, annulation, changement de tour, disparition de carte et focus accessible ; l'étape 6 ajoute le transport partagé, le heartbeat et la reprise réseau. Le code est présent sur `main`. | Activation distante vérifiée, recette indépendante à deux comptes restante. |
| BombParty | `bombparty` | 🟢 | Moteur pur versionné (`bombparty-1`/`bombparty-engine-1`), normalisation serveur stricte, refus `DEADLINE_EXPIRED` moteur avant mutation sur `SUBMIT_WORD` tardif (RESIGN/CLAIM_FORFEIT restant possibles), métriques individuelles par siège (`sumResponseMs`/`responseCount`/`longestWordLength` en tuples 0/1), index séquence→mots privé via `loadBombpartyContent()` (pack `bombparty-fr-seed-2026-09`, 431 formes, CC0-1.0, sha256 `a6b74cd6…`, fichier inchangé), projection sans fuite (ni index, ni `usedWords`, ni `packId`/`packChecksum`), routes create/start/commands/get, worker durable (`turn_timeout` + `check_absence` avec garde `phaseId`), entraînement solo `/entrainement/syllabes` + 3 routes refusées pendant un match actif (`server_has_active_match`, suggestions max 20), UI violette sobre, migration CLI `20260911183307_bombparty_activate.sql` (appliquée à distance, vérification du 13 septembre), registre TS `ready`. Cycles 1+2 relus par Muse ; commit `cb88b8c` intégré dans `main`, push vérifié sur `origin/main`. | Valider SQL/RPC sur base isolée, recette à deux sessions, E2E et mesure latence p95 <2 s. |
| Bataille navale | `bataille-navale` | 🟢 | Moteur pur versionné (`bataille-navale-1`/`bataille-navale-engine-1`), grille 10×10, catalogue serveur carrier5/battleship4/cruiser3/submarine3/destroyer2 (17 cases, contacts autorisés), setup 180 s avec `SET_FLEET` partiel/`RANDOMIZE_FLEET` (backtracking fini)/`READY`/`UNREADY`, premier tireur aléatoire serveur, `FIRE` un tir par tour sans rejouer, victoire immédiate à 17, config stricte `{turnSeconds:null\|60}` default null, tir auto serveur sur case inconnue, projection sans `shipId` avant coulé et sans flotte adverse avant fin, coulé avec `shipId`/`shipType`/`sunkCells` serveur, UI violette (tirs gauche/flotte droite desktop, onglets mobile, zoom, roving tabindex, tirer-en-B7), routes rooms/start/commands/get, worker `preparation_timeout`/`turn_timeout`/`check_absence` avec garde `phaseId`, migration CLI `20260911190958_bataille_navale_activate.sql` (appliquée à distance, vérification du 13 septembre), registre TS `ready`, 54 tests moteur/projection. Deux cycles de comparaison et une revue de contrat ont été réalisés via CLI OpenCode/Muse ; commit `34fcd67` intégré dans `main` (push à vérifier). | Valider SQL sur base isolée, recette à deux sessions, E2E. |
| Compatibilité | `compatibilite` | 🟢 | Pack local original versionné : 160 questions, 40 par catégorie ; moteur pur et projection sans fuite, réponses simultanées, cinq réserves pour trois passages, révélation temporisée, score coopératif, résultat/historique sans victoire-défaite, API/worker, UI violette sobre, migration additive et RPC de contenu `service_role` uniquement. Étape 5 corrige les triggers coopératifs et le cumul des métriques ; 13 tests dédiés plus régressions locales. | L'historique distant contient les 33 migrations locales jusqu'à l'additive Étape 8, mais aucune recette de production ou à deux sessions n'est validée ; préserver les engagements avant toute reprise. |
| Longueur d'onde | `longueur-onde` | 🟢 | Pack local original généré et validé : 80 axes opposés, 30 quotidien/25 culture/25 absurde, labels bornés, exemples de tutoriel hors partie, manifest et migration/RPC versionnés. Moteur pur versionné (`longueur-onde-1`/`longueur-onde-engine-1`), indice borné et filtré, cible secrète, estimation tactile, révélation, score coopératif, projection sans fuite, résultat/historique sans victoire-défaite, API/worker durable, UI violette sobre, page `/jeux/longueur-onde`, registre `ready`, 16 tests dédiés. Étape 5 corrige la jointure `match_id`, les triggers coopératifs et le cumul des métriques ; les migrations initiale `20260911210000_longueur_onde_ready.sql` et corrective `20260911233839_longueur_onde_cooperative_result_triggers.sql` restent immuables et appliquées à distance. | L'historique distant contient les 33 migrations locales jusqu'à l'additive Étape 8, mais aucune recette de production ou à deux sessions n'est validée ; préserver les engagements avant toute reprise. |

## Difficultés rencontrées pendant le développement

Cette section est un journal des problèmes réellement observés pendant le travail. L'IA l'alimente automatiquement lorsqu'un problème est signalé ou détecté, notamment quand l'utilisateur dit « ça ne marche pas », « j'ai une erreur » ou lorsqu'une commande, un test ou une implémentation échoue.

Ne pas y inventer de risques théoriques. Si la cause n'est pas confirmée, l'indiquer comme hypothèse. Une difficulté résolue reste dans le journal ; on ajoute sa résolution au lieu de réécrire ou supprimer l'entrée.

### 11/09/2026 — Contrôle documentaire sous Windows

- Problème : `scripts/check-docs.mjs` construisait un chemin `C:\C:\...` avec `URL.pathname` et ne contrôlait pas `progression.md`.
- Résolution : utilisation de `fileURLToPath` et ajout de `progression.md` à la vérification.
- Vérification : `node scripts/check-docs.mjs` valide 21 fichiers Markdown.

### 11/09/2026 — Vérifications après refonte de la homepage

- Problème : `pnpm test` échouait sur quatre suites avec `Cannot find package '@/games/...'` ; la suite E2E était également bloquée au départ par l'absence des navigateurs Playwright.
- Cause confirmée : l'alias Vitest utilisait `URL.pathname`, incompatible avec le chemin Windows ; les navigateurs Chromium et WebKit n'étaient pas installés dans l'environnement.
- Résolution : remplacement par `fileURLToPath` dans `vitest.config.ts`, puis installation des navigateurs Playwright nécessaires à la recette desktop/mobile.
- Vérification : `pnpm test` valide 5 fichiers et 28 tests ; `pnpm typecheck`, `pnpm lint` et `tests/e2e/home.spec.ts` passent sur Chromium et mobile.

### 11/09/2026 — Diagnostic transversal du code

- Demande : examiner les erreurs, incohérences et problèmes de maintenance, puis proposer des corrections. Aucune contradiction avec AGENTS.md ; aucune autorisation de correction générale ou de déploiement n'est déduite de cet audit.
- Rapport : [diagnostic du code et correctifs proposés](docs/audit-code-2026-09-11.md). Les constats sont distingués des fonctionnalités encore incomplètes et des configurations distantes non vérifiées.
- Erreur reproduite : `pnpm content:validate` échoue avec `ENOENT` sur un chemin Windows commençant par `C:\\C:\\` et contenant `%20`. Cause confirmée : `URL.pathname` utilisé comme chemin système. Le même motif existe dans les scripts de génération du pack et du SQL ; correctif proposé avec `fileURLToPath`, non appliqué dans cette tâche.
- Bugs établis par lecture croisée : attribution inversée du forfait Géographie ; annulation des timers lors d'actions conservant la phase ; traitement des commandes avant recherche du reçu ; absence de heartbeat UNO ; absence de contrôle d'une partie active par joueur ; jobs en échec définitif sans finalisation technique. Voir le rapport pour preuves, scénarios et autres écarts. Les scénarios SQL ne sont pas déclarés reproduits en base.
- Vérifications exécutées : `pnpm test` (5 fichiers/28 tests), `pnpm typecheck`, `pnpm lint`, `pnpm build` réussis. `pnpm test:e2e` : 2 tests accueil réussis et 4 tests de jeu ignorés faute de mot de passe de fixture. `pnpm docs:check` valide les 21 fichiers préexistants avant rédaction du rapport.
- Vérifications complémentaires : les probes déléguées de moteur reproduisent le forfait Géographie attribué à l'adversaire et le rejet `NOT_YOUR_TURN` d'une action UNO répétée après changement de tour. Double transformation des marqueurs de carte confirmée par lecture du composant et de la projection. Après ajout du rapport, `pnpm docs:check` valide 22 fichiers.
- Limites réelles : Docker et CLI Supabase absents du PATH ; un worker a confirmé l'échec de `supabase --version`. Pas d'exécution SQL locale ni de recette multijoueur dans cet audit. Aucun dashboard ou statut de migration distant inspecté.
- Incidents d'outillage : certaines lectures d'inventaire et de routes dynamiques ont été refusées par le garde de lecture ; poursuite par extraits ciblés autorisés et lectures déléguées. Des recherches sur un dossier `.github` absent ou un glob Windows invalide ont échoué ; l'inventaire ciblé a ensuite permis de conclure. Ces incidents ne sont pas des bugs de l'application.
- Nettoyage des vérifications : Next a régénéré `next-env.d.ts` pendant le build. La tentative de restauration Git a échoué car `.git/index.lock` n'était pas accessible en écriture ; une lecture programmatique pour normaliser ce fichier a aussi été refusée par le garde. Son contenu initial connu et ses fins de ligne ont ensuite été rétablis directement, et le statut Git confirme l'absence de modification applicative.
- Statut : diagnostic et propositions uniquement. Aucun correctif applicatif, migration, commit ou déploiement. Prochaine étape utile : corriger les bugs de forfait/timers/reçus/présence avec leurs tests de régression, puis réaliser la recette transactionnelle sur une base isolée.

### 11/09/2026 — Vérifications d’outillage pour l’implémentation complète

- Problème : la CLI Supabase installée (`2.104.0`) ne peut pas consulter les projets distants dans ce shell car aucun `SUPABASE_ACCESS_TOKEN` n’est fourni ; la première commande sans désactivation de télémétrie a aussi été bloquée par une écriture EPERM dans `~/.supabase/telemetry.json`.
- Résolution partielle : `SUPABASE_TELEMETRY_DISABLED=1 supabase --version` fonctionne. La vérification distante reste en attente d’une authentification/connexion effective ; aucune migration distante n’est déclarée appliquée.
- Problème : l’exécution non interactive OpenCode/Muse a signalé `Error starting FSEvents stream` puis est restée sans réponse dans l’environnement shell isolé ; le catalogue du modèle était toutefois disponible.
- Résolution : un test CLI relancé avec l’accès réseau et aux fichiers nécessaires a fonctionné avec `opencode 1.18.30` et `opencode/muse-spark-1.3-contributor-free`, en retournant `READY` avec un coût `0`. Aucun fichier du dépôt n’a été modifié par OpenCode.
- Statut : le CLI est utilisable ; l’erreur précédente était liée à l’environnement shell isolé. La vérification Supabase distante reste en attente d’une authentification effective.

### 11/09/2026 — Implémentation TTMC À ton niveau (clone, sans commit)

- Demande : implémenter TTMC jouable à deux via CLI uniquement, sans Docker, sans commit ni push, en respectant AGENTS.md, progression.md et les docs 01/02/03/05/08 + fiche `02-ttmc.md`, en réutilisant exactement le pack fourni (22 thèmes, 440 questions). Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Réalisation (arbre de travail uniquement) : `src/games/ttmc/` (config, types, moteur pur, projection, juge déterministe, setup/match, tests moteur/projection/pack), `src/server/ttmc/` (loader via `server_get_ttmc_content()`, juge serveur DeepSeek, tests loader/juge/RPC statiques), page `/jeux/ttmc`, intégration registry (`ready`), home selector, match-game, routes rooms/start/commands/get, worker durable (choose_level_timeout/turn_timeout/advance_reveal/contest_timeout/judge_answer/check_absence), nouvelle migration additive `20260911180000_ttmc_activate.sql` (activation `ready`, sans toucher au pack ni à la RPC fournis). Configs 30/20 et 20/15 fonctionnelles, 50/30 refusée `CONTENT_UNAVAILABLE` avec 22 thèmes. Toutes les mutations passent `commitMatch` côté serveur ; `byLevel`/réponses privées jamais exposées avant reveal.
- Vérifications : `pnpm typecheck` OK, `pnpm lint` OK (0 erreur), `pnpm test` OK (18 fichiers / 100 tests), build `next build --webpack` OK (route `/jeux/ttmc` présente), `pnpm docs:check` OK (22 fichiers). E2E `tests/e2e/ttmc.spec.ts` ajoutée (ignorée sans `E2E_PASSWORD`, comme les autres jeux). Pack `content/quiz/dist/ttmc.json` non modifié.
- Non vérifiable sans Supabase distant (ni Docker/CLI ici) : exécution réelle des migrations/RPC en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, benchmark DeepSeek daté. Aucune migration n'est déclarée appliquée en production ; les trois fichiers SQL TTMC restent non committés dans ce clone.
- Statut : code présent, validé localement. Validation Supabase distante non faite. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Typecheck du nouveau test TTMC

- Problème : `pnpm typecheck` échouait sur `src/games/ttmc/engine.test.ts` (Transition confondue avec State : `scores`/`phase` inexistants sur `TtmcTransition`). Cause confirmée : helper de test qui étalait la Transition au lieu de son `.state`.
- Résolution : le helper déballe désormais `.state` là où un état est attendu ; les cas utilisant la Transition (deadlines) sont inchangés.
- Vérification : `pnpm typecheck` OK, puis `pnpm test` OK (18 fichiers / 100 tests).

### 11/09/2026 — Implémentation Skyjo Douze cases (clone, sans commit)

- Demande : implémenter Skyjo jouable à deux via CLI uniquement, sans application OpenCode, sans Docker, sans commit ni push, en respectant AGENTS.md, progression.md et les docs 01/02/03/08 + fiche `04-skyjo.md`, en réutilisant les conventions UNO/TTMC/Trou Noir. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Réalisation (arbre de travail uniquement) : `src/games/skyjo/` (config short/full + 30/60/90, types Zod stricts, moteur pur, projection sans fuite, setup/match, tests moteur/projection), page `/jeux/skyjo`, intégration registre (`ready`), home selector, match-game, routes rooms/start/commands/get, worker durable (preparation_timeout/turn_timeout/advance_reveal/check_absence avec garde phaseId), migration additive `20260911180344_skyjo_activate.sql` créée avec la CLI (`supabase migration new`, télémétrie désactivée), sans toucher aux migrations existantes. Règles : paquet 150 (5×-2, 15×0, 10×-1, 10×1..12), setup simultané 60 s (plus forte somme commence, égalité aléatoire serveur, manches suivantes alternées), budget unique par tour conservé à la prise, colonnes vers `removed`, dernier tour exact, pénalité doublée seulement si brute positive non strictement inférieure (15→30 vs 12, 10→20 vs 10, -2 vs -3 inchangé), timeouts par sous-phase, recyclage en conservant le sommet, cap 200 tours sans pénalité, short 3 manches, full seuil 100 ou 20 manches, plus petit cumul gagne, égalité possible, RESIGN/CLAIM_FORFEIT/absence communs. Aucune logique de jeu dans le client ; erreurs lisibles mappées côté serveur.
- Vérifications : `pnpm typecheck` OK, `pnpm lint` OK, `pnpm test` OK (20 fichiers / 158 tests, dont 42 Skyjo), `pnpm exec next build --webpack` OK (route `/jeux/skyjo` présente), `pnpm docs:check` OK (22 fichiers). `next-env.d.ts` régénéré par le build puis restauré. Pendant le développement, deux échecs de tests Skyjo dus aux maquettes (colonnes uniformes parasites) ont été corrigés en rendant les grilles de test non uniformes hors colonne visée ; `pnpm typecheck` signalait un import orphelin et `pnpm lint` une réassignation inutile, tous deux corrigés.
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, E2E Skyjo. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers Skyjo restent non committés dans ce clone.
- Statut : code présent, validé localement. Validation Supabase distante non faite. Aucun commit, push, déploiement ou migration distante.

### Format des prochaines entrées

`date — problème` : signalement ou erreur, contexte, cause si connue, correction ou statut actuel, puis test ou vérification effectuée.

### 11/09/2026 — Implémentation BombParty Syllabe Express (clone, sans commit)

- Demande : première version complète via CLI uniquement, sans application OpenCode, sans Docker, sans commit ni push, en respectant AGENTS.md, progression.md et les docs 01/02/03/05/08 + fiche `06-bombparty.md`, avec le pack fourni (`content/bombparty/lexicon.json`, packId `bombparty-fr-seed-2026-09`, 431 formes, CC0-1.0, sha256 `a6b74cd6950107f917b7d206b8601d80f15876fe6139fe30318e9514c22a0a39`) non remplacé, non élargi, sans génération de mot à la volée. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Réalisation (arbre de travail uniquement) : `src/games/bombparty/` (config 3/5 vies + 10/15/20 s + easy/normal/hard, `normalize.ts` stricte, types Zod, moteur pur `bombparty-1`/`bombparty-engine-1`, projection sans fuite, helpers d'entraînement purs tri/pagination/vérification/indice, 4 suites de tests), `src/server/bombparty/content.ts` (lecture fichier, checksum sha256, déduplication normalisée, index séquence→mots trié, cache immuable, garde `hasActiveBombpartyMatch`), registre `ready`, routes rooms/start/commands/get, 3 routes d'entraînement (sequence/suggestions/check, refus `TRAINING_BLOCKED_DURING_MATCH` pendant un match actif), worker durable (`turn_timeout` + `check_absence`, garde `isBombpartyDeadlineJobStale`), codes d'erreur FR (`WORD_*`, `TRAINING_*`), UI compétition (`/jeux/bombparty`, séquence centrale, input autofocus/Entrée, vies, timer serveur, mots acceptés surlignés) + entraînement (`/entrainement/syllabes`, libre/chrono 5..30 s, indice, suggestions paginées, stats de session), E2E deux sessions (ignorée sans `E2E_PASSWORD`), migration CLI `20260911183307_bombparty_activate.sql` (`ready` + `server_has_active_match`, non appliquée à distance). Règles : `max(5, initial - floor(valides/6))`, 200 tours (vies puis mots puis draw, reason `turn_limit`), `dictionary_exhausted` en draw, refus sans commit (version/deadline inchangées), RESIGN/CLAIM_FORFEIT (abandon avant premier tour, sinon vainqueur), absence 120 s/180 s. `content/bombparty/lexicon.json` inchangé (checksum revérifié après travaux).
- Vérifications : `pnpm test` OK (25 fichiers / 211 tests, dont 45 BombParty), `pnpm exec next build --webpack` OK (routes `/jeux/bombparty`, `/entrainement/syllabes`, 3 routes training présentes), `pnpm typecheck` OK, `pnpm lint` OK (0 erreur), `pnpm docs:check` OK (22 fichiers). `next-env.d.ts` régénéré par le build puis restauré. Cycle 1 de comparaison : 26 fichiers / 218 tests (dont 52 BombParty), mêmes contrôles verts dans l'ordre test/build/typecheck/lint/docs:check ; voir l'entrée dédiée ci-dessous.
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration/RPC en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, mesure latence p95 <2 s, E2E BombParty. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers BombParty restent non committés dans ce clone.
- Statut : code présent, validé localement. Validation Supabase distante non faite. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 1 de revue croisée Trou Noir (clone, sans commit)

- Demande : base retenue provisoirement ; corriger loader contenu via RPC, compter `contestsAccepted`, vérifier judging/timeouts/remplacements/forfait/double NEXT, conserver 120 questions et design, lancer typecheck/lint/test. Aucune contradiction avec AGENTS.md ; aucune autorisation de commit, push, Docker ou modification de l'application OpenCode n'est déduite. Journal sans contradiction inventée.
- Corrections : `src/server/quiz/content.ts` n'interroge plus `content_packs`/`content_items` via PostgREST ; lecture via `server_get_quiz_content()` (nouvelle migration `20260911130000_trou_noir_quiz_rpc.sql`, réponse plate validée par `quizQuestionSchema`, `numericValue` null normalisé). `perPlayerStatsSchema` + `emptyStats` + `applyCloseOfReveal` + `resultFor` comptent `contestsAccepted` lors d'une contestation acceptée ; `questionsPlayed = correct + incorrect` (les timeouts sont déjà inclus dans incorrect). Remplacements ambigus : 2 remplacements autorisés, abandon au 3e void (`> MAX` au lieu de `>= MAX`). Projection : `NEXT` masqué après sa propre confirmation et pendant contestation pending. Tests ajoutés : judging (seconde réponse refusée, verdict périmé), timeouts (contest_timeout maintient le rejet, advance_reveal périmée après clôture), non-double-comptage contestation + ResultSpec, forfait RESIGN/CLAIM_FORFEIT/abandon avant premier tour, double NEXT, RPC plate, visibilité NEXT/RESOLVE_CONTEST.
- Vérifications : `pnpm typecheck` OK, `pnpm lint` OK, `pnpm test` OK (11 fichiers / 60 tests). Pack `content/quiz/dist/trou-noir.json` conservé à 120 questions ; aucun changement de design violet/sauge. SQL non exécuté localement (pas de Docker/CLI Supabase dans cette tâche) ; recette à deux sessions et benchmark DeepSeek restant à faire.
- Statut : corrections présentes dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 2 de revue croisée Trou Noir (clone, sans commit)

- Demande : repartir des corrections du cycle 1 ; auditer puis corriger si nécessaire les 9 points (RPC `server_get_quiz_content`, anti-lecture private, contestsAccepted/questionsPlayed/score, guards phase/attemptId/lease/deadlines, remplacements ambigus, RESIGN/CLAIM_FORFEIT/absence, double NEXT, projection sans fuite, routes/worker/UI/E2E 120 questions et thème) ; ajouter/corriger les tests ; lancer typecheck/lint/test/build. Travail via CLI uniquement, sans Docker, sans application OpenCode, sans commit ni push. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Audit : (1) RPC conforme — `SECURITY INVOKER`, `search_path = ''`, `private.*` qualifié, `jsonb_strip_nulls`, forme plate compatible `quizQuestionSchema` (`numericValue` null strippé puis normalisé), `ORDER BY` interne à `jsonb_agg`, `revoke public/anon/authenticated` + `grant service_role`, `create or replace` rejouable, ordre lexicographique `...120000` < `...130000`, pack `...120000` idempotent (`on conflict do update/do nothing`, sans DROP) et cohérent (slug `trou-noir`, `published`, 120 inserts vérifiés). (2) Aucune lecture PostgREST directe des tables private dans `src` (recherche `content_packs|content_items|private\.` sans résultat ; loader via `admin.rpc` uniquement). (3) `contestsAccepted` incrémenté une seule fois sur contestation acceptée, `questionsPlayed = correct + incorrect` (timeouts inclus dans incorrect), timeout `-10` non contestable, `contest_timeout` expiré maintient le rejet, `ResultSpec` exposant les compteurs. (4) Guards moteur (phase, `NOT_YOUR_TURN`, `ALREADY_SUBMITTED`, `attemptId` vs `STALE_DEADLINE`, `CONTEST_PENDING`, `ALREADY_ACKNOWLEDGED`, `NO_CONTEST_PENDING`) et worker (phase + `attemptId` vérifiés avant jugement, verdict périmé annulé, ancien job inopérant par `previousPhaseId`) conformes. (5) 2 remplacements puis abandon technique au 3e void (`consecutive > MAX`), événement sans fuite. (6) RESIGN tardif => adversaire gagne, CLAIM_FORFEIT tardif => demandeur gagne, avant premier tour => `abandoned` sans vainqueur, absence => `abandoned` sans vainqueur. (7) NEXT exige deux `actorId` participants distincts (contrôle `seatForActor` + `ALREADY_ACKNOWLEDGED`). (8) Projection pré-reveal sans prompt-fuite : ni `schedule`, `replacements`, `canonical`, `aliases`, `logicalKey`, `packId`, `itemId`, `themeLabel`, `pendingVerdict`, ni explication ; reveal avec `expectedAnswer`/`explanation` autorisés mais sans `aliases`. (9) Routes start/commands, worker, UI `geo-*` violet/sauge, page `/jeux/trou-noir`, E2E à deux sessions (ignorée sans `E2E_PASSWORD`), pack 120 questions confirmé (`trou-noir.json` : 120 ; migration : 120 inserts).
- Corrections du cycle 2 (arbre de travail uniquement) : `engine.test.ts` — assertion morte `expect(() => onTrouNoirDeadline(...))` sans matcher remplacée par une vraie vérification de convergence idempotente (`contest_timeout` sur état résolu ferme exactement une fois avec `correct=1`/`contestsAccepted=1`, puis deadline rejouée => `STALE_DEADLINE`) ; ajout du cas `CLAIM_FORFEIT` avant premier tour (`abandoned`, sans vainqueur, raison `claimed_forfeit`). `projection.test.ts` durci (clés `aliases/logicalKey/packId/itemId/themeLabel/pendingVerdict`, explication et `schedule` absents pré-reveal ; reveal sans `aliases/logicalKey/schedule`). Nouveau `src/server/quiz/rpc.test.ts` (4 tests) verrouillant sans base : ordre lexicographique et rejouabilité des deux migrations, `SECURITY INVOKER`/`search_path` vide/`private.*`/`jsonb_strip_nulls`/absence-de-résultat, forme plate + grants/revokes, interdiction de lecture PostgREST directe des tables private dans le loader.
- Vérifications : `pnpm typecheck` OK, `pnpm lint` OK, `pnpm test` OK (12 fichiers / 64 tests), `pnpm build` OK (routes `/jeux/trou-noir`, `/api/matches/[matchId]/commands`, `/api/internal/jobs/run` présentes).
- Non vérifiable sans Supabase distant (ni Docker/CLI ici) : exécution réelle de `server_get_quiz_content()` en base (résolution UUID->jsonb, `ORDER BY` dans `jsonb_agg`, comportement 0-ligne sans pack, application effective des `revoke/grant` et idempotence par rejouement), historique/hypothèses d'objets distants préexistants, recette à deux sessions avec `commitMatch`/`lease`/`deadlines` réels, benchmark DeepSeek daté. Aucune migration n'est déclarée appliquée en production ; les deux fichiers SQL restent non committés dans ce clone.
- Statut : audit + durcissements présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Validation du build après intégration Trou Noir

- Problème : `pnpm build` avec le bundler Turbopack échoue dans cet environnement avant compilation (`Failed to write app endpoint /page`, processus CSS incapable de binder un port, `Operation not permitted`). Cause confirmée comme restriction système/outillage ; ce n’est pas une erreur TypeScript ou applicative.
- Résolution : `pnpm exec next build --webpack` compile et génère toutes les pages/routes avec succès ; le projet garde sa configuration existante, sans contournement ajouté pour Turbopack. Le fichier généré `next-env.d.ts` a été restauré à son contenu versionné.
- Vérifications : `pnpm typecheck`, `pnpm lint` et `pnpm test` passent dans `main` (12 fichiers / 64 tests) ; build webpack OK. Le build Turbopack reste un incident d’environnement documenté.

### 11/09/2026 — Cycle 1 de revue comparative TTMC (clone, sans commit)

- Demande : auditer puis corriger le clone TTMC contre la référence, AGENTS.md, docs 01/02/03/05/08 et fiche `02-ttmc.md`, via CLI uniquement, sans Docker, sans application OpenCode, sans commit ni push, pack partagé contractuel strictement inchangé (22 thèmes, 440 questions). Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Audit (code réellement inspecté : moteur, projection, juge, loader, worker, routes rooms/start/commands/get, registry, UI setup/match, 3 migrations, E2E) : moteur pur à deux joueurs, seat/phaseId/échéances conformes ; choix 1..10, thème fixé au démarrage, paires distinctes par siège, file de secours 4/niveau sans remise ni répétition, 50/30 refusé `CONTENT_UNAVAILABLE` ; score seulement à la clôture après deux NEXT distincts, fin après manche complète, draw sans départage ; contestation après rejet non-timeout, résolution adversaire, crédit exact une fois ; timeouts choix/réponse, RESIGN/CLAIM_FORFEIT, absence 120 s/180 s ; judge déterministe puis DeepSeek serveur seul, aucune fuite pré-reveal ; `commitMatch`, idempotence par reçu, worker avec baux, RPC `service_role` seule, aucun accès PostgREST direct aux tables private ; routes conformes aux conventions existantes ; UI française violette/sauge ; E2E deux sessions ignorée sans `E2E_PASSWORD`, sans prétendre l'avoir exécutée.
- Divergences réelles corrigées (arbre de travail uniquement) : (1) remplacement technique invisible — ajout de `technicalReplacement` à `TtmvView`/`projectTtmc` (depuis `replacementCount > 0`, sans exposer la file privée) et notice UI « Question de remplacement : incident technique, même niveau, sans pénalité » ; (2) option 50 proposée alors que le pack couvre 22 thèmes — option désactivée avec mention « 32 thèmes requis, pack actuel : 22 » + note que le serveur refuse son démarrage. Tests ajoutés sans faux test : moteur (seconde réponse refusée, contestation après accept/mauvais attemptId, NEXT pendant contest => `CONTEST_PENDING`, RESOLVE mauvais attemptId => `NO_CONTEST_PENDING`, `contest_timeout` expiré clôt à 0, RESIGN pendant reveal sans cumul du delta + job périmé ensuite `MATCH_FINISHED`, `CLAIM_FORFEIT` avant premier tour => `abandoned`, seuils d'absence 120 s/180 s), projection (absence de `pendingMethod`/`normalizedAnswer`/`rawAnswer`/`canonical`/`themeLabel` pré-reveal, reveal sans `spareByLevel`/`logicalKey`/`pendingVerdict`, drapeau remplacement), juge serveur (alias normalisé accepté, entité fausse non devinée), RPC statique (rejouabilité `create or replace`, `order by`, pas de `truncate`, conflits idempotents).
- Vérifications : `pnpm typecheck` OK, `pnpm lint` OK (0 erreur), `pnpm test` OK (18 fichiers / 110 tests), `pnpm exec next build --webpack` OK (route `/jeux/ttmc` présente), `pnpm docs:check` OK (22 fichiers). `next-env.d.ts` régénéré par le build puis restauré. Pack `content/quiz/dist/ttmc.json`, manifest et scripts/migrations de contenu non régénérés ni modifiés.
- Non vérifiable sans Supabase distant (ni Docker/CLI ici) : exécution réelle des migrations/RPC en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, benchmark DeepSeek daté. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers TTMC restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 2 de revue finale TTMC (clone, sans commit)

- Demande : release gate contradictoire avant sélection, via CLI uniquement, sans application OpenCode, sans Docker, sans commit ni push, pack partagé immuable (22 thèmes, 440 questions, manifest/seeds/scripts/migrations de contenu non modifiés). Mêmes contrats (AGENTS.md, docs 01/02/03/05/08, fiche `02-ttmc.md`). Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Revue (code réellement relu : moteur, projection, juge jeu/serveur, loader, worker, routes commands/get/start, registry, UI setup/match/page, 3 migrations, E2E) : pur à deux sièges, acteur cohérent du siège actif, idempotence par reçu/version/`previousPhaseId`, `nextPhaseId` UUID frais ; paires distinctes, file 4/niveau sans remise, 20/15 et 30/20 OK, 50/30 refusé ; score une seule fois à la clôture après deux NEXT distincts, manche complète, draw, resign/forfeit/absence ; juge déterministe puis DeepSeek serveur seul, réponse bornée 240 et instruction-safe, ambiguous sans pénalité, contestation seulement rejet non-timeout par l'adversaire, crédit une fois ; whitelist stricte (ni canonical/aliases/explication/itemId/packId/logicalKey/normalizedAnswer/verdict/method/file de réserves avant reveal, niveau confirmé seul) ; `commitMatch`, vues parallèles, RPC `service_role` seule, aucun accès PostgREST direct aux tables private ; UI `geo-*` violette/sauge, mobile/clavier, chargement/erreur, boutons désactivés pendant envoi, timer sur heure serveur.
- Faiblesses réelles corrigées (arbre de travail uniquement) : (1) ancien job d'échéance rejouant sur une nouvelle phase de même nom — le `turn_timeout` d'avant remplacement clôturait la phase answering de remplacement (temps complet volé), idem `choose_level_timeout` sur manche suivante ; ajout de `isTtmcDeadlineJobStale(jobPhaseId, phaseId)` dans le moteur et garde dans `processTtmcJob` (cancel `STALE_DEADLINE` sans mutation ; jobs `judge_answer` exclus, gardés par `attemptId`) ; (2) `RESOLVE_CONTEST` forçait le repli `contest_timeout` même sans échéance courante — conserve désormais l'échéance en cours, repli `advance_reveal` frais à 12 s seulement si absente ; (3) page `/jeux/ttmc` annonçait « 20 / 30 / 50 — Cibles au choix » alors que 50 est désactivé — pastille corrigée en « 20 / 30 — Cibles disponibles ». Tests comportementaux ajoutés (6) : garde phaseId (démontre la mutation sans garde puis l'interdiction), unicité globale des 440 questions allouées + remplacement sans répétition et hors `answeredCount`, conservation/repli d'échéance après RESOLVE, jugement après RESIGN en judging => `STALE_DEADLINE`, seconde contestation après résolution => `CONTEST_ALREADY_OPEN`, whitelist exhaustive par clés sur choose/answering/judging/reveal pour les deux spectateurs.
- Vérifications : `pnpm typecheck` OK, `pnpm lint` OK (0 erreur), `pnpm test` OK (18 fichiers / 116 tests), `pnpm exec next build --webpack` OK (routes `/jeux/ttmc`, `/api/matches/[matchId]/commands`, `/api/internal/jobs/run` présentes), `pnpm docs:check` OK (22 fichiers). `next-env.d.ts` régénéré par le build puis restauré. Pack `content/quiz/dist/ttmc.json`, manifest, seeds, scripts et migrations de contenu non régénérés ni modifiés.
- Non vérifiable sans Supabase distant (ni Docker/CLI ici) : exécution réelle des migrations/RPC en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, E2E TTMC (ignorée sans `E2E_PASSWORD`), benchmark DeepSeek daté. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers TTMC restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 1 de revue comparative Skyjo (clone, sans commit)

- Demande : relire tout le Skyjo du clone contre `docs/games/04-skyjo.md`, `docs/08-engine-contracts.md` et une implémentation concurrente, point par point (projection, setup, 150 cartes, sommes/alternance, budget, sources, remplacements, colonnes, dernier tour, pénalité, cap 200, formats, NEXT/timeout, forfaits, jobs/phaseId, routes/worker/migration/registre/UI) ; inspecter le code, ajouter/corriger les tests des cas limites, corriger immédiatement ; exécuter test, build webpack, typecheck, lint, docs:check dans cet ordre ; sans commit/push, sans Docker, sans application OpenCode. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Audit (code réellement relu : moteur, projection, config, setup/match, page, registre, routes rooms/start/commands/get, worker, migration, http) : paquet 150 exact (5×-2, 15×0, 10×-1, 10×1..12, IDs uniques), conservation vérifiée, setup simultané 60 s avec révélation immédiate par confirmation (conforme fiche), égalité départagée par entropie serveur, alternance via `startNextRound`, budget conservé à la prise (`sameBudgetTransition` + test), recyclage en gardant le sommet, `removed` jamais recyclé, REPLACE/DISCARD_AND_REVEAL bien séparés, pénalité stricte (15→30 vs 12, 10→20 vs 10, négatifs jamais doublés), cap 200 sans pénalité, short 3 / full 100-ou-20, NEXT double distinct, RESIGN/CLAIM_FORFEIT (abandon avant premier tour, sinon vainqueur), absence 120 s/180 s, garde `isSkyjoDeadlineJobStale` dans moteur + worker, erreurs lisibles mappées, migration additive CLI comme UNO/TTMC, registre `ready`, UI violette `#6d28d9` sans logique de jeu.
- Défauts réels corrigés (arbre de travail uniquement) : (1) dernier tour — `completeTurn` ne terminait que si l'adversaire avait lui aussi tout révélé, prolongeant la manche au-delà de l'unique dernier tour ; termine désormais après le tour adverse dès que `closingSeat` est posé par l'autre siège, quel que soit l'état de sa grille ; (2) `finishRound` comptait les colonnes révélées en fin de manche dans `roundSummary.clears` mais pas dans `counters.columnClears` — ajout du cumul par siège ; (3) grille setup incliquable — `disabled` incluait `!isMyTurn` alors qu'`activePlayerId` est nul en setup simultané, bloquant la sélection des deux cartes ; exempté le setup ; (4) `allowedActions` proposait `TAKE_DRAW` même sans carte recyclable (pioche vide + ≤1 défausse) — masqué côté projection, le serveur restant l'autorité (`DRAW_UNAVAILABLE`). Tests ajoutés (5) : dernier tour avec adversaire encore caché, budget conservé sur `TAKE_DRAW`, colonnes de révélation finale et compteurs, recyclage sans toucher à `removed`, `TAKE_DRAW` masqué/offert selon recyclable ; test NEXT durci (alternance 0→1, cumulé conservé, 150 cartes en manche 2).
- Vérifications : `pnpm test` OK (20 fichiers / 163 tests), `pnpm exec next build --webpack` OK (route `/jeux/skyjo` présente), `pnpm typecheck` OK, `pnpm lint` OK (0 erreur), `pnpm docs:check` OK (22 fichiers). `next-env.d.ts` régénéré par le build puis restauré.
- Non vérifiable sans Supabase distant (ni Docker/CLI ici) : exécution réelle de la migration en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, E2E Skyjo. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers Skyjo restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 2 de revue finale Skyjo (clone, sans commit)

- Demande : release gate finale après le cycle 1, sans supposer que les 163 tests suffisent ; relire moteur, types/schemas, projection, UI, routes et worker contre `docs/games/04-skyjo.md`, `docs/08-engine-contracts.md` et AGENTS.md, en cherchant activement tout défaut restant sur (a) projection/setup, (b) 150 cartes/recyclage, (c) sièges/phases/deadlines/phaseId, (d) premier siège/formats, (e) dernier tour/scores/pénalité/cap, (f) actions impossibles/HTTP, (g) forfaits/commitMatch, (h) UI ; corriger le réel, ajouter des régressions sans modifier la fiche ni inventer d'exception ; exécuter test, build webpack, typecheck, lint, docs:check ; restaurer `next-env.d.ts` ; journaliser ; sans commit/push, sans Docker ni application OpenCode. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Audit (code réellement relu : `src/games/skyjo/engine.ts`, `types.ts`, `projection.ts`, `config.ts`, `engine.test.ts`, `projection.test.ts`, `components/skyjo-setup.tsx`, `components/skyjo-match.tsx`, `src/app/jeux/skyjo/page.tsx`, registre, routes rooms/start/commands/get, `src/server/jobs/worker.ts`, `src/server/http.ts`, migration `20260911180344_skyjo_activate.sql`) : (a) projection sans ID/valeur cachée, sans `drawPile`/`initialSums`/`heldSource`, `held` actif seul et `{hidden:true}` adverse, `discardTop`+comptes seuls, setup simultané par siège avec `activePlayerId` nul, révélation immédiate par confirmation conforme à la fiche ; (b) 150 cartes conservées à chaque transition, recyclage avec sommet gardé et `removed` exclu ; (c) `activeSeat`/`actorSeat` vérifiés, un seul job par transition, `sameBudgetTransition` conserve l'échéance après TAKE_DRAW/TAKE_DISCARD, garde `isSkyjoDeadlineJobStale` moteur + worker, timeouts setup/choose/resolve/replace/reveal couverts ; (d) somme manche 1 puis alternance, short 3 / full 100-ou-20, plus petit cumul gagne, égalité possible ; (e) un seul dernier tour même adversaire incomplet, colonnes vers `removed`, pénalité stricte (15→30, 10→20, négatifs jamais doublés), cap 200 sans pénalité ; (f) WRONG_PHASE/NOT_YOUR_TURN/INVALID_SLOTS/SLOT_EMPTY/SLOT_NOT_HIDDEN/SLOT_ALREADY_REVEALED/DRAW_UNAVAILABLE/REPLACE_NOT_ALLOWED/ILLEGAL_MOVE/ALREADY_SUBMITTED/ALREADY_ACKNOWLEDGED/MATCH_FINISHED/STALE_DEADLINE/MATCH_BLOCKED mappés, double pile vide en incident technique sans carte inventée ; (g) RESIGN/CLAIM_FORFEIT (abandon avant premier tour, sinon vainqueur adverse/demandeur), absence 120 s/180 s vers `abandoned`, mutations via `commitMatch` avec vues parallèles ; (h) UI violette `#6d28d9` responsive, `aria-label`/`aria-pressed`/`aria-live`, boutons gouvernés par `allowedActions`.
- Défauts réels corrigés (arbre de travail uniquement) : (1) projection incohérente — `players[].active` restait vrai pour le siège 0 en `setup` et `activePlayerId` restait renseigné en `round_reveal` alors que les aides interdisent d'agir en reveal ; `setup` exclu des phases à tour et `round_reveal` renvoie désormais `activePlayerId: null` ; (2) `initializeSkyjo` forçait `activeSeat: 0` même avec `firstSeat: 1` ; initialisé sur le premier siège configuré (sans effet après setup manche 1, qui recalcule au plus fort) ; (3) `roundSummary.turns` et `roundRecords[].summary.turns` comptaient `state.turns + 1` alors que `completeTurn` avait déjà incrémenté, avec compensation `turns: 199` au cap 200 laissant l'état à 199 ; comptent désormais `state.turns`, le cap conserve `turns: 200` et le résumé vaut 200 sans pénalité. Tests de régression ajoutés (3) : init `firstSeat: 1` → `activeSeat: 1`, résumé à 2 tours après deux coups complets, cap 200 avec état et résumé à 200 ; projection sans joueur actif en setup ni en reveal ; durcissement du test de cap existant.
- Vérifications : `pnpm test` OK (20 fichiers / 166 tests), `pnpm exec next build --webpack` OK (route `/jeux/skyjo` présente), `pnpm typecheck` OK, `pnpm lint` OK (0 erreur), `pnpm docs:check` OK (22 fichiers). `next-env.d.ts` régénéré par le build puis restauré via `git checkout`.
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, E2E Skyjo. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers Skyjo restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Garde `server-only` dans le test du chargeur BombParty

- Problème : `src/server/bombparty/content.test.ts` échouait au chargement (`This module cannot be imported from a Client Component module`) car le chargeur importe `server-only`.
- Cause confirmée : l'environnement Vitest n'applique pas la condition `react-server` ; les suites serveur existantes (`quiz/judge.test.ts`, `ttmc/content.test.ts`) neutralisent déjà ce garde par mock.
- Résolution : `vi.mock("server-only", () => ({}))` en tête de suite, comme les suites existantes.
- Vérification : `pnpm test src/games/bombparty src/server/bombparty` OK (5 fichiers / 45 tests).

### 11/09/2026 — Typecheck du composant d'entraînement BombParty

- Problème : `pnpm typecheck` échouait dans `training-syllabes.tsx` (`data.sequence` possiblement `undefined` dans la fermeture du chrono).
- Cause confirmée : réponse JSON typée avec `sequence?` optionnelle.
- Résolution : garde `if (data.sequence)` avant l'appel à `onExpire`.
- Vérification : `pnpm typecheck` OK, puis `pnpm test` OK (25 fichiers / 211 tests).

### 11/09/2026 — Migration BombParty voisine non rédigée ici

- Constat : une migration non suivie `supabase/migrations/20260911183429_bombparty_ready.sql` (fonction `server_has_active_game`, activation `ready`) est apparue dans l'arbre pendant la tâche ; hypothèse non confirmée : fichier déposé par le contexte partagé avec l'implémentation concurrente. Elle n'a été ni créée, ni modifiée, ni supprimée ici.
- Décision : la migration rédigée dans cette tâche (`20260911183307_bombparty_activate.sql`, fonction `server_has_active_match`) est disjointe en nom ; les routes n'utilisent que cette dernière. Les deux `update public.games` sont idempotents. Aucune migration n'est déclarée appliquée en production.
- Vérification : `pnpm docs:check` OK ; cohérence à revalider sur base isolée (une seule des deux fonctions devrait subsister à terme).

### 11/09/2026 — Cycle 1 de comparaison BombParty (clone, sans commit)

- Demande : comparer l'implémentation du clone à la version Codex concurrente (`/Users/thibaudfrancony/Documents/ChatGPT/tibo.fun`, fichiers BombParty non committés uniquement), sans copier aveuglément ; relire AGENTS.md, fiche `06-bombparty.md` et docs 01/02/03/05/08 ; vérifier pack partagé (431 formes, sha256 `a6b74cd6…a39`), normalisation, index privé sans fuite, séquence difficulté/5 candidats/recent/épuisement, vies/départage, tour/200, durée et plancher, refus sans mutation ni prolongation, mot accepté/métriques, deadline DB et job `turn_timeout` avec `phaseId`, replay/idempotence, resign/claim/absence, routes/worker/registry/http, entraînement refusé en match actif et pagination sans doublons, UI violette/accessibilité ; chercher des défauts même si les tests passent, ajouter des régressions et corriger ; garder uniquement la migration `20260911183307_bombparty_activate.sql` si cohérente ; ne modifier ni le lexique ni la fiche ; lancer test, build webpack, typecheck, lint, docs:check dans cet ordre. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée. Aucun commit ni push.
- Audit (code réellement relu des deux côtés : engine, normalize, types, config, projection, training, content loader, worker, routes commands/start, training routes, http, registry, UI match/setup, migrations, E2E) : socle local conforme — pack 431 formes inchangé (checksum revérifié `a6b74cd6…a39`), normalisation stricte accents/œ/æ/ponctuation, index `sequence→mots` trié privé sans fuite (aucun import lexique côté client, projection sans `bySequence`/`usedWords`/`turnStartedAt`/`packChecksum`), catégories easy ≥200/normal 50..199/hard 10..49 avec ≥5 inutilisés, évitement des 5 dernières, élargissement puis draw `dictionary_exhausted`, vies 3/5 puis mots puis draw (`turn_limit`), tour courant initial 1 selon contrat 08, durée `max(5, initial − floor(valides/6))`, refus sans commit (version/deadline inchangées, saisie conservée, chrono non réinitialisé), deadline DB via `server_commit_match` + job `turn_timeout` par tour avec garde `isBombpartyDeadlineJobStale` moteur + worker, idempotence par reçu/commandHash, RESIGN/CLAIM_FORFEIT (abandon avant premier tour, sinon vainqueur adverse/demandeur), absence 120 s à deux/180 s seul vers `abandoned`, registre `ready`, entraînement solo refusé `TRAINING_BLOCKED_DURING_MATCH` sur les 3 routes, pagination opaque sans doublons, UI violette `#6d28d9` sobre (autofocus conditionnel, Entrée, `autoCapitalize=none`, `aria-live`, erreurs `role=alert`). Divergences concurrentes relevées sans reprise : tour initial 0 (contraire au contrat 08), RESIGN donnant la victoire au démissionnaire (au lieu de l'adversaire), `Math.random` dans le tirage d'entraînement (au lieu d'entropie serveur), `autoFocus` inconditionnel, absence de tests projection/normalisation/training et de codes `WORD_*`/`TRAINING_UNAVAILABLE` côté http.
- Défauts réels corrigés (arbre de travail uniquement) : (1) checksum partagé non verrouillé — le chargeur calculait le sha256 sans le comparer à la valeur contractuelle, dérive silencieuse possible ; ajout de `EXPECTED_CHECKSUM` avec rejet `BOMBPARTY_CONTENT_MISMATCH` et assertion exacte en test ; (2) surlignage de la séquence cassé sur affichage accentué — `highlightSequence` cherchait dans `toLowerCase()` brut (`École` vs `eco` introuvable) ; nouveau module pur `highlight.ts` (`findSequenceRange`/`splitAroundSequence` avec alignement normalisé caractère par caractère, ligature œ→oe couverte) utilisé par le composant, avec 4 tests ; (3) pastilles de vies figées à 5 cercles — trompeur en config 3 vies (3●2○ lu comme 2 vies perdues) ; pleins seuls + nombre, `aria-label` inchangé ; compteur `Tour` plafonné à `maxTurns` en affichage (le tour clos 200 portait l'état fini à 201). Tests de régression ajoutés (7) : checksum exact, surlignage (accent/ligature/casse/absent), sélection (exclusion <5 inutilisés avec élargissement, évitement recent, rejeu de la seule restante). Écart volontairement conservé : repli final `>=1` sans filtre de catégorie (un filtrage strict rendrait l'initialisation impossible sur petits pools de fixtures) ; moyenne/longueur globales partagées entre joueurs documentées comme limite, schéma d'état inchangé dans ce cycle.
- Vérifications dans l'ordre demandé : `pnpm test` OK (26 fichiers / 218 tests, dont 52 BombParty), `pnpm exec next build --webpack` OK (routes `/jeux/bombparty`, `/entrainement/syllabes` présentes), `pnpm typecheck` OK, `pnpm lint` OK (0 erreur), `pnpm docs:check` OK (22 fichiers). `next-env.d.ts` régénéré par le build puis restauré. Lexique et fiche non modifiés ; migration partagée `...183429...` ni committée ni modifiée ; migration dédiée `...183307...` conservée, cohérente avec les routes (`server_has_active_match`).
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle des migrations/RPC en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, E2E BombParty (ignorée sans `E2E_PASSWORD`). Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers BombParty restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 2 et dernier contrôle BombParty (clone, sans commit)

- Demande : revue indépendante du code actuel puis correction des seuls défauts réels, avec tests ; conserver exactement `content/bombparty/lexicon.json` (431 formes, `bombparty-fr-seed-2026-09`, CC0-1.0, sha256 `a6b74cd6…a39`) ; ne pas modifier `supabase/migrations/20260911183429_bombparty_ready.sql` (contexte concurrent) et garder la seule migration cohérente `20260911183307_bombparty_activate.sql` ; contrôles prioritaires (deadline moteur, worker phase/idempotence, métriques et départage 200 tours, replay, forfaits, séquences/`dictionary_exhausted`, index privé, training, routes/registry/UI) ; ordre test/build/typecheck/lint/docs:check/diff/status ; journalisation réelle sans commit ni push. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Revue (code réellement relu : `engine.ts`, `types.ts`, `config.ts`, `projection.ts`, `normalize.ts`, `training.ts`, `highlight.ts`, `content.ts`, routes commands/start, 3 routes training, `http.ts`, worker `processBombpartyJob`/`processBombpartyAbsenceJob`, registre, UI match/setup/page/entraînement, E2E, migration dédiée) : tour courant initial 1, durée `max(5, initial − floor(valides/6))`, refus sans commit (version/échéance inchangées, saisie conservée), `turn_timeout` par tour avec garde `isBombpartyDeadlineJobStale` moteur + worker, idempotence `commandId`/`commandHash` (hash sans `expectedVersion`), RESIGN→adversaire / CLAIM_FORFEIT→demandeur (abandon sans vainqueur avant premier tour), absence 120 s à deux / 180 s seul vers `abandoned`, catégories easy ≥200/normal 50..199/hard 10..49 avec ≥5 inutilisés, évitement des 5 dernières, élargissement puis draw `dictionary_exhausted`, entraînement refusé `TRAINING_BLOCKED_DURING_MATCH` sur les 3 routes avec pagination opaque max 20 sans doublons, entropie serveur (`entropyValues`, pas de `Math.random`), aucun import lexique côté client, UI `#6d28d9` sobre (autofocus conditionnel au changement de tour, Entrée, `autoCapitalize=none`, `aria-live`, erreurs `role=alert`).
- Défauts réels corrigés (arbre de travail uniquement) : (1) soumission après échéance acceptée par le moteur — `applySubmitWord` ignorait `ctx.currentDeadlineAt` alors que la route le fournit et que la fiche §3 exige le rejet à `dbNow >= deadlineAt` même si le worker n'a pas agi ; ajout d'une garde `DEADLINE_EXPIRED` (déjà mappée 409 côté `http.ts`) avant toute validation/mutation, RESIGN/CLAIM_FORFEIT restant possibles après expiry ; (2) métriques `longestWordLength`/`meanAcceptedResponseMs` globales partagées entre les deux sièges dans `ResultSpec.players[].metrics` — `sumResponseMs`/`responseCount`/`longestWordLength` deviennent des tuples par siège (ordre 0/1), la projection gardant une moyenne globale et un max pour l'affichage ; l'état reste en `schemaVersion: 1` (aucune partie déployée, aucune ligne à migrer). Tests de régression ajoutés (8) : refus à l'échéance exacte sans mutation, refus 30 s après, acceptation 1 ms avant, RESIGN post-échéance, métriques individuelles différenciées après deux tours (longueurs et moyennes distinctes par siège), timeout au 200e tour départagé aux vies après décrément, `dictionary_exhausted` sur acceptation sans séquence suivante, verrou `TRAINING_SUGGESTION_LIMIT === 20` + `packId` absent de la projection sérialisée.
- Écarts volontairement conservés : état fini au tour 201 après clôture du 200e (résumé `turnsPlayed: 200` exact, affichage plafonné à `maxTurns`) ; repli final `>=1` sans filtre de catégorie sur petits pools de fixtures ; vérification serveur des 90 s d'absence avant `CLAIM_FORFEIT` absente comme dans les six autres jeux du socle (convention socle, hors périmètre BombParty).
- Vérifications dans l'ordre demandé : `pnpm test` OK (26 fichiers / 226 tests, dont 60 BombParty), `pnpm exec next build --webpack` OK (routes `/jeux/bombparty`, `/entrainement/syllabes` présentes), `next-env.d.ts` régénéré puis restauré via `git checkout`, `pnpm typecheck` OK, `pnpm lint` OK, `pnpm docs:check` OK (22 fichiers), `git diff --check` OK, `git status` sans modification hors périmètre. Lexique revérifié inchangé (sha256 `a6b74cd6…a39`, 431 formes) ; migration concurrente `...183429...` ni modifiée ni committée ; migration dédiée `...183307...` cohérente avec les routes (`server_has_active_match`).
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration/RPC en base, recette à deux sessions avec `commitMatch`/baux/échéances réels (dont refus tardif et replay au commit), mesure latence p95 <2 s, E2E BombParty (ignorée sans `E2E_PASSWORD`). Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers BombParty restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Implémentation Bataille navale Flotte cachée (clone isolé, sans commit ni push)

- Demande : implémenter Bataille navale jouable à deux via CLI uniquement, sans application OpenCode, sans Docker, sans commit ni push, en respectant AGENTS.md, progression.md, fiche `07-bataille-navale.md` et docs 01/02/03/04/08, en réutilisant les conventions Trou Noir/TTMC/Skyjo/UNO/BombParty. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Réalisation (arbre de travail uniquement) : `src/games/bataille-navale/` (config stricte `{turnSeconds:null|60}` default null, types Zod stricts refusant flottants/hors-limites, moteur pur `bataille-navale-1`/`bataille-navale-engine-1`, projection sans fuite, setup/match, 2 suites de 34 tests), page `/jeux/bataille-navale`, registre `ready`, match-game, routes rooms/start/commands/get, worker durable (`preparation_timeout`/`turn_timeout`/`check_absence` avec garde `phaseId`), migration additive CLI `20260911190958_bataille_navale_activate.sql` (non appliquée à distance). Règles : catalogue serveur 5/4/3/3/2 = 17 cases, rectilignes, contacts autorisés, setup 180 s avec `SET_FLEET` partiel + `RANDOMIZE` backtracking fini (complément conservant les fixés, repli flotte entière), `READY` exige 5 uniques/valides, `UNREADY` avant ready adverse, second `READY` démarre avec premier tireur aléatoire serveur, `FIRE` un tir par tour sans rejouer, répétition `ALREADY_SUBMITTED` sans mutation, victoire immédiate à 17 sans dernier tour, timeout 60 s = tir auto serveur sur case inconnue (peut gagner, message explicite), sans timer deadline null, RESIGN/CLAIM_FORFEIT (abandon sans vainqueur avant premier tir, sinon vainqueur), absence 120 s/180 s vers `abandoned`. Score = cases adverses touchées 0..17, metrics shots/hits/misses/sunkShips/automaticShots/précision nulle si 0, round record avec flottes + journal. UI violette sobre : tirs gauche/flotte droite desktop, onglets mobile, zoom grille, roving tabindex flèches/Entrée, sélection + « Tirer en B7 », aide courte, dernier tir, révélation de fin. Aucun corpus/asset ajouté ; catalogue constant partagé.
- Problèmes rencontrés pendant le développement (causes confirmées, corrigées) : (1) le backtracking consommait ~120+ valeurs d'entropie par niveau, au-delà des 128 fournies par `entropyValues()` en production — remplacé par un PRNG déterministe (mulberry32) amorcé par une seule valeur d'entropie par mélange ; (2) `pnpm lint` signalait un `setState` synchrone dans un `useEffect` du composant de tir — effet supprimé (sélection déjà réinitialisée à la confirmation) ; (3) un test de victoire n'accumulait que 8 touches par alternance stricte au lieu de 16 — boucle réécrite jusqu'à 16 touches ; (4) un test de projection utilisait la même flotte des deux côtés et détectait à tort une fuite — flotte adverse rendue volontairement différente.
- Vérifications : `pnpm test` OK (28 fichiers / 260 tests, dont 34 Bataille navale), `pnpm exec next build --webpack` OK (route `/jeux/bataille-navale` présente), `next-env.d.ts` restauré, `pnpm typecheck` OK, `pnpm lint` OK (0 problème), `pnpm docs:check` OK (22 fichiers), `git diff --check` OK. Pack/lexique BombParty (sha256 `a6b74cd6…a39`) et migrations existantes non modifiés.
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration en base, recette à deux sessions avec `commitMatch`/baux/échéances réels, E2E Bataille navale. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers Bataille navale restent non committés dans ce clone.
- Statut : code présent, validé localement. Validation Supabase distante non faite. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 1 de comparaison Bataille navale (clone, sans commit)

- Demande : comparer l'implémentation du clone à la copie locale Codex (`comparison-codex/`, fichiers de référence uniquement), relire AGENTS.md, fiche `07-bataille-navale.md` et docs 01/02/03/04/08 ; ne modifier ni fiches, ni jeux validés, ni migrations existantes ; inspecter réellement moteur/types/config/projection/tests, routes start/commands/get/rooms, worker, registry, home selector, match UI et migration ; corriger les défauts réels avec tests en retenant les meilleures idées des deux versions ; vérifier soumission FIRE à `dbNow >= deadlineAt` refusée avant mutation, stale jobs, fixtures touchantes sans overlap, fin exacte au dernier hit, accès propre/tiers, arrays et Zod strict ; ordre test/build/typecheck/lint/docs:check/diff/status ; sans application OpenCode, Docker, commit ni push. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée. Lexique BombParty non touché.
- Audit (code réellement relu des deux côtés : engine, types, config, projection, 2 suites de tests, worker `processNavalJob`/`processNavalAbsenceJob`, routes rooms/start/commands/get, `http.ts`, registre, match-game, home selector, UI setup/match, page, migrations) : socle local conforme — deux joueurs, 10×10, catalogue serveur 5/4/3/3/2 = 17 cases, longueurs déduites du catalogue (payload strict sans `length`), contacts autorisés sans overlap/dépassement, setup 180 s avec `SET_FLEET` partiel + `RANDOMIZE` (backtracking fini conservant les fixés, repli flotte entière si aucun complément — conforme fiche §2, la version Codex lève `FLEET_COMPLETION_FAILED` au lieu du repli), `READY` complet/unique + second `READY` démarre une seule fois, `UNREADY` avant ready adverse, premier tireur par entropie serveur, `FIRE` un tir/tour sans rejouer (hit/couler ne rejouent pas), victoire immédiate au 17e sans dernier tour, config stricte `{turnSeconds:null|60}` default null, tir auto serveur sur case inconnue sans lecture de la flotte cachée avec message « Tir automatique » explicite, deadline null sans timer, projection personnelle (flotte propre, tirs reçus/propres, ready adverse, coulés, `shipId` seulement sur coulé, aucune flotte adverse avant finished, révélation en fin aux participants), score = cases touchées 0..17, `commitMatch` avec `previousPhaseId`/reçu idempotent, garde `isNavalDeadlineJobStale` moteur + worker, absence 120 s à deux/180 s seul vers `abandoned`, UI violette `#6d28d9` (sélection + « Tirer en B7 », roving tabindex, zoom, onglets mobile, debounce `SET_FLEET` 600 ms ≥ 500 ms, aucune logique serveur côté client), migration additive `20260911190958_bataille_navale_activate.sql` équivalente à la référence (`ready` + `bataille-navale-1`). Divergences concurrentes relevées sans reprise : `dedupeKey` sans `matchId` (convention socle `${matchId}:${phaseId}:${kind}` conservée), `jobsToCancel` inconnu des autres moteurs du socle, codes d'erreur non mappés dans `http.ts` (`FLEET_LOCKED`/`ALREADY_SHOT`/… → 500, contre `ALREADY_SUBMITTED`/`SHIPS_OVERLAP`/… mappés ici), absence stricte `>` (convention socle `>=` comme Skyjo conservée), tour initial 0 (contrat 08 : tour courant initial 1).
- Défauts réels corrigés (arbre de travail uniquement) : (1) métrique nommée `accuracy` au lieu de `precision` exigée par la fiche §5 et le contrat de la tâche — renommée dans `metricsFor` (`shots/hits/misses/sunkShips/automaticShots/precision`, nulle si 0) ; (2) `navalStateSchema` non strict (seul schéma naval sans `.strict()`) — `.strict()` ajouté à l'état et à `lastShot` ; (3) double contrôle de phase mort dans `FIRE` (`setup && playing` puis `playing`) — réduit au seul contrôle utile. Tests de régression ajoutés (10) : `precision` exacte (1/1 et 0/1) + `sunkShips:1` après coulé du destroyer, `SET_FLEET` à 6 bateaux rejeté par le schéma, `completeFleet([])` valide 17 cases, RESIGN en préparation → `abandoned` sans vainqueur, timeout de préparation avec chrono → `turn_timeout` armé (1 job), FIRE exactement à l'échéance refusé sans mutation, garde `phaseId` avec `undefined`, vue setup (brouillon visible, flotte adverse nulle, `activePlayerId` nul, `SET/RANDOMIZE/READY` offerts sans `FIRE`), `UNREADY_FLEET` offert après mon seul READY, projection refusée à un tiers, état rejeté avec clé inconnue.
- Vérifications dans l'ordre demandé : `pnpm test` OK (28 fichiers / 270 tests, dont 44 Bataille navale), `pnpm exec next build --webpack` OK (route `/jeux/bataille-navale` présente), `next-env.d.ts` inchangé par le build (restauration sans effet), `pnpm typecheck` OK, `pnpm lint` global en échec sur la seule copie de référence `comparison-codex/` (1 erreur `set-state-in-effect` + 7 avertissements, fichiers non modifiables par consigne ; `eslint` ciblé sur `src/games/bataille-navale`, worker, routes, composants et page : 0 problème), `pnpm docs:check` OK (22 fichiers), `git diff --check` OK, `git status` sans modification hors périmètre (fiches, jeux validés, migrations existantes et lexique BombParty intacts).
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration en base, recette à deux sessions avec `commitMatch`/baux/échéances réels (dont refus tardif et replay au commit), E2E Bataille navale. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers Bataille navale restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Cycle 2 et dernier contrôle Bataille navale (clone, sans commit)

- Demande : revue indépendante de release avant intégration, via CLI uniquement, sans accès hors répertoire, sans application OpenCode, sans Docker, sans commit ni push. Relire AGENTS.md, progression.md, fiche `07-bataille-navale.md` et docs 01/02/03/04/08 ; vérifier réellement engine/types/config/projection/tests, routes rooms/start/commands/get, worker, registry/home/match-game, UI setup/match, migration ; chercher surtout validation stricte (flottants/out-of-range/IDs/longueurs catalogue), contacts sans overlap, completeFleet/RANDOMIZE (backtracking fini, fixés conservés, repli flotte entière), setup 180 s avec ready préservés, READY/UNREADY et double READY, activeSeat/tour courant, FIRE avant/après deadline DB et stale phaseId, tir répété sans mutation, hit/couler sans rejouer, sunk complet seul, victoire au 17e sans dernier tour, auto-shot inconnu sans flotte cachée, metrics/précision nulle si zéro, RESIGN/CLAIM/absence, projection no-leak, result/round record, commitMatch/replay, route/worker/UI. Ajouter uniquement les tests nécessaires, corriger les défauts réels sans changer la fiche ni les jeux validés. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée. Lexique Bombparty et fiches non touchés.
- Revue (code réellement relu : `engine.ts`, `types.ts`, `config.ts`, `projection.ts`, 2 suites, routes rooms/start/commands/get, `http.ts`, worker `processNavalJob`/`processNavalAbsenceJob`, registre, match-game, home selector, UI setup/match/page, migration `20260911190958_bataille_navale_activate.sql`) : deux joueurs, 10×10, catalogue serveur 5/4/3/3/2 = 17 cases, longueurs déduites du catalogue côté `validateFleetEntries`, Zod stricts partout (flottants et hors-limites rejetés, `length` intrus rejeté, config `{turnSeconds:null|60}` défaut null), contacts côtés/diagonales autorisés sans overlap ni dépassement, setup 180 s avec `SET_FLEET` partiel + `RANDOMIZE` backtracking fini (fixés conservés, repli flotte entière si complément impossible), `READY` complet/unique, `UNREADY` avant ready adverse, second `READY` démarre une seule fois avec premier tireur par entropie serveur, `FIRE` un tir/tour avec `DEADLINE_EXPIRED` moteur à `dbNow >= deadlineAt` avant mutation (RESIGN/CLAIM restant possibles), répétition `ALREADY_SUBMITTED` sans mutation, hit/couler sans rejouer, sunk seulement toutes cases touchées, victoire immédiate au 17e sans dernier tour, tir auto serveur uniforme sur cases inconnues sans lecture de la flotte cachée, deadline nulle sans timer, garde `isNavalDeadlineJobStale` moteur + worker, absence 120 s à deux/180 s seul vers `abandoned`, projection sans `shipId` sur hit et sans flotte adverse avant finished (révélation en fin aux participants), score = cases touchées 0..17 avec `precision` nulle (null) si zéro tir, round record unique avec flottes + journal, `commitMatch` avec `previousPhaseId`/reçu idempotent, erreurs navales mappées en français côté `http.ts`, UI violette `#6d28d9` (tirs gauche/flotte droite desktop, onglets mobile, zoom, roving tabindex, « Tirer en B7 », debounce `SET_FLEET` 600 ms, aucune logique serveur côté client), migration additive cohérente avec le registre (`ready`, `bataille-navale-1`).
- Défaut réel corrigé (arbre de travail uniquement) : le brouillon local du panneau de préparation n'adoptait pas le tirage serveur après `RANDOMIZE_FLEET` — l'affichage restait sur l'ancien brouillon et le prochain `SET_FLEET` débouncé risquait d'écraser le tirage ; `send` retourne désormais la vue rafraîchie et le gestionnaire Aléatoire adopte `view.myFleet` renvoyée (les dépôts `SET_FLEET` ne sont pas auto-écrasés pour ne pas perdre une frappe en cours). Aucun autre défaut réel trouvé : `INVALID_ENTROPY` (panne serveur d'entropie uniquement) reste sur le 500 générique du socle, volontairement conservé sans toucher `http.ts` partagé.
- Tests de régression ajoutés (9) : config stricte (défaut null, 30/chaîne/clés inconnues/ID inconnu refusés), setup 180 s pile + 1 job, tir répété sans mutation (comparaison JSON), `CLAIM_FORFEIT` en préparation sans vainqueur et après échéance, tir auto indépendant de la flotte cachée (mêmes entropie/coups, flottes différentes → même case inconnue), comptage `automaticShots` + précision exacte, complément d'une flotte partielle touchante, round record (flottes + 17 tirs journalisés).
- Vérifications dans l'ordre demandé : `pnpm test` OK (28 fichiers / 279 tests, dont 53 Bataille navale), `pnpm exec next build --webpack` OK (route `/jeux/bataille-navale` présente), `next-env.d.ts` régénéré puis restauré via `git checkout`, `pnpm typecheck` OK, `pnpm lint` OK (0 problème), `pnpm docs:check` OK (22 fichiers), `git diff --check` OK, `git status` limité au périmètre naval (fiches, jeux validés, migrations existantes et lexique intacts).
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration en base, recette à deux sessions avec `commitMatch`/baux/échéances réels (dont refus tardif et replay au commit), E2E Bataille navale. Supabase distant non vérifié ; aucune migration déclarée appliquée en production ; les fichiers Bataille navale restent non committés dans ce clone.
- Statut : audit + corrections présents dans l'arbre de travail uniquement. Aucun commit, push, déploiement ou migration distante.

### 11/09/2026 — Finalisation Bataille navale, contrat coulé shipId/type/cellules (clone, avec commit sans push)

- Demande : finaliser les deux cycles déjà achevés sans repartir de zéro, auditer surtout le contrat « un coulé inclut shipId, type et cellules », ajouter type/cellules côté serveur s'ils manquent, les garder absents des tirs non coulés visibles par l'adversaire, mettre à jour projections et tests ciblés, préserver l'architecture, lancer tests ciblés/typecheck/lint, puis committer avec le message exact `feat: implement Bataille navale`, sans push. Aucune contradiction avec AGENTS.md ; journal sans contradiction inventée.
- Audit : le coulé n'exposait que `shipId` (`engine.resolveShot` + `projection.shotView` + schémas `navalShotSchema`/`NavalShotView`), sans `shipType` ni cellules du bateau, contrairement à la fiche `07-bataille-navale.md` §3.
- Corrections (même changement) : `types.ts` ajoute `shipType` (enum naval) et `sunkCells` (0..5 cases strictes) optionnels au schéma strict et à `NavalShotView` ; `engine.ts` calcule `shipType`/`sunkCells` depuis la flotte serveur dans `resolveShot` (miss/hit sans ces champs) et les inclut dans le payload d'événement uniquement sur coulé ; `projection.ts` expose `shipId`/`shipType`/`sunkCells` sur coulé (avec repli `shipType ?? shipId` pour compatibilité) et les omet sur miss/hit. Tests ciblés : moteur (coulé avec `shipId`/`shipType`/`sunkCells` exacts du destroyer, absence sur hit, payload sans fuite sur miss) et projection (absence `shipId`/`shipType`/`sunkCells` sur hit des deux côtés, coulé identique attaquant/défenseur sans flotte révélée).
- Vérifications : `pnpm vitest run src/games/bataille-navale` OK (2 fichiers / 54 tests), `pnpm typecheck` OK, `pnpm lint` OK (0 problème), `pnpm test` OK (28 fichiers / 280 tests, dont 54 Bataille navale).
- Non vérifiable sans Supabase distant (ni Docker ici) : exécution réelle de la migration en base, recette à deux sessions, E2E. Supabase distant non vérifié ; aucune migration déclarée appliquée en production.
- Statut : committé dans ce clone avec le message exact demandé, sans push ni déploiement.

### 11/09/2026 — Préparation du pack Compatibilité

- Réalisation : création de `scripts/build-compatibilite-pack.mjs`, source originale reproductible de 160 questions à choix (40 par catégorie : `quotidien`, `absurde`, `amitie`, `couple`), avec 2 à 4 options, IDs stables, sensibilité `light`/`personal`, manifest et pack JSON générés dans `content/compatibilite/`.
- Migration préparée : `supabase/migrations/20260911200000_compatibilite_ready.sql` contient l'insertion idempotente du pack, la RPC serveur `server_get_compatibilite_content()` avec droits `service_role` uniquement et l'activation finale du jeu ; elle ne sera considérée appliquée qu'après vérification distante.
- Vérifications : génération du pack OK (160 questions, 4 catégories), limites structurelles contrôlées par le script, `git diff --check` OK. Le jeu reste `coming_soon` tant que son moteur, ses projections, son UI et ses tests ne sont pas intégrés.
- Aucune contradiction avec `AGENTS.md` ; aucune donnée sensible ou question de notation psychologique n'a été ajoutée.

### 11/09/2026 — Première implémentation Compatibilité et incident Muse

- Réalisation en cours : moteur pur et projection sans fuite ajoutés avec choix simultanés, trois passages, révélation, score coopératif, résultat détaillé, contenu local/RPC, routes, worker et UI violette sobre ; validation ciblée initiale à 12 tests.
- Problème corrigé : le premier test de fin attendait 55 % alors que son scénario comportait 5 accords sur 10 ; assertion corrigée à 50 %, puis suites ciblées et typecheck validés.
- Problème externe confirmé à trois reprises : le CLI OpenCode avec `opencode/muse-spark-1.3-contributor-free` a lu le contexte puis s'est arrêté sur `Rate limit exceeded`, sans produire de code dans le clone. N'étendre aucune conclusion de comparaison à partir de cette tentative ; nouvelle tentative à faire avant les cycles de revue.
- Problème E2E corrigé : le premier lancement sans privilège ne pouvait pas binder le port 3000 (`listen EPERM`), puis l'exécution autorisée a révélé une assertion `toHaveText` trop stricte sur la carte Longueur d'onde ; remplacement par `toContainText`, sans défaut produit confirmé à ce stade.

### 11/09/2026 — Préparation du pack Longueur d'onde

- Réalisation : création de `scripts/build-longueur-onde-pack.mjs`, source originale reproductible de 80 axes opposés (30 `quotidien`, 25 `culture`, 25 `absurde`), labels limités à 60 caractères, trois exemples de tutoriel et IDs stables ; génération de `content/longueur-onde/longueur-onde.json`, `manifest.json` et de la migration additive `20260911210000_longueur_onde_ready.sql`.
- Problème corrigé : la première version du générateur déduisait mal les catégories lorsque les exemples facultatifs étaient absents (`78/1/1` au lieu de `30/25/25`) ; la catégorie est maintenant déterminée par blocs d'axes et le générateur refuse toute couverture inattendue.
- Vérification : `pnpm longueur-onde:pack` produit 80 axes avec la couverture attendue. Aucune implémentation de jeu n'est encore déclarée dans ce jalon.

### 11/09/2026 — Implémentation À l'unisson et incident Muse

- Réalisation : moteur pur et schémas stricts, projection privée, alternance indice/estimation, cible aléatoire serveur, révélation temporisée, score coopératif, abandon/absence, API de salon et de partie, worker durable des échéances, UI responsive violette sobre, page dédiée et activation `ready` ajoutés. Le cadran est rendu en SVG et ne nécessite pas d'asset bitmap.
- Vérifications : `pnpm test` valide 37 fichiers et 308 tests ; `pnpm typecheck`, `pnpm lint`, `pnpm exec next build --webpack`, `pnpm docs:check` et `git diff --check` réussissent. La recette Playwright Chromium de l'accueil passe ; les sept parcours authentifiés restent ignorés faute de `E2E_PASSWORD`.
- Processus Muse : trois exécutions CLI avec le modèle exact `opencode/muse-spark-1.3-contributor-free` ont échoué côté fournisseur avec `Rate limit exceeded. Please try again later`, sans écrire de fichier dans les clones isolés. Les deux cycles de comparaison ne sont donc pas déclarés réalisés pour ce jeu ; aucun code Muse n'a été intégré ni présenté comme équivalent.
- Limites : la CLI Supabase locale répond avec `SUPABASE_TELEMETRY_DISABLED=1`, mais les opérations distantes restent bloquées par l'absence de `SUPABASE_ACCESS_TOKEN`. La migration et la recette réelle à deux sessions restent à effectuer.

### 11/09/2026 — Vérification navigateur CLI de Longueur d'onde

- Problème : le binaire `agent-browser` recommandé par la procédure de vérification n'est pas installé (`command not found`).
- Résolution : utilisation du runner Playwright déjà versionné dans le dépôt ; le test Chromium de l'accueil passe et le lancement complet Chromium compte 1 succès et 7 tests authentifiés ignorés faute de mot de passe. Un probe initial du formulaire d'inscription ciblait deux rôles `alert` (dont l'annonceur Next vide) ; le sélecteur a été resserré et le formulaire affiche bien le garde Supabase attendu quand les variables navigateur sont absentes.
- Vérification distante en lecture seule : `https://thibfun.vercel.app` répond 200, l'accueil expose les neuf `data-game` et le compteur `9 disponibles`, et les neuf routes de jeu répondent 200 après le push `ca5d521`.

### 12/09/2026 — Inscription libre et espace compte

- Demande : supprimer l’obligation d’invitation ; permettre l’inscription e-mail/mot de passe, puis afficher clairement le compte une fois connecté. Cette demande contredit l’ancienne décision documentaire « comptes sur invitation » ; la décision, l’ancien comportement, le nouveau comportement, le périmètre et la raison sont consignés dans `AGENTS.md`, `docs/README.md` et les contrats concernés.
- Cause confirmée : `auth.signUp` existait déjà côté client, mais aucune ligne `public.profiles` ni `private.site_members` n’était créée. `server_get_actor` exige pourtant les deux lignes et les routes métier refusaient donc les nouveaux comptes comme non admis. La production affichait encore « compte invité » et « membres admis ».
- Réalisation dans l’arbre de travail : migration CLI `20260911231740_self_service_signup.sql` avec provisionnement serveur idempotent, trigger après création `auth.users`, rattrapage des comptes Auth existants sans profil, pseudo initial dérivé de l’e-mail sans stockage de l’e-mail dans `profiles`, conservation des membres désactivés et RPC `service_role` uniquement ; route `/api/auth/provision` de rattrapage ; callback `/auth/callback` sécurisé sur une redirection relative ; inscription avec `emailRedirectTo` ; redirection vers `/profil` ; écran `/profil`, navigation « Mon compte » et déconnexion. Les invitations admin restent documentées comme mécanisme historique optionnel et ne contrôlent plus l’admission.
- Configuration locale préparée : `supabase/config.toml` conserve `enable_signup = true`, ajoute les callback URLs locales et `https://thibfun.vercel.app/auth/callback`. Lecture publique de `https://ttogfwnlknmiscnmlhof.supabase.co/auth/v1/settings` vérifiée : inscription non désactivée (`disable_signup=false`), e-mail activé et confirmation d’e-mail actuellement requise (`mailer_autoconfirm=false`). La allow-list exacte, le SMTP et le schéma distant n’ont pas pu être inspectés : le dashboard Supabase ouvert dans Safari n’a pas exposé de projet et la CLI n’a pas de `SUPABASE_ACCESS_TOKEN`.
- Vérifications réussies : `pnpm test` (37 fichiers / 308 tests), `pnpm typecheck`, `pnpm lint`, `pnpm exec next build --webpack`, `pnpm docs:check` (22 fichiers) et `git diff --check`.
- Difficultés rencontrées : `pnpm build` avec Turbopack a échoué sur `Operation not permitted` lors de la création d’un processus/port dans l’environnement isolé ; le build Webpack équivalent a réussi. `SUPABASE_TELEMETRY_DISABLED=1 supabase status` a échoué car Docker n’est pas démarré (`Cannot connect to the Docker daemon`), donc la migration et les tests pgTAP n’ont pas été exécutés sur une base locale. Aucune migration distante, commit, push ou déploiement n’est déclaré.
- Prochaine étape utile : démarrer Docker ou utiliser une base Supabase isolée, appliquer/valider la migration, vérifier les droits `service_role`/RLS et tester deux inscriptions réelles (confirmation activée puis désactivée), une connexion existante, un compte désactivé et l'affichage `/profil` avec deux sessions séparées. Sur le projet distant, vérifier manuellement les réglages Auth et ajouter exactement les callback URLs de chaque environnement.

### 12/09/2026 — Vérification Supabase distante et correction additive

- Réalisation : après connexion du Personal Access Token, la CLI Supabase 2.104.0 a listé le projet lié `ttogfwnlknmiscnmlhof` et `supabase migration list --linked` a confirmé la correspondance des 16 migrations locales avec l'historique distant.
- Vérifications SQL en lecture seule : les neuf lignes de `public.games` sont `ready`, les packs distants Trou Noir/TTMC/Compatibilité/Longueur d'onde contiennent respectivement 120/440/160/80 éléments, les fonctions serveur restent réservées à `service_role`, les tables publiques ont RLS activée et le trigger d'inscription est présent.
- Défaut réel corrigé : les quatre triggers coopératifs Longueur d'onde manquaient à distance car la migration initiale avait été appliquée avant l'ajout local de ces triggers. La migration immuable n'a pas été réécrite ; `supabase migration new longueur_onde_cooperative_result_triggers` a créé `20260911233839_longueur_onde_cooperative_result_triggers.sql`, son dry-run a ciblé uniquement ce fichier, puis `supabase db push --linked` l'a appliqué.
- Vérification après correction : l'historique distant contient maintenant 17 migrations et les quatre triggers Longueur d'onde (`player_results`, `history_entries`, `player_game_stats`, `match_results`) sont actifs.
- Advisors : il reste deux avertissements : `is_site_member()` est un `SECURITY DEFINER` volontairement exécutable par `authenticated` pour servir les politiques RLS, et la protection Auth des mots de passe compromis est désactivée. Ce dernier réglage doit être activé dans la configuration Auth du projet.
- Limite outillage : `supabase db diff --linked` tente de créer une base shadow et échoue sans Docker ; aucune nouvelle tentative n'a été faite, conformément à la consigne de ne pas utiliser Docker.

### 13/09/2026 — Diagnostic complet après échec des boutons abandon/forfait

- Demande et périmètre : diagnostiquer le code des neuf jeux et produire un plan de correction, sans implémentation, commit ni déploiement. Aucune contradiction avec AGENTS.md ; aucune nouvelle décision produit.
- Problèmes confirmés : garde SQL interdisant aussi les sorties après échéance ; forfait Géographie attribuant la victoire à l'adversaire ; Cron actif mais Vault vide ; JSON dispatcher refusé par le schéma worker ; jugement quiz rattaché à l'ancienne phase et refusé sans deadline ; annulation globale des jobs lors d'actions partielles ; six jeux sans heartbeat ; triggers coopératifs SQL ambigus. Les autres défauts, niveaux de preuve, fichiers et corrections proposées figurent dans les 29 défauts et 6 observations d'infrastructure de l'[audit daté](docs/audit-code-2026-09-13.md).
- État distant observé en lecture seule : 21 migrations appliquées ; trois matchs TTMC actifs bloqués, six jobs échus depuis le 12 septembre avec `attempts=0`, un jugement de phase différente du match ; engagements simultanés multiples confirmés. Aucun secret lu, aucun RPC de mutation ni abandon distant exécuté. L'ambiguïté SQL `game_slug` a été reproduite par `EXPLAIN` sans `ANALYZE`, dans une transaction en lecture seule ; aucune partie coopérative distante n'a été finalisée pour le test.
- Étape 0 du plan, lecture seule : le projet `ttogfwnlknmiscnmlhof` est `ACTIVE_HEALTHY` en `eu-west-1`, PostgreSQL `17.6`; Cron `tibo-fun-dispatch-due-jobs` est actif chaque seconde et ses dernières exécutions répondent `succeeded/1 row`, mais `vault.secrets` ne contient aucun nom. Le schéma privé refuse `USAGE` et `SELECT` à `anon`/`authenticated`; l'advisor signale malgré tout 18 tables sans RLS, ainsi que `is_site_member()` SECURITY DEFINER, `pg_net` dans `public`, politiques anonymes attendues, protection des mots de passe compromis désactivée, 13 clés étrangères non indexées et 6 index inutilisés. L'alerte RLS est conservée comme décision à prendre ; son SQL automatique n'a pas été exécuté.
- Étape 0 côté déploiement, relevée après ouverture de l'accès : le projet Vercel `thib.fun` est identifié dans l'équipe `team_UrGuXgGQZHe1NQuwU3IFwoZz`, avec l'identifiant `prj_SIq42aMngLTaKKMWbNGwmlU1RQKf`. Son dernier déploiement de production `dpl_35YN2mtRFLrgAW7tXWqSs3CcNBc7` est `READY` et correspond exactement au commit `58eaca94ec35e58ed28fcedab59a0d8b405c8848` de `main`; les domaines `thibfun.vercel.app`, `thibfun-thibaud73000s-projects.vercel.app` et `thibfun-git-main-thibaud73000s-projects.vercel.app` sont associés. Les logs de build ne montrent qu'un avertissement sur l'évolution automatique de Node et aucun runtime error n'a été remonté. `.vercel/project.json` et `vercel.json` sont absents ; le connecteur ne permet pas de lister les noms de variables ni le réglage d'origine autorisée.
- Vérifications locales : `pnpm test` (42 fichiers/318 tests), `pnpm typecheck`, `pnpm lint`, `pnpm exec next build --webpack` réussis. Huit probes Vitest temporaires réussies (sept ruptures reproduites + fixture UNO) ; six probes Playwright réussies, trois défauts sur Chromium et WebKit mobile : dialogue joker fermé au polling, abandon restant occupé après panne réseau, absence de heartbeat. Les tests navigateur utilisent des API simulées, pas deux comptes distants. Quatre tests E2E existants réussis, quatorze ignorés sans `E2E_PASSWORD`. Cinq commandes de validation de contenu réussies en copie isolée, SQL généré identique ; validation Géographie en chemin avec espaces en échec `ENOENT` reproduit (D29).
- Incidents d'outillage pendant l'audit : premier serveur local refusé par le sandbox (`EPERM`), puis démarré sur loopback après autorisation et arrêté après recette ; connexion navigateur initialement refusée et référence de tab absente, corrigées après démarrage. Quatre premières probes navigateur échouaient à cause du sélecteur de joker et de la confirmation automatiquement refusée par Playwright ; harnais corrigé, six probes finales réussies. Deux requêtes d'introspection SQL ont échoué sur un mauvais schéma puis une colonne non qualifiée ; lectures corrigées. Ces incidents ne sont pas comptés comme défauts applicatifs. `next-env.d.ts` régénéré par le build puis restauré à son état initial.
- Limites : les noms de variables et le réglage d'origine autorisée Vercel restent non consultables via le connecteur ; suite pgTAP complète non exécutée sur base isolée ; aucune partie complète des neuf jeux avec deux vrais comptes dans cette session. Les probes temporaires ont été retirées du dépôt après reproduction ; elles ne constituent pas une suite de régression livrée.
- Contrôle documentaire final : `pnpm docs:check` réussi (24 fichiers), `git diff --check` réussi ; changements limités au rapport, à son index, au plan pas à pas et aux deux suivis.
- Plan demandé le 13 septembre : création du [plan de correction pas à pas](docs/plan-correction-2026-09-13.md), avec étapes 0 à 10, dépendances, tests et critères de sortie. Aucun code, migration, secret, traitement de données ou déploiement n'a été engagé ; exécution suspendue en attente de relecture utilisateur.
- Étape 0 terminée le 13 septembre après ouverture de l'accès Vercel : projet et déploiement de production identifiés, commit et domaines vérifiés, build/runtime inspectés sans erreur. La liste des noms de variables et l'origine autorisée restent à obtenir via un canal Vercel qui les expose ; aucune demande supplémentaire n'est nécessaire pour commencer l'étape 1.
- Résolution : aucune correction appliquée. Plan proposé : recette reproductible → transactions/jobs/résultats → présence/réseau/UI → livraison et configuration contrôlées → robustesse/données → parcours manquants et recette complète. Ne pas se limiter à remplir Vault : cela réveillerait aussi les absences fictives et les anciennes tâches.

### 13/09/2026 — Exécution de l'étape 1 du plan de correction

- Demande et périmètre : après relecture du plan, renforcer la suite de tests et la CI sans corriger encore les défauts métier, appliquer de migration, configurer de secret, traiter les parties distantes ou déployer. Aucune contradiction avec `AGENTS.md` ; la demande est conforme au périmètre de l'étape 1.
- Réalisation : helper E2E commun avec redirection `/profil` et garde CI explicite ; assertions pgTAP filtrées par pack publié et manifeste ; fixture locale Alice/Bob actifs + tiers désactivé ; horloge et adaptateur DeepSeek simulés ; contrats versionnés pour dispatcher, échéance, reçus, présence et triggers ; trois régressions UNO Playwright ; matrice `pass`/`fail`/`blocked`/`not-run` et contrôle `pnpm test:matrix` ; workflow CI statique et isolé, avec Supabase local uniquement.
- Vérifications : `pnpm test` réussit avec 43 fichiers, 330 tests passés et 2 sentinelles `it.fails` attendues ; `pnpm typecheck`, `pnpm lint`, `pnpm test:matrix`, `pnpm docs:check` et `git diff --check` réussissent. Playwright hors CI : 10 résultats sont acceptés (4 scénarios ordinaires et 6 échecs attendus des 3 régressions UNO sur Chromium et mobile) et 16 sont ignorés : 14 scénarios sans `E2E_PASSWORD` et 2 sessions anonymes réservées à la CI Supabase locale. `pnpm test:db` reste bloqué sans Docker local ; aucune base distante n'a été utilisée.
- Difficultés réelles : le premier lancement Playwright avec `pnpm dev -- --hostname` a échoué car Next a interprété `--hostname` comme un répertoire ; la configuration utilise maintenant `pnpm exec next dev --hostname 127.0.0.1`. Le sandbox refusait aussi l'écoute réseau (`EPERM`) ; la recette a été relancée sur loopback avec l'autorisation d'exécution prévue. Le premier `pnpm test:db` a tenté d'écrire `~/.supabase/telemetry.json` et a échoué (`EPERM`) ; la télémétrie est désormais désactivée dans le script, le fixture et la CI. Une relance autorisée a confirmé le blocage restant : le socket Docker Desktop n'existe pas et PostgreSQL local refuse la connexion. Ces incidents ont été corrigés ou classés et ne sont pas des défauts métier.
- Limites : les défauts dispatcher/délai restent volontairement signalés par deux sentinelles attendues et les trois régressions UNO restent `fail` dans la matrice ; la panne réseau UNO produit encore un `unhandledRejection` visible dans le serveur de développement. Les correctifs appartiennent aux étapes 2, 5 et 6. Le secret E2E, Docker et une base Supabase locale sont nécessaires pour débloquer la recette à deux comptes, sans inscrire de credentials dans Git.
- Statut : étape 1 terminée côté preuve et automatisation ; aucun RPC Supabase distant, migration de production, secret Vercel/Supabase, donnée de reprise ou déploiement de production n'a été effectué dans cette étape.

### 13/09/2026 — Exécution de l'étape 2 du plan de correction

- Demande et périmètre : formaliser uniquement le contrat transactionnel commun sur la branche dédiée `codex/step2-contract`, depuis `dfa0acf`, sans corriger les neuf jeux, le dispatcher, le worker, une migration de production, la configuration Supabase/Vercel ou les parties existantes. Aucune contradiction avec `AGENTS.md` ; la branche dédiée demandée est conforme à ce périmètre.
- Réalisation : `src/server/matches/transaction-contract.ts` expose les règles pures d'échéance, la clé `[matchId, phaseId, stateVersion]`, l'identité de commit, la vérification du siège authentifié, le replay de reçu, le bail de job, les clés de déduplication, la réconciliation explicite des jobs et les issues techniques/cooperatives sans gagnant. `docs/08-engine-contracts.md` et `docs/plan-correction-2026-09-13.md` décrivent le contrat arrêté.
- Choix de contrat : un coup ordinaire est refusé à `dbNow >= deadline`; `RESIGN` reste possible après expiration ; `CLAIM_FORFEIT` exige 90 secondes d'absence adverse ; `check_absence` est indépendant de phase mais revalide 120/180 secondes ; le jugement n'a pas de deadline joueur ; préparation et timeout de phase exigent job, phase et échéance cohérents ; l'interruption coopérative abandonne sans gagnant. Les reçus comparent acteur/type/hash, excluent `expectedVersion`, et un rejeu renvoie exactement la réponse committée avant les contrôles obsolètes.
- Jobs et erreurs : seuls les IDs de `jobsToCancel` sont annulés ; les jobs valides sont conservés ; une clé de phase remplacée est nouvelle ; un job terminal n'est jamais réactivé. Un bail invalide est rejeté, une donnée obsolète est annulée/rejetée, les erreurs réessayables utilisent `1/2/4/8 s`, une cinquième erreur finalise `technical_error` sans victoire et une panne de base conserve le job avec alerte.
- Tests ajoutés : `src/server/matches/transaction-contract.test.ts` couvre les classes de commandes, frontières d'échéance, exits, snapshot, identité, receipts, baux, jobs, erreurs et une concurrence simulée sérialisée ; `src/server/matches/transaction-contract-gaps.test.ts` garde cinq écarts du SQL actuel en `it.fails`. La suite complète passe avec 347 tests réussis et 7 échecs attendus (les 2 sentinelles de l'étape 1 et les 5 SQL de l'étape 2).
- Vérifications finales : `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm docs:check` et `git diff --check` réussissent ; `pnpm test` valide 45 fichiers, 347 tests passés et 7 échecs attendus. `docs:check` contrôle 25 fichiers Markdown.
- Difficultés réelles : la création initiale du worktree a été refusée par le sandbox sur le verrou de référence Git (`unable to create directory for .git/refs/heads/codex/step2-contract`) puis a réussi après autorisation explicite, sans toucher au code du checkout partagé. Les deux premiers `pnpm vitest` dans le worktree ont tenté d'installer les dépendances et ont échoué sur `npm ERR! code ENOTFOUND`. Une installation locale `CI=true pnpm install --offline --frozen-lockfile` a ensuite échoué sur `ERR_PNPM_NO_OFFLINE_TARBALL` pour `@supabase/supabase-js`; les commandes finales ont utilisé temporairement l'arbre de dépendances déjà présent, via un lien local non versionné qui sera supprimé avant le commit. Aucun accès ou changement distant n'a été effectué.
- Échecs restants : le RPC `server_commit_match` lit encore le reçu avant le verrou, ne revalide pas acteur/type/hash, annule globalement les jobs et applique une règle d'échéance uniforme ; `server_fail_job` ne finalise pas encore `technical_error` après cinq échecs ; `server_set_room_ready` n'applique pas encore le replay vérifié sous verrou. Ces cinq écarts sont volontairement réservés à l'étape 3.
- Limitations : les helpers sont purs et ne sont pas encore branchés sur `repository.ts`, la route de commandes, PostgreSQL, le dispatcher ou le worker ; la concurrence testée est un harnais de sérialisation et non une recette sur base isolée avec deux sessions ; aucune décision non couverte par les documents n'a été inventée. Les tests E2E, pgTAP, Supabase/Vercel et les données existantes restent hors périmètre.

### 13/09/2026 — Recherche shell avec motif contenant des backticks

- Problème : une commande de recherche documentaire a échoué avec `zsh: command not found: sql`.
- Cause confirmée : les backticks du motif de recherche ont été interprétés par `zsh` au lieu d'être traités comme du texte ; aucun fichier ni comportement applicatif n'a été affecté.
- Résolution : les recherches suivantes utilisent des motifs sans substitution shell implicite ou des guillemets adaptés ; les contrôles documentaires restent réussis.
- Contradiction : aucune avec `AGENTS.md` ; incident d'outillage uniquement.

### 14/09/2026 — Exécution de l'étape 4 : dispatcher et jugements quiz

- Demande et périmètre : rétablir uniquement le dispatcher et les jugements Trou Noir/TTMC décrits dans l'étape 4 de `docs/plan-correction-2026-09-13.md`. Aucune contradiction avec `AGENTS.md` n'a été détectée ; aucune étape suivante, migration de production, configuration Vault, donnée existante ou déploiement n'a été engagé.
- Précondition vérifiée : l'étape 3 est terminée et validée localement. `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` passe avec 3 fichiers et 105 tests, `supabase db lint --local` ne remonte aucune erreur de schéma, et le fixture isolé à deux sessions a démontré le verrouillage/rejeu idempotent d'un même `RESIGN` avec une seule partie, un reçu, un résultat, deux historiques, deux statistiques et un agrégat de duo. L'inspection Supabase distante est restée en lecture seule ; elle rapporte 22 migrations et les hashes des fonctions Étape 3 concordent avec le local.
- Dispatcher et worker : `workerJobSchema` et `workerRequestSchema` restent stricts ; `claim_due_jobs`/`dispatch_due_jobs` ne transmettent que `jobId` et `leaseToken`. La route interne vérifie la configuration serveur puis le secret avant parsing, borne le corps à 16 KiB, traite les lots de 1 à 4 éléments et conserve les résultats individuels. `processWorkerJob` vérifie le bail via `getJobContext`, recharge le payload/état privé, puis relit ce contexte après un appel de correction lente avant de committer ; un bail invalide ou un job obsolète n'écrit pas de transition métier.
- Jugements et phases : `src/games/trou-noir/engine.ts` et `src/games/ttmc/engine.ts` produisent un `judge_answer` avec `attemptId`, `phaseId` et `expectedPhaseId` égaux à la phase judging attendue, ainsi qu'une clé de déduplication stable. Les événements et la table privée `quiz_attempts` rattachent la réponse au match, au joueur, à la tentative et à sa phase. Un timeout est persisté sur la phase de révélation créée par la transition ; un jugement ne requiert pas `deadline_at`, conformément au contrat de la phase judging. Les verdicts ambigus ou indisponibles déclenchent un remplacement sans pénalité, puis `judging_unavailable` sans vainqueur après le plafond prévu ; acceptation/rejet suivent la révélation.
- Budget, appels et cache : les migrations CLI `20260913220544_step4_dispatch_quiz_judgments.sql`, `20260913223824_step4_quiz_reservation_phase_guards.sql` et `20260913225319_step4_release_ai_reservations.sql` ajoutent les tentatives privées, le budget persistant par job/match, les compteurs quotidiens, les appels IA, le cache versionné et le job de libération des réservations. Les réservations verrouillent match → job → tentative → budget/compteur, limitent atomiquement à deux appels par tentative et revalident phase/état avant réservation ; la réconciliation est idempotente, une réservation expirée devient `unknown` avec son coût maximal, les coûts/tokens restent privés et seuls les verdicts accept/reject complets sont cachés 30 jours. La clé inclut révision/question, pack, clé logique, réponse normalisée, modèle, prompt et politique.
- Configuration et sécurité : `getQuizAiConfiguration()`/`requireQuizAiConfiguration()` contrôlent la clé, le modèle et les plafonds avant démarrage d'un quiz ; la route de lancement refuse Trou Noir/TTMC si cette configuration manque. DeepSeek reste serveur uniquement. Le tarif n'est pas inventé : sans coût de réservation configuré, la consommation conserve `price_revision = unavailable` et `AI_RESERVED_CALL_COST_USD` vaut 0 par défaut. Aucun secret réel n'a été ajouté à `.env.example`.
- Tests de comportement : les adaptateurs DeepSeek sont simulés pour l'exactitude, le faux verdict, l'alias normalisé, la réponse sémantique, les erreurs de transport et la correction lente/timeout ; les moteurs couvrent remplacement, timeout, révélation, rattachement phase/tentative et absence de pénalité sur ambiguïté. La route couvre secret valide/invalide, configuration absente, lot vide/malformé/trop riche, lot partiel, répétition d'un bail et 413 avant `JSON.parse`.
- Vérifications réellement réussies : ciblage Étape 4 — 7 fichiers, 84 tests ; suite Vitest complète — 48 fichiers, 381 tests réussis et 2 `expected fail` historiques ; `node_modules/.bin/tsc --noEmit` ; `node_modules/.bin/eslint .` ; `node scripts/check-docs.mjs` (25 fichiers) ; `node scripts/check-test-matrix.mjs` ; `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` — 4 fichiers, 145 tests ; `SUPABASE_TELEMETRY_DISABLED=1 supabase db lint --local` — aucune erreur de schéma, avec seulement des avertissements PL/pgSQL non bloquants (`v_usage`/`v_attempt` non lus dans les nouveaux RPC et avertissements historiques). Les trois migrations Étape 4 ont été appliquées uniquement à PostgreSQL local isolé. Aucun appel DeepSeek réel, aucune migration distante, aucun réglage Vault et aucune mutation GitHub/Supabase de production n'ont été effectués.
- Causes confirmées corrigées : (D01–D03) payload dispatcher incompatible et contexte métier transmis au lieu du couple minimal ; (D03/D22) jugement sur la mauvaise phase, exigence erronée de deadline et horloge lue avant une correction lente ; (D25) absence de budget/tentatives/configuration/cache et parsing de corps non borné. Les preuves sont les tests de contrat, les moteurs et les assertions pgTAP ; aucune hypothèse non testée n'est présentée comme cause.
- Difficultés réellement rencontrées : le premier test SQL Étape 4 a échoué avec `function public.server_settle_ai_usage(uuid, uuid, uuid, integer, unknown, unknown, unknown, jsonb, integer, integer, numeric, text, unknown, unknown) does not exist` à la ligne 204 ; cause confirmée, les littéraux `call_no` étaient inférés comme `integer` alors que la signature exige `smallint`, résolution par `1::smallint`, puis 138 tests passent. Le premier ciblage Vitest a échoué sur l'assertion `expected ... to contain 'v_job.phase_id is distinct from v_match.phase_id'` et sur une configuration attendue nulle mais héritant de `fixture-key` ; causes confirmées, garde atomique ajoutée dans une migration suivante et test corrigé pour vider la clé. La première compilation a échoué avec `Type '({ callNo, status, verdict, reasonCode }: AiAttemptSettlement) => Promise<number>' is not assignable to type '(settlement: AiAttemptSettlement) => Promise<void>` ; cause confirmée, `Array.push` retournait un nombre dans les callbacks de test, résolu par des blocs sans valeur de retour. Deux connexions Supabase locales initiales ont aussi échoué sur `dial tcp 127.0.0.1:54322: connect: operation not permitted` avant relance autorisée réussie. Une recherche Node de comptage mal formée a enfin échoué sur `Expected ',', got ';'`, sans effet sur le dépôt ; la commande corrigée a confirmé les 33 assertions initiales. Ces erreurs ne concernent pas une base distante.
- Hypothèses et limites : aucune cause hypothétique ne reste sur les chemins locaux testés. Le worker/dispatcher n'a pas été exercé avec deux navigateurs et un endpoint de production, aucun appel DeepSeek réel n'a été lancé, et les secrets Vault/worker Cron de production n'ont pas été configurés. Le contenu de fixture Trou Noir peut avoir un identifiant non UUID, d'où `question_revision_id` nullable pour le fallback fichier ; les contenus DB de production utilisent des UUID. La recette à deux sessions, le benchmark DeepSeek avec tarif daté et la préservation contrôlée des parties/jobs distants restent à faire dans les étapes prévues.
- Statut : l'étape 4 est présente dans le code et validée localement ; elle n'est pas déclarée déployée ni validée en production. Prochaine étape utile : recette isolée intégrée dispatcher → endpoint → worker avec deux sessions, puis configuration/validation distante autorisée à l'étape 10. Les migrations locales restent immuables après application ; toute correction future devra créer une nouvelle migration.
- Livraison : le commit `99688783358c83fa988410098dad9a9250742a2f` a été poussé vers `origin/main`. `git ls-remote origin refs/heads/main` a d'abord échoué avec `Could not resolve host: github.com` dans le sandbox ; la relance autorisée, en lecture seule, a confirmé `99688783358c83fa988410098dad9a9250742a2f refs/heads/main`.
- Contradiction : aucune avec `AGENTS.md`.

### 14/09/2026 — Exécution de l'étape 5 : règles, projections et métriques

- Précondition vérifiée : l'étape 4 est réellement validée localement avant cette implémentation. Le ciblage Étape 4 passe avec 7 fichiers et 84 tests ; la suite SQL locale passe avec 4 fichiers et 145 assertions ; `node_modules/.bin/tsc --noEmit`, `node_modules/.bin/eslint .`, `node scripts/check-docs.mjs`, `node scripts/check-test-matrix.mjs` et `git diff --check` passent. L'étape 4 n'est pas déclarée déployée ni validée en production.
- Réalisation Géographie : `resignTransition` distingue le forfait demandé (victoire du demandeur), la démission (victoire adverse) et l'interruption avant premier placement (abandon sans vainqueur). La projection conserve le point propre au joueur qui le connaît et masque celui de l'autre siège ; la carte utilise un espace de coordonnées SVG de base avec viewport appliqué une seule fois aux marqueurs, borne le curseur clavier et rend son curseur visible dès l'interaction. Le chargement GeoJSON est annulable, vérifie les réponses HTTP, affiche une erreur accessible et permet une nouvelle tentative.
- Réalisation UNO : la sélection de couleur reste ouverte pour un joker valide en `playing` ou `after_draw`, et se ferme uniquement si l'action n'est plus légale, si elle est annulée ou si l'utilisateur choisit une couleur. Le dialogue a autofocus initial, piège Tab, retour de focus et Échap ; la main, le siège actif, la phase et la présence de la carte sont contrôlés avant de conserver l'intention.
- Réalisation Compatibilité/Longueur d'onde : migration additive créée par la CLI `20260914163944_step5_game_rules_metrics.sql`, sans réécrire les migrations appliquées. Les triggers utilisent des variables `v_game_slug`, joignent explicitement `m.id = new.match_id`, normalisent les résultats coopératifs et recalculent les stats de duo sans collision de variable. Les résultats `cooperative`/`abandoned`, les historiques et les scores partagés restent distincts des victoires/défaites.
- Revue commune des neuf jeux : `RESIGN`, `CLAIM_FORFEIT`, `NEXT`, les actions `READY`, `RANDOMIZE_FLEET`, la résolution de contestation, les délais automatiques et les écrans `draw/cooperative/abandoned` ont été relus avec leurs projections et leurs résultats. Aucun secret, état complet, bonne réponse, flotte ou placement adverse n'est ajouté à une projection.
- Agrégation et hasard : `private.merge_game_metrics` additionne les compteurs et dénominateurs, fusionne les compteurs imbriqués, conserve minimum/maximum ou dernière valeur selon le champ et recalcule les moyennes/ratios. Les fonctions de reconstruction repartent des résultats conservés ; les triggers de stats ne remplacent plus un cumul par la dernière partie. L'entropie serveur par défaut passe à 4 096 octets normalisés et les préparations Geo automatiques utilisent ce budget ; les valeurs manquantes sont rejetées par les moteurs.
- Tests ajoutés : régressions moteur Geo/UNO/Compatibilité/Longueur d'onde et tests des deux sièges ; tests de projection Geo/UNO, limites du curseur, erreur de carte, validité des jokers et dialogue accessible ; tests de fusion de métriques et de définition/privilèges des triggers dans `supabase/tests/step5_game_rules_metrics.sql` (23 assertions prévues).
- Vérifications réellement réussies : `node_modules/.bin/vitest run` — 51 fichiers, 395 tests réussis et 2 `expected fail` historiques ; `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` — 5 fichiers, 168 tests ; `SUPABASE_TELEMETRY_DISABLED=1 supabase db lint --local` — aucune erreur de schéma, avec seulement des avertissements PL/pgSQL préexistants ; TypeScript, ESLint, documentation, matrice et diff passent. La base Supabase utilisée pour les assertions a été réinitialisée et reste strictement locale.
- Difficultés réellement rencontrées : une première exécution pgTAP avec une migration locale déjà appliquée révélait deux valeurs périmées (longueur maximale et précision navale) ; la cause était le rejeu incomplet de la migration et la collision entre la métrique scalaire `shots` et la liste des tirs, résolue par reset local et séparation des formes. Un test SSR attendait `&apos;` alors que React rend `&#x27;`, et un test de confidentialité Geo attendait à tort que le joueur perde son propre placement ; les deux attentes ont été corrigées sans modifier le contrat. `pnpm typecheck`/`pnpm lint` ont été bloqués par Corepack (`ERR_PNPM_META_FETCH_FAIL`, puis `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`) ; les binaires locaux versionnés passent.
- Limitations : aucune mutation Supabase distante, action GitHub de migration, configuration Vault, appel DeepSeek ou recette E2E à deux comptes n'a été effectué. La migration Étape 5 est versionnée et validée sur la base locale, mais doit être inspectée puis appliquée via le connecteur Supabase dans une tâche explicitement autorisée ; si ce connecteur échoue, la procédure de secours reste à fournir sous forme de commande `bash` ou de SQL `sql` selon la destination. Le recalcul historique est disponible via les fonctions `service_role` ; il ne fabrique aucune métrique absente des résultats conservés. La présence et le réseau restent hors périmètre, réservés à l'étape 6.
- Statut : l'étape 5 est présente dans le code et validée localement ; elle n'est pas déclarée déployée ni validée en production. Prochaine étape utile : revue distante en lecture seule puis recette autorisée de la migration, avant l'étape 6 de présence/réseau.
- Contradiction : aucune avec `AGENTS.md`.

### 14/09/2026 — Exécution de l'étape 6 : présence, réseau et reprise

- Précondition vérifiée : l'étape 5 est terminée dans le code et validée localement avant ce cycle, avec les commits fonctionnel `4d19233` et documentaire `ebf1348`, les tests moteur/projection/composants, la migration locale et la revue des neuf jeux déjà consignés ci-dessus. La suite applicative relancée pendant cette étape confirme les 52 fichiers Vitest, 401 tests réussis et 2 sentinelles d'échec attendu. Le nouveau contrôle SQL local n'a pas pu se connecter à PostgreSQL ; cette limitation d'outillage n'invalide pas la preuve SQL locale déjà obtenue pour l'étape 5.
- Helper commun : `src/lib/network-sync.ts` centralise `refresh`, `heartbeat`, `send`, l'état occupé, les messages de transport, les commandes en attente dans `sessionStorage`, le rejeu explicite avec le même `commandId`, la garde `resourceId`/génération, la mise à jour d'horloge et le contrôle des phases. Un clic double est ignoré par le verrou local ; une panne libère toujours l'interface dans `finally` et ne rejoue jamais automatiquement une commande dans une phase différente.
- Parties et salons : les neuf composants `src/games/*/components/*-match.tsx` et `src/app/salons/[roomId]/room-lobby.tsx` utilisent le helper. Les snapshots HTTP/Realtime ne sont appliqués que s'ils ciblent la ressource courante et portent une version strictement supérieure ; une réponse plus ancienne est ignorée. Un changement de `matchId`, un retour d'onglet, `online` ou une reconnexion Realtime déclenchent une relecture puis un heartbeat. Le polling et le heartbeat sont arrêtés dès que la projection indique une fin.
- Realtime et accès : `src/lib/realtime.ts` valide les identifiants/versions des invalidations, gère les statuts de souscription et signale les reconnexions. La migration additive CLI `supabase/migrations/20260914173142_step6_realtime_active_membership.sql` restreint le Broadcast privé utilisateur à l'extension Broadcast et à `public.is_site_member()` ; aucune migration antérieure n'a été réécrite et aucune mutation Supabase distante n'a été exécutée.
- Tests de coupure et responsive : `tests/e2e/network-regressions.spec.ts` couvre RESIGN, réponse Trou Noir, FIRE, placement Géographie et RANDOMIZE_FLEET ; chaque scénario vérifie la conservation de l'intention et la libération du bouton. `tests/e2e/uno-regressions.spec.ts` couvre aussi le dialogue joker, la panne, le double clic, le heartbeat, la réponse perdue, le Realtime manqué et deux onglets. Les 24 tests ciblés passent sur Chromium et mobile.
- Vérifications réellement réussies : `node_modules/.bin/tsc --noEmit`, ESLint ciblé, Vitest ciblé (3 fichiers/9 tests), suite Vitest complète (52 fichiers/401 tests + 2 attendus), `node_modules/.bin/next build --webpack`, Playwright ciblé (24/24 desktop/mobile), `node scripts/check-docs.mjs`, `node scripts/check-test-matrix.mjs` et `git diff --check`.
- Difficultés réellement rencontrées : la première commande `supabase migration new` a échoué sur l'écriture de `~/.supabase/telemetry.json.tmp` avec `EPERM` ; la relance avec `SUPABASE_TELEMETRY_DISABLED=1` a créé la migration par la CLI. Le premier lancement Playwright via `webServer` a échoué avec `Process from config.webServer was not able to start. Exit code: 1` ; le serveur Next local a ensuite été lancé explicitement, puis les 24 tests ont passé. Une assertion statique intermédiaire a aussi ciblé à tort `geographie-match.tsx` au lieu de `geography-match.tsx` ; le chemin exact a été corrigé et les 401 tests passent. Une tentative de réinitialiser l'état de ressource dans un effet a été refusée par `react-hooks/set-state-in-effect` ; l'état occupé et l'horloge sont maintenant indexés par ressource, le nettoyage de génération reste sans rendu en cascade, et lint/typecheck repassent. Une dernière passe parallèle a fait lire `.next` à `tsc` pendant sa régénération et a produit des `TS6053` ; le typecheck séquentiel relancé après le build passe. Les relances `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` et `supabase db lint --local` échouent toutes deux avec `dial tcp 127.0.0.1:54322: connect: operation not permitted` ; aucun fichier distant ni schéma distant n'a été modifié.
- Limitations : les E2E à deux comptes réels restent dépendants de `E2E_PASSWORD`, la souscription Broadcast réelle et la migration Étape 6 restent à inspecter/appliquer via le connecteur dans une tâche explicitement autorisée, et la base locale PostgreSQL est indisponible dans cet environnement. Aucun secours bash/sql n'a été fourni car aucune tentative de mutation distante ni aucun connecteur de mutation n'a été lancé ; si une application distante est autorisée ultérieurement, respecter la procédure `bash` terminal ou `sql` éditeur Supabase selon la destination, jamais GitHub.
- Statut : l'étape 6 est présente dans le code et validée localement pour les tests applicatifs et navigateur ciblés ; elle n'est pas déclarée déployée, validée sur Supabase distant ni validée avec deux comptes réels. Prochaine étape utile : revue/validation distante autorisée puis étapes 7–9 du plan, sans modifier les règles ou migrations des étapes précédentes.
- Contradiction : aucune avec `AGENTS.md`.

### 14/09/2026 — Exécution de l'étape 7 : erreurs, versions et contenu

- Précondition vérifiée : l'étape 6 est terminée dans le code et testée avant ce cycle. Le commit `55687d0` est présent sur `main` et aligné avec `origin/main` ; la suite réseau ciblée passe et la validation navigateur de l'étape 6 déjà consignée reste à 24/24 tests Chromium/mobile. L'étape 7 a donc pu commencer sans modifier son périmètre.
- HTTP et authentification : `src/server/http.ts` n'accepte plus une chaîne arbitraire comme `error.code`. Les codes publics sont catalogués et stables ; les erreurs inconnues, dont les messages SQL, deviennent `INTERNAL_ERROR` avec un `x-diagnostic-id` interne et un journal sans message brut. Les réponses JSON utilisent `private, no-store`, `Pragma: no-cache` et `Vary: Cookie`. `safeNext` côté formulaire et callback rejette les origines externes, doubles slashs, antislashs, encodages répétés/malformés, caractères de contrôle et chemins non autorisés ; le formulaire libère toujours son état occupé et nettoie une session invité incomplète.
- Versions et loaders : les loaders fichier et base de Trou Noir, TTMC, Compatibilité, Longueur d'onde et Géographie conservent leurs anciennes signatures/chemins tout en validant désormais l'identité du pack, la version, le manifeste, la quantité, la couverture et le checksum lorsqu'ils sont disponibles. Les références attendues `{packId, packVersion}` sont refusées si elles ne correspondent pas. Le branchement de ces références dans le snapshot persistant des parties existantes reste à traiter avec les routes/migrations concernées, hors modification autorisée de cette étape ; aucune partie en cours ne perd son ancien loader.
- Packs et scripts : les manifests des cinq packs concernés portent provenance, licence, auteur, relecture, checksum et couverture. Le pack Trou Noir contient réellement 120 questions (24 par catégorie, 6 par couple catégorie/difficulté), avec seuil de lancement documenté à 300 questions et 60 par catégorie ; `launchReady` reste faux. TTMC contient 440 questions réparties sur 22 thèmes, avec au moins 2 questions par niveau et thème ; les configurations 20/15 et 30/20 sont disponibles, 50/30 est refusée, et le seuil de lancement à 30 thèmes reste faux. `scripts/content/validate-packs.mjs` ne fait que lire/valider ; les générateurs de migrations Compatibilité/Longueur d'onde sont séparés des générateurs de packs et n'écrivent qu'une nouvelle migration. Les scripts Géographie utilisent `fileURLToPath`, et `write-new-migration.mjs` refuse l'existence d'une migration, y compris en cas de course `EEXIST`. Aucune migration versionnée existante n'a été réécrite.
- Revue sécurité/advisors : les alertes précédemment observées sur RLS/ACL, `SECURITY DEFINER`, `pg_net`, accès invité, mots de passe compromis et index de clés étrangères ont été relues. Aucune remédiation automatique n'a été lancée : il manque, pour chaque alerte, la preuve du risque dans l'environnement cible, la politique écrite et l'autorisation explicite requises par le périmètre. La commande locale de conseil a seulement tenté une lecture et n'a pas pu joindre PostgreSQL.
- Vérifications réellement réussies : `node scripts/content/validate-packs.mjs` ; `node_modules/.bin/vitest run` (56 fichiers, 415 tests réussis et 2 échecs attendus) ; `node_modules/.bin/tsc --noEmit` ; `node_modules/.bin/eslint .` ; `node_modules/.bin/next build --webpack` ; `node scripts/check-docs.mjs` (25 fichiers) ; `node scripts/check-test-matrix.mjs` (10 parcours, 5 pass, 4 blocked, 1 not-run) ; `git diff --check`. Les cinq garde-fous d'écriture de migration ont été exercés sur les migrations existantes et ont tous refusé l'écrasement. Aucun appel de mutation Supabase distante, action GitHub de migration, déploiement ou écriture de schéma distant n'a été effectué.
- Difficultés réellement rencontrées : `pnpm --offline content:validate` a été rejeté par pnpm avec `Unknown option: 'offline'`; `pnpm content:validate` a ensuite échoué avec `[ERR_PNPM_META_FETCH_FAIL] GET https://registry.npmjs.org/pnpm: fetch failed`, puis `[ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY]`, donc la commande équivalente directe `node scripts/content/validate-packs.mjs` a été utilisée et passe. Une commande de garde de migration mal quotée a renvoyé exactement `zsh:1: unmatched "`; la relance avec un quoting corrigé a réussi. Une première exécution de typecheck a signalé l'export de `safeNext` dans le handler Next (`.next/types` rapportait une incompatibilité de propriété) ; l'helper est redevenu interne au routeur et le typecheck final passe. Une première passe ESLint signalait `manifestText` inutilisé dans le loader Geo ; la variable a été supprimée et ESLint final passe. `SUPABASE_TELEMETRY_DISABLED=1 supabase db advisors --local --output-format json` n'a pas pu se connecter : `failed to connect to postgres: failed to connect to \`host=127.0.0.1 user=postgres database=postgres\`: `dial error (dial tcp 127.0.0.1:54322: connect: operation not permitted)` ; aucune remédiation n'a suivi.
- Limitations : les références de contenu ne sont pas encore persistées dans `MatchSnapshot`/les routes de création existantes, car ces emplacements sortent du périmètre Step 7 ; la conservation des parties déjà en cours est assurée par les loaders compatibles, mais la liaison complète version-partie nécessitera une étape autorisée distincte. La validation distante des advisors et des migrations, la recette à deux comptes et le benchmark DeepSeek restent à faire. Aucun secours `bash`/`sql` n'a été nécessaire : aucun connecteur de mutation distante n'a été appelé ni n'a échoué ; si une application Supabase est autorisée plus tard, appliquer la procédure documentée selon la destination, jamais GitHub.
- Statut : l'étape 7 est présente dans le code et validée localement pour les erreurs HTTP/auth, loaders, manifests et scripts ; elle n'est pas déclarée déployée ni validée sur Supabase distant. Prochaine étape utile : persister explicitement la référence de contenu au démarrage des nouvelles parties dans un périmètre autorisé, puis effectuer la revue distante et la recette à deux sessions.
- Contradiction : aucune avec `AGENTS.md`.

### 14/09/2026 — Exécution de l'étape 8 : parcours salons, compte, historique et entraînement

- Précondition vérifiée : l'étape 7 est terminée dans le code et testée avant ce cycle. Le commit `f0281a7` est présent sur `main` et aligné avec `origin/main`; `node scripts/content/validate-packs.mjs`, les 23 tests ciblés de l'étape 7 et les critères documentés de `progression.md` sont passés. L'étape 8 a donc pu commencer.
- Salons : `src/app/salons/[roomId]/room-lobby.tsx` conserve les commandes réelles `SET_READY` et `START`, affiche les deux sièges, l'état réseau et l'expiration quand `hostId`/`expiresAt` sont projetés, et propose la sortie/configuration/transfert uniquement avec `ROOM_CHANGE_RPC_ENABLED=true`. `src/app/salons/[roomId]/actions/route.ts` valide les neuf configurations, l'identité, la version et le `commandId`, puis cible la RPC atomique documentée `server_change_room`; il n'y a donc aucun bouton décoratif si cette RPC n'est pas disponible. Les helpers/test associés couvrent l'hôte projeté ou le repli siège 0, exactement deux membres et les limites d'expiration.
- Compte : `src/app/profil/profile-editor.tsx`, `src/app/profil/profile/route.ts` et `src/app/profil/avatar/route.ts` permettent de modifier pseudo/preset et de téléverser un avatar privé après contrôle MIME, signature, taille et dimensions, puis réencodage WebP 256×256. `src/app/profil/account-security.tsx` et `/profil/mot-de-passe` couvrent oubli, changement, renvoi de confirmation et récupération Supabase côté navigateur; les invités restent sans mot de passe, avatar personnalisé, profil persistant ou historique.
- Historique : `src/app/historique/**` et `src/app/api/history/**` ajoutent une page de 20 lignes avec curseur, filtres jeu/issue, pseudo adverse issu du snapshot, détail membre-only et historique du duo paginé. Les issues victoire, défaite, égalité, coopération et interruption restent séparées; les agrégats distinguent victoires des deux côtés, égalités, coopération et abandons. Le détail de manche est filtré par liste blanche (Compatibilité, Longueur d'onde et résumé Skyjo); les payloads privés et réponses secrètes ne sont pas renvoyés.
- Entraînement BombParty : `src/games/bombparty/training.ts`, `training-syllabes.tsx` et la route de vérification bornent `usedWords` à 200 après normalisation/déduplication, plafonnent les suggestions à 20 par page, ignorent les réponses d'une requête ou séquence obsolète et restaurent la session locale après navigation avec reprise/arrêt correct du chrono.
- Tests réellement réussis : 5 fichiers/19 tests ciblés après le correctif de récupération; suite Vitest complète `59` fichiers, `424` tests réussis et `2` sentinelles `it.fails` attendues (`step1-contracts.test.ts`); `node_modules/.bin/tsc --noEmit`; `node_modules/.bin/eslint .`; `node_modules/.bin/next build --webpack` (routes Step8 générées); `node scripts/content/validate-packs.mjs`; `node scripts/check-docs.mjs`; `node scripts/check-test-matrix.mjs`; `git diff --check`. La recette navigateur de l'accueil est concluante. Les E2E ciblés auth/réseau/UNO couvrent desktop Chromium et mobile : `26` passés, `2` ignorés par les garde-fous d'authentification sans `E2E_PASSWORD`.
- Limites de périmètre et de schéma : aucune migration n'a été créée ou modifiée et aucune mutation Supabase distante n'a été exécutée. L'audit local des migrations ne contient pas `server_change_room`; `server_join_room` vérifie encore l'expiration avant l'idempotence du membre déjà présent; `RoomView` ne projette pas encore `hostId`/`expiresAt`. En conséquence, sortie réelle, changement de jeu/configuration, transfert d'hôte et reprise après expiration sont codés côté route/UI et restent désactivés par défaut jusqu'à une migration/RPC additive explicitement autorisée. La création du bucket privé `avatars` et la recette Auth permanente nécessitent la configuration de l'environnement; aucun secret n'a été inventé. Les détails `round_results` des autres jeux restent masqués faute de projection sûre dédiée.
- Difficultés réellement rencontrées : l'exécution E2E avec la configuration standard a d'abord échoué avant les tests (`Process from config.webServer was not able to start. Exit code: 1`) car elle tentait de relancer `pnpm exec next dev`; l'exécution sandboxée a ensuite échoué au lancement Chromium avec `bootstrap_check_in ... Permission denied (1100)` et WebKit avec `Abort trap: 6`. Après autorisation d'exécution, un serveur local existant et une configuration Playwright temporaire non versionnée, les 26 scénarios restants ont passé et cette configuration a été supprimée. Une première lecture de chemins dynamiques non quotés a produit `zsh:1: no matches found`; une première lecture du dépôt a ciblé à tort `src/server/rooms/repository.ts` inexistant; un premier patch de chemin avec apostrophes a échoué, puis les chemins `[playerId]` corrects ont été créés sans fichier parasite suivi. Le premier typecheck du listener Supabase a signalé trois paramètres implicites (`TS7031`/`TS7006`); les types `AuthChangeEvent`/`Session` ont été ajoutés et le typecheck final passe. Le build Webpack conserve seulement l'avertissement non bloquant `Critical dependency: the request of a dependency is an expression` dû au chargement différé de `sharp` dans l'upload avatar.
- Vérification de livraison : `git push origin main` a réussi avec `f0281a7..ff13c2a main -> main`, puis `git status` et `git log` indiquent `HEAD` et `origin/main` sur `ff13c2a`. La relecture indépendante `git ls-remote origin refs/heads/main` a échoué exactement avec `Could not resolve host: github.com`; le hash distant n'a donc pas pu être relu après le push, sans indication d'un échec du push.
- Contradiction : aucune avec `AGENTS.md`; les migrations sont restées immuables, GitHub n'a pas servi à appliquer le schéma et aucune mutation distante n'a été lancée.
- Statut : les parcours compte, historique, duo et entraînement sont présents dans le code et validés localement; les garde-fous de confidentialité et d'exactement deux membres sont en place. L'étape n'est pas déclarée déployée ni validée avec deux comptes permanents; la gestion avancée des salons reste en attente de la RPC/projection serveur mentionnée ci-dessus. Prochaine étape utile : étape 9 sur environnement isolé, puis une tâche explicitement autorisée pour réconcilier le schéma salon et valider deux sessions.

## Points à savoir pour les prochains développements

- Une fiche Markdown est un contrat de conception, pas la preuve qu'une fonction existe.
- Toute fonctionnalité doit être suivie séparément comme documentée, codée, testée localement, validée avec deux sessions et déployée.
- Une partie V1 a exactement deux participants ; les résultats coopératifs ne doivent jamais être transformés en défaite artificielle.
- Le navigateur envoie une intention. Le serveur choisit l'acteur authentifié, le score, les cartes, la bonne réponse, l'heure et l'état suivant.
- Les moteurs sont purs et versionnés (`rulesVersion`, `engineVersion`, `stateSchemaVersion`).
- Les secrets restent côté serveur ; aucun état complet, secret ou contenu sensible dans le bundle, `NEXT_PUBLIC_*`, Realtime ou les logs.
- Les migrations Supabase sont versionnées, testées localement et immuables après application. Une migration de production est un événement de déploiement.
- L'inspection Supabase peut être faite en lecture seule ; l'application ou la modification distante exige une autorisation explicite de la tâche active et passe par le connecteur prévu ou le secours précisément formaté.
- Toute tâche ayant produit une modification réelle du dépôt se termine par un commit et un push vers `main`, sauf instruction explicite contraire.
- Les tests payants DeepSeek ne doivent pas tourner dans la CI standard ; utiliser un provider mock et un benchmark réel daté avant activation.
- Toute modification importante doit laisser `progression.md` plus précis qu'avant, sans annoncer une capacité non vérifiée.

## Journal des contradictions et décisions utilisateur

Cette rubrique concerne uniquement les demandes explicites de l'utilisateur qui modifient une règle d'`AGENTS.md`. Une documentation obsolète ou une erreur d'implémentation n'est pas une contradiction utilisateur.

| Date | Demande ou décision utilisateur | Ancienne règle | Nouvelle règle | Périmètre / raison | Statut |
|---|---|---|---|---|---|
| 11/09/2026 | Récupérer le dépôt GitHub et configurer le push sur `main`. | `main` est déjà la branche de production et le remote de référence. | Aucun changement de règle. | Demande conforme aux guidelines existantes. | Consigné, sans dérogation |
| 11/09/2026 | Demande de maintenir un suivi de progression et d'actualiser `AGENTS.md` de manière proactive. | Mise à jour du statut seulement en fin d'implémentation. | `progression.md` devient le suivi opérationnel ; l'agent doit le mettre à jour après chaque changement significatif et journaliser les contradictions explicites. | Améliore la traçabilité du projet. | Appliqué |
| 11/09/2026 | Documenter automatiquement les problèmes signalés pendant le code ou détectés par les tests. | La section mélangeait difficultés réelles et risques anticipés. | La section devient un journal simple des problèmes réellement rencontrés ; les risques théoriques n'y sont plus ajoutés. | Permet de retrouver les erreurs et leurs résolutions sans bruit. | Appliqué |
| 11/09/2026 | Refonte de la homepage en violet, clean, responsive et sobre. | Aucune règle métier ou technique ne prescrivait une exception pour la homepage. | Aucun changement de règle ; le périmètre reste visuel et conserve les flux réels ainsi que les jeux non disponibles désactivés. | Demande conforme aux contrats UI et d'accessibilité existants. | Consigné, sans dérogation |
| 12/09/2026 | Supprimer l’obligation d’invitation et permettre l’inscription e-mail/mot de passe avec un compte visible après connexion. | Les comptes étaient admis uniquement via invitation et le profil/membre étaient créés après admission. | Inscription libre ; provisionnement automatique/rattrapage du profil et de l’admission, confirmation e-mail selon la configuration Auth, espace `/profil`. | Permettre à deux amis de créer leurs comptes sans intervention manuelle, tout en conservant Auth Supabase, les contrôles serveur et RLS. | Implémenté dans l’arbre de travail ; migration et configuration distante non vérifiées |
| 13/09/2026 | Ne plus utiliser la connexion GitHub/Supabase pour les migrations ; privilégier le connecteur Supabase Codex et fournir un secours copiable en cas d'erreur. | L'intégration GitHub native Supabase appliquait les migrations après push/merge sur `main`. | GitHub sert au dépôt, à la revue et à la CI ; les migrations distantes passent par le connecteur Supabase. En cas d'échec, fournir la commande ou le SQL exact dans un bloc `bash` avec l'erreur exacte ; aucun basculement silencieux vers GitHub. | Garder le contrôle explicite de l'application du schéma et permettre une exécution manuelle fidèle dans Supabase. | Appliqué dans la documentation ; connecteur et secours manuel non testés dans cette tâche |
| 13/09/2026 | Clarifier le secours Supabase, le statut réel, le sens de « privé », l'obligation de livraison et l'autorisation des mutations distantes. | Le secours ne distinguait pas terminal et éditeur SQL ; le début d'`AGENTS.md` donnait un statut daté ; « privé » pouvait signifier invitation obligatoire ; les commits/push n'étaient pas requis ; une mutation distante pouvait être lue comme automatique. | La destination du secours est explicitement indiquée ; `progression.md` fait foi ; privé ne signifie pas invitation ; toute modification réelle est commitée et poussée vers `main` sauf opposition explicite ; inspection distante par défaut en lecture seule et mutation seulement autorisée dans la tâche active. | Réduire les hésitations d'un modèle d'exécution et conserver le contrôle humain sur les changements Supabase. | Appliqué dans la documentation ; commit `408a96d` poussé sur `origin/main` et vérifié |

### 12/09/2026 — Accès invité et délégation Muse

- Demande : depuis l'inscription, proposer « Continuer en tant qu'invité », avertir dynamiquement que le pseudo et la progression ne seront pas sauvegardés, puis permettre de rejoindre les salons comme avec un compte.
- Réalisation : ajout du parcours UI et du dialogue accessible, création d'une session `signInAnonymously`, provisionnement idempotent d'un membre actif avec pseudo aléatoire serveur, propagation du statut `isGuest` dans l'acteur, les salons et les parties, et refus explicite des routes de profil/historique pour les invités. Une inscription permanente quitte d'abord une session anonyme locale ; aucune conversion implicite n'est promise.
- Protection des données : la migration CLI `20260912000100_anonymous_guest_access.sql` exclut les invités des lectures d'historique/statistiques et annule leurs écritures personnelles ou d'agrégats de duo via triggers. Aucun nettoyage automatique des comptes anonymes n'est encore configuré.
- Vérifications réussies : `pnpm test` (39 fichiers / 311 tests), `pnpm typecheck`, `pnpm lint`, `pnpm exec next build --webpack`, `pnpm docs:check`, `git diff --check` et `pnpm test:e2e tests/e2e/auth-guest.spec.ts` (Chromium + WebKit : 2 tests réussis). Le lint SQL local n'a pas pu démarrer faute de Postgres/Docker accessible (`127.0.0.1:54322`, `operation not permitted`).
- Difficultés d'outillage : la première recette E2E dans le sandbox a été refusée au bind du port 3000 (`listen EPERM`) et la première exécution hors sandbox signalait WebKit absent ; l'exécution autorisée après installation du binaire WebKit passe sur les deux projets. La relecture de l'état distant Supabase reste impossible dans ce shell sans `SUPABASE_ACCESS_TOKEN` ; aucune application distante de la migration invitée n'est déclarée.
- Délégation : trois tentatives OpenCode CLI dans un contexte indépendant avec `opencode/muse-spark-1.3-contributor-free` ont été interrompues par l'outillage ou le fournisseur (`Rate limit exceeded. Please try again later`) ; Muse n'a écrit aucun fichier. Luna a repris l'implémentation, la revue et la validation.
- Contradiction : aucune contradiction avec `AGENTS.md` ; la fonctionnalité respecte l'exigence de deux participants, les mutations serveur, RLS et la source de vérité PostgreSQL.

### Incohérences documentaires corrigées, sans décision utilisateur

- Le README indiquait qu'aucun jeu n'était implémenté ; il indique maintenant que l'accueil, Géographie et UNO sont présents dans `main`.
- Le suivi indiquait Géographie sur `codex/geographie` et UNO non commité ; il est maintenant réaligné sur `main` et le commit `2a33854`.
- `AGENTS.md` indiquait que le dépôt ne contenait aucune migration ; il précise maintenant que les migrations versionnées existent, sans conclure à leur application distante.

### 12/09/2026 — Session invité invisible côté SSR après `signInAnonymously`

- Problème signalé : après une connexion invité depuis `src/components/auth-form.tsx`, la navigation vers `/` arrivait déconnectée côté SSR. Causes confirmées dans le code : aucun proxy de rafraîchissement de session (convention Next.js 16), écriture `setAll` sans effet dans les Server Components, et route `/auth/callback` liant son client Supabase au magasin `cookies()` de la requête au lieu de la réponse de redirection (cookies d'échange perdus).
- Résolution : `src/proxy.ts` + `src/server/supabase/session-refresh.ts` (motif officiel `getUser()` + recopie `setAll` vers la réponse, sans redirection métier) ; `src/app/auth/callback/route.ts` écrit désormais les cookies de session sur sa réponse de redirection via `NextRequest`. Flux invité `router.push` + `router.refresh()` inchangé : il suit déjà le motif documenté une fois le proxy présent.
- Vérification : nouveau `src/server/supabase/session-refresh.test.ts` (2 tests : recopie des cookies rafraîchis, passage sans configuration), `pnpm test` (40 fichiers / 313 tests), `pnpm typecheck`, `pnpm lint`, `pnpm exec next build --webpack`, `pnpm docs:check`, `git diff --check` et `pnpm test:e2e tests/e2e/auth-guest.spec.ts` (Chromium + WebKit : 2 tests) réussis. Le build affiche `ƒ Proxy (Middleware)`.
- Difficulté de validation : un premier lancement parallèle du build et du typecheck a provoqué une course sur les fichiers générés `.next/types` ; les commandes ont été relancées séquentiellement et passent. Le scénario complet avec un compte Supabase anonyme réel reste à confirmer après déploiement.
- Contradiction : aucune avec `AGENTS.md` ; correctif limité au périmètre du bug, hors règles métier.

### 12/09/2026 — Navigation RSC trop tôt après connexion invité / e-mail

- Problème signalé : après une connexion invitée ou e-mail/mot de passe depuis `src/components/auth-form.tsx`, `router.push` déclenchait une navigation RSC trop tôt et le site revenait déconnecté.
- Résolution : redirections après authentification réussie uniquement remplacées par une navigation complète du navigateur (`window.location.href = "/profil"` en signIn/signUp avec session, `window.location.href = safeNext(...)` en invité) ; `safeNext` conservé pour l'invité, flux métier (provisionnement, avertissement, erreurs) inchangé, `useRouter`/`router.refresh` retirés de ce composant.
- Vérification : nouveau `src/components/auth-form.test.ts` (2 tests : navigation complète sans `router.push`/`router.refresh`/`useRouter`, garde `safeNext` conservé), `pnpm vitest run src/components/auth-form.test.ts` (1 fichier / 2 tests) réussi, `pnpm typecheck` et `pnpm lint` réussis sans avertissement ; la règle Next est désactivée localement sur les deux navigations complètes intentionnelles.
- Contradiction : aucune avec `AGENTS.md` ; correctif limité au périmètre demandé.

### 12/09/2026 — Projection serveur impossible après provisionnement

- Problème signalé et reproduit en production : l'authentification et `/api/auth/provision` réussissaient, mais les pages et API qui appellent `getAuthenticatedAccount()` retombaient sur l'état déconnecté. Le profil invité créé était bien actif en base.
- Cause confirmée par `supabase db query --linked` en `service_role` : `public.server_get_actor(uuid)` lisait `auth.users` en `SECURITY INVOKER`, avec l'erreur `permission denied for table users`.
- Résolution : migration `supabase/migrations/20260912085840_server_get_actor_security_definer.sql` ; la RPC serveur reste interdite aux rôles client et passe en `SECURITY DEFINER` avec `search_path` vide, afin que sa lecture interne de `auth.users` fonctionne.
- Vérification : migrations locales et distantes synchronisées avant ce correctif ; profil invité actif et RPC `server_get_actor` vérifiés en lecture seule ; `pnpm test` (41 fichiers / 316 tests), `pnpm typecheck`, `pnpm lint`, `pnpm docs:check`, `git diff --check` et `pnpm exec next build --webpack` réussis ; `supabase db push --linked --dry-run` a confirmé que cette migration était la seule en attente. Après le push, `supabase migration list` confirme l'application distante, une requête `service_role` retourne l'acteur, et un navigateur neuf en production affiche `Invité · Joueur-…`, `Créer un compte` et `Se déconnecter` sur l'accueil.
- Limitation de recette : le parcours e-mail/mot de passe n'a pas été exécuté avec un compte existant, car aucun identifiant de test n'a été fourni ; il partage toutefois le même `getAuthenticatedAccount()`/`server_get_actor` corrigé et le même correctif de navigation complète.
- Difficultés de diagnostic : un premier appel `supabase migration list` a été bloqué par l'écriture de télémétrie locale puis a réussi avec l'autorisation adaptée ; le dépôt ne contient pas de `.env.local`, donc la comparaison directe avec la clé serveur n'a pas été possible depuis le poste. Une requête liée exécutée en `service_role` a toutefois reproduit exactement l'erreur SQL. La délégation OpenCode à Muse n'a pas pu démarrer (FSEvents puis connexion API) ; la migration minimale a été reprise par l'orchestrateur.
- Contradiction : aucune avec `AGENTS.md` ; correctif limité à l'autorisation interne nécessaire à la projection serveur.

Ces corrections ne constituent pas des contradictions de l'utilisateur avec `AGENTS.md`.

### 12/09/2026 — Création d’un salon en erreur serveur après l’accès invité

- Problème signalé : l’interface affiche « Une erreur serveur est survenue. » lors de la création d’une partie en mode aléatoire. Le chemin client envoie bien `POST /api/rooms`; la route valide la configuration puis appelle `server_create_room` via le client serveur Supabase.
- Cause fortement établie dans le code : `server_create_room` appelle `private.refresh_room_views` après l’insertion du salon. La migration `20260912000100_anonymous_guest_access.sql` a remplacé ce helper en ajoutant `exists (select 1 from auth.users ...)` pour calculer `isGuest`, mais la fonction reste `SECURITY INVOKER`. Le même accès à `auth.users` en invoker avait déjà produit en production `permission denied for table users` dans `server_get_actor`; ce dernier a été corrigé séparément par `20260912085840_server_get_actor_security_definer.sql`. La création peut donc être annulée après l’insertion logique, puis l’API reçoit un message SQL non mappé et le transforme en 500 générique.
- Confirmation distante manquante pour cette occurrence précise : les commandes CLI Supabase sont installées mais cette session ne reçoit pas de `SUPABASE_ACCESS_TOKEN`, et le dashboard ouvert n’est pas authentifié. La preuve locale et l’incident distant analogue rendent cette cause très probable, sans la déclarer encore confirmée par les logs actuels.
- Impact connexe à vérifier : `server_get_match` et `server_get_pair_history` de la même migration lisent aussi `auth.users` en `SECURITY INVOKER`; une correction limitée à la création risquerait de déplacer l’erreur au chargement de la partie ou de l’historique.
- Plan de correction, non appliqué dans cette tâche : inspecter d’abord à distance les définitions, privilèges et logs PostgreSQL; créer une nouvelle migration additive (sans réécrire les migrations appliquées) qui sécurise les helpers/RPC serveur concernés avec un contexte `SECURITY DEFINER` strict, `search_path` vide, schéma privé ou droits `service_role` explicites, et sans exposition aux rôles client; compléter les tests de régression SQL/projection puis tester création, chargement et historique avec deux sessions. Enfin vérifier l’historique de migrations et le comportement de l’API afin que les erreurs métier restent mappées et que les erreurs SQL ne soient plus masquées sans trace.
- Vérifications effectuées dans cette session : lecture croisée du routeur, du repository, des migrations et du correctif `server_get_actor`; la CLI Supabase 2.104.0 a été invoquée en lecture seule mais a refusé les requêtes distantes faute de jeton. Aucun RPC de création n’a été exécuté, aucune donnée Supabase n’a été modifiée, et aucun correctif applicatif ou SQL n’a été appliqué.

### 12/09/2026 — Reproduction distante de l’erreur de création de salon

- Connexion rétablie au projet Supabase `ttogfwnlknmiscnmlhof` via le dashboard, branche `main`. Les fonctions distantes correspondent au code local : `private.refresh_room_views(uuid)`, `public.server_create_room(uuid,uuid,text,jsonb)`, `public.server_get_match(uuid,uuid)` et `public.server_get_pair_history(uuid,uuid,text)` sont toutes `SECURITY INVOKER`, tandis que `public.server_get_actor(uuid)` est `SECURITY DEFINER`.
- Vérification des privilèges en lecture seule : `service_role` peut exécuter `refresh_room_views` et `server_create_room`, mais `has_table_privilege('service_role', 'auth.users', 'select')` retourne `false`.
- Reproduction contrôlée sous `service_role` : l’appel de `private.refresh_room_views` sur le salon existant échoue avec `ERROR 42501: permission denied for table users`, hint PostgreSQL `GRANT SELECT ON auth.users TO service_role`, contexte `private.refresh_room_views(uuid) line 9 at SQL statement`. Le contexte correspond exactement au `select ... from auth.users` ajouté par la migration invités.
- Cause confirmée : `server_create_room` insère le salon puis appelle `refresh_room_views`; ce helper tente de lire `auth.users` avec le rôle invoker `service_role`, qui n’a pas ce privilège. PostgreSQL annule alors la transaction. Le message SQL non mappé est ensuite transformé par `mapServerError` en « Une erreur serveur est survenue. ».
- Aucun changement persistant : les vérifications ont été faites en lecture seule ou dans une transaction annulée; aucun salon, privilège, migration ou fonction n’a été modifié.
- Plan de correction inchangé et non appliqué : nouvelle migration additive, helper interne sécurisé et étroitement autorisé pour la lecture `auth.users`, traitement des lectures analogues dans `server_get_match`/`server_get_pair_history`, tests SQL/API puis recette à deux sessions. Ne pas accorder directement une lecture large de `auth.users` à `service_role` sans revoir le modèle de sécurité.

### 12/09/2026 — Correctif local des projections invitées

- Résolution implémentée dans l’arbre de travail : nouvelle migration CLI `supabase/migrations/20260912092235_fix_guest_auth_projection_permissions.sql`. Elle ajoute `private.site_members.is_guest`, recalcule ce statut depuis `auth.users` dans le contexte privilégié de la migration, puis le maintient lors du provisionnement des comptes.
- Les RPC `private.refresh_room_views`, `public.server_get_actor`, `public.server_get_match` et `public.server_get_pair_history` redeviennent des fonctions `SECURITY INVOKER` sans lecture de `auth.users`; elles utilisent uniquement la projection privée `site_members`. Aucun privilège SELECT large sur `auth.users` n’est accordé à `service_role`.
- Test de régression ajouté : `src/server/guest-auth-projection-permissions.test.ts`, vérifiant la matérialisation du statut invité, les fonctions invoker et l’absence de lecture `auth.users` dans les RPC concernées.
- Vérifications réussies : `pnpm test` (42 fichiers / 318 tests), `pnpm typecheck`, `pnpm lint`, `pnpm exec next build --webpack`, `pnpm docs:check` et `git diff --check`.
- Difficulté de validation : `supabase db lint --local --fail-on error` ne peut pas se connecter à Postgres local (`127.0.0.1:54322`, `operation not permitted`). La migration n’est pas encore appliquée au projet distant, aucun commit/push/déploiement n’a été effectué dans cette tâche.
- Prochaine étape de recette : appliquer cette migration via le flux de déploiement Supabase prévu, vérifier `supabase migration list`, puis retester la création d’un salon aléatoire avec la session invitée et le chargement d’une partie/historique avec deux sessions.
- Contradiction : aucune avec `AGENTS.md` ; correctif limité aux permissions de lecture nécessaires aux projections serveur.

### 12/09/2026 — TTMC bloqué après expiration du choix de niveau

- Problème signalé avec capture d'écran : après « Temps écoulé, niveau 1 attribué… », le compteur reste à `0s` et la partie demeure visuellement bloquée.
- Constat confirmé par lecture du code : le composant client calcule localement `expired` et affiche ce libellé, mais n'envoie aucune commande d'expiration. La transition réelle (`choose_level` → `answering`, niveau 1, question et nouveau délai) dépend du job serveur `choose_level_timeout` traité par le worker.
- Cause probable à confirmer à distance : le dépôt contient le dispatcher `private.dispatch_due_jobs()` et le worker TTMC, mais aucune déclaration `cron.schedule`/équivalente n'est versionnée ; le statut du projet indique également que Cron, pg_net, Vault, les baux et la latence de production ne sont pas vérifiés. Une absence/mauvaise configuration du réveil, des secrets Vault ou du endpoint worker peut donc laisser le job en attente.
- Plan initial non appliqué au moment du diagnostic : inspecter le match et son job en base, vérifier le cycle Cron → dispatcher → pg_net → `/api/internal/jobs/run` → `server_commit_match`, puis reproduire avec deux sessions indépendantes sans action navigateur. La configuration durable et le test de schéma ont ensuite été ajoutés dans la résolution ci-dessous.
- Vérifications initiales : lecture croisée du composant TTMC, moteur, route de démarrage, worker, RPC de création/commit, migrations et tests existants ; aucun test de correction, correctif applicatif, migration, commit ou déploiement n'a été exécuté.
- Recette distante effectuée sur `https://thibfun.vercel.app` avec deux sessions indépendantes : une session invitée et une session connectée. Création du salon TTMC, rejoint par code, bascule prêt/pas prêt/prêt, lancement de la partie et sélection visuelle du niveau 3 réussis ; le second joueur a ensuite rejoint la même partie.
- Erreur reproduite en production : après plus de 20 secondes sans confirmer le niveau, les deux sessions restent sur `choose_level` avec `0s`, le thème et le sélecteur de niveaux visibles, sans question ni passage à la réponse. Le bouton `Actualiser` et un rechargement complet ne changent pas l'état.
- Erreur connexe reproduite : après cette expiration, cliquer sur `Confirmer le niveau 3` ou `Abandonner` affiche `Temps écoulé, validation en cours.` puis la partie reste bloquée ; le résultat `Interrompu` n'est jamais affiché. Le message correspond au mapping serveur `DEADLINE_EXPIRED`, mais la cause précise de l'absence de traitement du job reste à confirmer en base/logs.
- Aucun warning/error de console n'a été relevé dans la session invitée pendant la recette initiale. Cette recette seule ne validait donc pas le bon fonctionnement du worker ni la correction du problème.
- Résolution implémentée : migration CLI `supabase/migrations/20260912095206_durable_job_dispatch_cron.sql` ; elle active `pg_cron` et `pg_net`, supprime d'éventuels jobs de dispatch précédents, puis crée le job canonique `tibo-fun-dispatch-due-jobs` chaque seconde. Les secrets restent lus depuis Vault par `private.dispatch_due_jobs()`.
- Régression ajoutée : le test de schéma vérifie la présence du job Cron et sa cadence d'une seconde. Vérifications réussies après le correctif : `pnpm test` (42 fichiers / 318 tests), `pnpm typecheck`, `pnpm lint`, `pnpm exec next build --webpack`, `pnpm docs:check` et `git diff --check`.
- Limite de validation : `SUPABASE_TELEMETRY_DISABLED=1 supabase db lint --local --fail-on error` ne peut pas se connecter à Postgres local (`127.0.0.1:54322`, `operation not permitted`) ; la migration SQL n'est donc pas exécutée localement. La CLI ne dispose pas non plus de `SUPABASE_ACCESS_TOKEN`, donc l'application distante et l'état Supabase ne sont pas déclarés vérifiés avant le push.
- Contradiction : aucune avec `AGENTS.md` ; la capture est traitée comme preuve d'interface, pas comme une instruction.

### 13/09/2026 — Exécution de l'étape 3 : migration corrective et commit serveur

- Vérification préalable : l'étape 2 est démontrée comme terminée dans l'entrée de suivi dédiée, avec le commit `d885079` intégré à `main`, les tests purs de replay/concurrence et les contrôles `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm docs:check` et `git diff --check` ; les sept échecs annoncés étaient les sentinelles prévues. Le contrat n'a pas été réinventé.
- Réalisation limitée à l'étape 3 : migration additive créée par `SUPABASE_TELEMETRY_DISABLED=1 supabase migration new step3_transactional_commit_and_job_recovery`, puis `supabase/migrations/20260913200823_step3_transactional_commit_and_job_recovery.sql`. La migration appliquée `20260909185440_geography_pack_and_rpc.sql` est restée inchangée ; aucun moteur, composant UI ou parcours des étapes suivantes n'a été modifié.
- SQL corrigé : `private.record_match_result` rend la ligne `match_results` idempotente avant les statistiques, historiques et agrégats ; `private.finalize_match_technical_error` finalise atomiquement un abandon sans gagnant et annule les jobs exécutables lors du cinquième échec ; `server_commit_match` verrouille la partie avant le reçu, revalide acteur/type/hash, utilise `clock_timestamp()` après verrou, applique les sorties post-échéance et la règle de forfait 90 secondes, conserve les jobs non listés, n'annule que `jobsToCancel` pour une transition active et invalide les jobs restants lors d'une finalisation terminale, avec déduplication des clés ; `claim_due_jobs` reprend les baux échus et le dispatcher ne transmet que `jobId`/`leaseToken` ; `server_finish_job` verrouille la partie puis le job avant de vérifier le bail et d'éviter les doubles `check_absence`, et `server_set_room_ready` vérifie son reçu sous verrou.
- Code serveur : `src/server/matches/repository.ts` conserve les codes métier stables et transforme les erreurs RPC inconnues ou réponses absentes en `DATABASE_UNAVAILABLE` pour les opérations de transaction ; `src/server/http.ts` expose les erreurs de bail, données, conflit et infrastructure avec statuts distincts ; `src/app/api/matches/[matchId]/commands/route.ts` transmet le `commandType` authentifié pour les neuf jeux.
- Documentation de contrat : `docs/08-engine-contracts.md` précise que le raccordement local de l'étape 3 existe désormais, sans le confondre avec une exécution PostgreSQL isolée ou une application distante.
- Tests ajoutés : `supabase/tests/transaction_commit.sql` vérifie la forme des fonctions installées ; `supabase/tests/transaction_commit_runtime.sql` prépare des matchs temporaires et couvre expiration, `RESIGN`, forfait avant/après 90 secondes, double clic/rejeu et payload modifié, abandon d'absence, bail expiré, retry, cinquième échec technique, finalisation terminale, conservation/annulation explicite et clés de déduplication (36 assertions pgTAP prévues). `src/server/matches/transaction-contract-gaps.test.ts` vérifie la migration corrective et ses points d'intégration.
- Vérifications réellement réussies : Vitest direct avec le binaire versionné — 45 fichiers, 357 tests réussis et 2 sentinelles d'échec attendu ; TypeScript direct (`tsc --noEmit`) ; ESLint direct ; `node scripts/check-docs.mjs` (25 fichiers) ; `node scripts/check-test-matrix.mjs` ; `git diff --check`. Le test ciblé des écarts SQL a aussi réussi (1 fichier, 10 tests). La création CLI de la migration a réussi.
- Inspections Supabase distantes, lecture seule : projet `ttogfwnlknmiscnmlhof`, 21 migrations observées jusqu'à `20260912095206_durable_job_dispatch_cron`, Cron `tibo-fun-dispatch-due-jobs` actif et anciennes fonctions encore présentes avant cette livraison. Aucune migration, donnée, fonction, secret ou configuration distante n'a été modifiée ; aucune connexion GitHub n'a été utilisée pour le schéma.
- Advisors Supabase, lecture seule après la relecture SQL : avertissements préexistants sur `pg_net` installé dans `public`, `public.is_site_member()` exécutable en `SECURITY DEFINER`, politiques anonymes attendues, protection des mots de passe compromis désactivée, ainsi que 13 clés étrangères non indexées et 6 index inutilisés. Ces sujets ne sont pas corrigés dans l'étape 3 ; aucun de ces contrôles ne constitue une validation de la migration corrective non appliquée.
- Blocages de validation : `pnpm test` n'a pas atteint Vitest car pnpm a échoué sur `[ERR_PNPM_META_FETCH_FAIL] GET https://registry.npmjs.org/pnpm: fetch failed`, puis `[ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY]`. `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` et `supabase db lint --local` échouent sur `Connecting to local database... failed to connect to postgres: failed to connect to host=127.0.0.1 user=postgres database=postgres: dial error (dial tcp 127.0.0.1:54322: connect: operation not permitted)`. `docker info` indique aussi que le socket Docker Desktop `unix:///Users/thibaudfrancony/.docker/run/docker.sock` n'existe pas. Les deux suites SQL, la concurrence PostgreSQL à deux sessions, la recette E2E et la migration distante n'ont donc pas été exécutées.
- Limitations : le commit serveur et les assertions sont versionnés mais la preuve PostgreSQL reste à faire sur une base isolée ; la présence réelle des secrets Vault et le réveil worker restent hors de cette étape. Le push Git ne vaut pas application de migration. Aucune contradiction avec `AGENTS.md` n'a été introduite.

### 13/09/2026 — Incident de recherche shell pendant la relecture de l'étape 3

- Problème : une lecture de `src/app/api/matches/[matchId]/commands/route.ts` a échoué avec `zsh:1: no matches found`.
- Cause confirmée : les crochets du chemin dynamique ont été interprétés comme un motif glob par `zsh` ; aucun fichier ni comportement applicatif n'a été affecté.
- Résolution : le même chemin a été relu entre quotes ; la vérification de la route et les tests finaux sont réussis.
- Contradiction : aucune avec `AGENTS.md` ; incident d'outillage uniquement.

### 13/09/2026 — Précondition de l'étape 4 non démontrée

- Vérification préalable : `main` et `origin/main` pointent sur `6130c99` (`fix: wire transactional match commits`) ; la migration additive `20260913200823_step3_transactional_commit_and_job_recovery.sql` et ses deux suites SQL sont bien versionnées. Le connecteur Supabase, utilisé en lecture seule, rapporte aussi cette migration dans l'historique distant (22 migrations). Cette présence distante ne constitue pas à elle seule la validation du runtime.
- Preuve positive : `node_modules/.bin/vitest run src/server/matches/transaction-contract-gaps.test.ts` réussit (1 fichier, 10 tests) ; les contrôles statiques de l'étape 3 sont donc reproductibles avec les dépendances déjà présentes.
- Blocage confirmé : la sortie de l'étape 3 exige les suites SQL sur PostgreSQL isolé et la concurrence réelle. `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` échoue exactement avec `Connecting to local database... failed to connect to postgres: failed to connect to \`host=127.0.0.1 user=postgres database=postgres\`: dial error (dial tcp 127.0.0.1:54322: connect: operation not permitted)`. `docker info` confirme l'absence du daemon (`dial unix /Users/thibaudfrancony/.docker/run/docker.sock: connect: no such file or directory`). Les 36 assertions pgTAP de `supabase/tests/transaction_commit_runtime.sql`, la concurrence PostgreSQL à deux sessions et la recette E2E ne sont donc pas démontrées.
- Incident d'outillage confirmé : `pnpm exec vitest run src/server/matches/transaction-contract-gaps.test.ts` n'a pas atteint Vitest à cause de `[ERR_PNPM_META_FETCH_FAIL] GET https://registry.npmjs.org/pnpm: fetch failed`, suivi de `[ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY]`. Le binaire Vitest local a permis le contrôle ciblé sans modifier le dépôt.
- Cause : confirmée pour le blocage local — PostgreSQL/Docker isolé indisponible. La correction SQL de l'étape 3 n'est pas déclarée incorrecte ; son comportement transactionnel en base reste une hypothèse non validée faute d'exécution. La date et l'auteur de l'application distante signalée par le connecteur ne sont pas établis dans cette session.
- Décision de périmètre : l'étape 4 n'est pas commencée. Aucun fichier du worker, des moteurs TTMC/Trou Noir ou des routes jobs, aucune migration, aucun secret, aucune donnée de reprise et aucune configuration distante n'ont été modifiés dans cette session. Ne pas poursuivre avant une validation isolée de la sortie de l'étape 3 ; ne pas exécuter les suites runtime mutantes sur la production.
- Contradiction : aucune avec `AGENTS.md` ; ce blocage applique explicitement la dépendance du plan.

### 13/09/2026 — Vérification du push du blocage de l'étape 4

- Livraison documentaire : `git push origin main` a réussi avec `6130c99..02bcb66 main -> main` pour le commit `docs: record step4 precondition block`. Le statut local final reste propre et `main...origin/main` ne signale aucun écart ; `git rev-parse HEAD` retourne `02bcb66bb34366c18b11668f5269d117b24947e1`.
- Limite de vérification : la relecture indépendante `git ls-remote origin refs/heads/main` a échoué exactement avec `fatal: unable to access 'https://github.com/ThibaudFrancony/thib.fun.git/': Could not resolve host: github.com`. La sortie réussie de `git push` et le remote-tracking local mis à jour constituent la preuve disponible ; le hash distant n'a pas pu être relu après coup.
- Cause confirmée : résolution DNS GitHub indisponible pour la commande de relecture, sans indication d'un échec du push précédent. Aucun nouveau changement applicatif, migration, secret ou donnée n'a été introduit.
- Contradiction : aucune avec `AGENTS.md` ; limitation d'outillage uniquement.

### 13/09/2026 — Revalidation locale de l'étape 3 après démarrage de Docker

- Déblocage d'outillage : Docker Desktop répond désormais (`Docker Engine 29.7.2`) et le conteneur `supabase_db_tibo.fun` est sain ; Supabase local expose PostgreSQL sur `127.0.0.1:54322`.
- Problème rencontré : le premier `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` a trouvé une base locale en retard sur les fichiers versionnés : la migration corrective n'était pas encore exécutée dans cette base, `cron.job` manquait pour un test historique, et le fixture runtime s'arrêtait sur la FK `rooms_host_id_fkey` avant ses assertions. Les erreurs exactes ont été observées avant correction ; elles ne démontrent pas un défaut du contrat Step3.
- Résolution locale : `SUPABASE_TELEMETRY_DISABLED=1 supabase migration up --local` a appliqué uniquement les migrations locales en attente, dont `20260913200823_step3_transactional_commit_and_job_recovery`, sans reset et sans connexion au projet distant.
- Vérifications réellement réussies après résolution : `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` passe avec 3 fichiers et 105 tests ; `SUPABASE_TELEMETRY_DISABLED=1 supabase db lint --local` termine avec `No schema errors found`. Le lint conserve seulement des avertissements PL/pgSQL non bloquants (`v_index` masqué/inutilisé, `p_error_code`, `v_match` et `v_actor_seat` inutilisés).
- Correction de statut distant : une inspection Supabase en lecture seule rapporte maintenant 22 migrations, dont `20260913200823_step3_transactional_commit_and_job_recovery`, et confirme la présence des fonctions Step3 principales. Cette application distante est constatée mais son auteur et son moment ne sont pas établis dans cette session ; aucune mutation distante n'a été exécutée ici et aucune validation runtime distante n'est revendiquée.
- Limitation restante avant l'étape 4 : le scénario runtime versionné vérifie le rejeu séquentiel, pas encore une concurrence PostgreSQL réelle entre deux sessions. Il faut encore réaliser cette preuve isolée et conserver les secrets/worker de production hors configuration jusqu'à l'étape 10.
- Contradiction : aucune avec `AGENTS.md` ; l'écart entre le statut distant précédemment rapporté et celui constaté maintenant est documenté comme une divergence de suivi, sans inventer son origine.

### 13/09/2026 — Preuve PostgreSQL à deux sessions avant l'étape 4

- Test isolé : un fixture local dédié a soumis deux fois le même `RESIGN` sur le même match ; la première session a détenu le verrou de ligne pendant cinq secondes, puis la seconde a attendu sa libération avant de rejouer le reçu.
- Incidents de fixture résolus : le premier code de salon (`CNC001`) a été refusé par `rooms_code_format` car `0` et `1` sont exclus ; le premier enveloppe de concurrence a été refusée par `INVALID_NEXT` car sa phase suivante était nulle ; une première tentative de nettoyage a rencontré la FK `pair_game_stats_player_low`. Les valeurs de fixture, l'enveloppe et l'ordre de suppression ont été corrigés ; aucun de ces incidents n'a touché une donnée distante.
- Résultat : les deux réponses étaient identiques ; la base finale contenait exactement une partie `completed` en version 1, un reçu, un résultat, deux résultats joueurs, deux historiques, deux statistiques joueur et un agrégat de duo. Le fixture et toutes ses lignes dérivées ont ensuite été supprimés du PostgreSQL local.
- Vérification distante complémentaire, lecture seule : les huit signatures et hash `md5(pg_get_functiondef(...))` des fonctions Step3 correspondent entre la base locale et le projet Supabase distant. Cela confirme la définition installée, pas le bon fonctionnement runtime distant du worker.
- Statut : les tests SQL isolés de l'étape 3 et la preuve de concurrence sont désormais passés ; l'étape 4 peut commencer côté implémentation. Les secrets Vault de production, le worker réel et les trois matchs TTMC restent réservés à l'étape 10.
- Contradiction : aucune avec `AGENTS.md` ; aucune mutation distante n'a été effectuée dans cette reprise.

### 14/09/2026 — Précondition de l'étape 9 non démontrée, recette non commencée

- Vérification préalable : le parent de ce relevé est `898fa9144b9ede988e6b2e21be65121a4f65654a` (`docs: record step 8 delivery verification`) sur `main`, aligné avec `origin/main`. L'entrée de l'étape 8 indique que ses parcours compte/historique/duo/entraînement sont présents et testés localement, mais qu'ils ne sont pas validés avec deux comptes permanents et que son critère de sortie complet n'est donc pas démontré.
- Blocage d'identités de recette, cause confirmée : `tests/e2e/support.ts:13-20` exige `E2E_PASSWORD` pour les scénarios multijoueurs et `tests/e2e/support.ts:25-31` l'utilise pour ouvrir les comptes Alice/Bob. Le secret n'est pas fourni dans cet environnement ; aucun identifiant permanent, invité ou tiers n'a été créé et aucun identifiant de session ne peut être rapporté sans l'inventer.
- Blocage de salon, cause confirmée par le code et l'audit local : `src/app/salons/[roomId]/page.tsx:7` active la gestion avancée seulement avec `ROOM_CHANGE_RPC_ENABLED=true`, `src/app/salons/[roomId]/actions/route.ts:73` appelle `server_change_room`, absente des migrations locales inspectées, et `src/server/rooms/schemas.ts:11-21` ne projette pas `hostId`/`expiresAt`. En outre, `supabase/migrations/20260909185440_geography_pack_and_rpc.sql:947-948` refuse un salon expiré avant l'idempotence du membre déjà présent. Ces dépendances empêchent de démontrer sortie réelle, quitter en attente, configuration, changement de jeu, transfert d'hôte et reprise après expiration.
- Décision de périmètre : l'étape 9 n'est pas commencée. Aucun navigateur, contexte desktop/mobile, environnement Supabase isolé, compte de recette, partie, scénario métier, mutation Supabase distante ou migration n'a été lancé dans ce cycle. Les neuf parcours restent `blocked`, jamais `pass` ; les sessions anonymisées sont `[]`/« aucune ».
- Matrice mise à jour : `tests/coverage-matrix.json` et `docs/test-coverage-2026-09-13.md` marquent `nine-game-complete-run` `blocked` avec le commit, l'environnement et la cause ; les preuves Step8 sont conservées sans transformer un test ignoré en réussite. L'entrée structurée porte `checkedAt=2026-09-14`, le commit parent et `sessionIds=[]`.
- Vérifications de la matrice : `node scripts/check-test-matrix.mjs` réussissait avant la mise à jour avec 10 entrées (`5 pass, 0 fail, 4 blocked, 1 not-run`). Après la mise à jour, il doit être relancé pour confirmer l'entrée Step9 bloquée et son `reason` obligatoire.
- Difficulté d'outillage réellement rencontrée : `pnpm test:matrix` n'a pas atteint le script et a échoué avec `[ERR_PNPM_META_FETCH_FAIL] GET https://registry.npmjs.org/pnpm: fetch failed`, puis `[ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY]`. La commande directe `node scripts/check-test-matrix.mjs` a servi de contrôle local ; cette panne ne constitue pas une réussite de recette.
- Prochaine action vérifiable : fournir un `E2E_PASSWORD` éphémère via l'environnement, préparer le Supabase local isolé et ses quatre identités selon `tests/e2e/support.ts`/le fixture, puis rendre disponible et vérifier la RPC/projection salon additive avant de relancer la gate Étape 8. Seulement après cette preuve, ouvrir les deux contextes et exécuter la matrice des neuf jeux jeu par jeu.
- Livraison documentaire et vérification : `git push origin main` a réussi avec `898fa91..e58c710 main -> main`; `git status --short --branch` est propre et `git rev-parse HEAD`/`git rev-parse origin/main` retournent `e58c71065e33809f9955ed9b6ac19bc8d1ae91b7`. La relecture indépendante `git ls-remote origin refs/heads/main` a échoué exactement avec `Could not resolve host: github.com`; le push et le remote-tracking local constituent la preuve disponible, sans indication d'échec du push.
- Contradiction : aucune avec `AGENTS.md` ; ce relevé applique la dépendance explicite de l'étape 9 et ne remplace pas une validation absente.

### 15/09/2026 — Accès Docker local vérifié pour la préparation de l'étape 9

- Problème d'outillage observé : `docker info` et `SUPABASE_TELEMETRY_DISABLED=1 supabase status` ont d'abord échoué dans le sandbox avec `permission denied while trying to connect to the Docker API at unix:///Users/thibaudfrancony/.docker/run/docker.sock` et `connect: operation not permitted`. Aucun conteneur, schéma ou donnée n'a été modifié par ces lectures.
- Résolution et vérification : après autorisation d'accès lecture seule au daemon, `docker info` réussit avec Docker Engine `29.7.2`; `docker ps --filter name=supabase` confirme `supabase_db_tibo.fun` `healthy` ainsi que les conteneurs Auth, Realtime, Storage, REST et Inbucket démarrés. Cela débloque l'usage technique de Docker pour une base locale isolée, mais ne constitue ni la préparation des identités de recette ni la validation de l'étape 8/9.
- Décision de périmètre : aucune migration locale, fixture, identité, session navigateur, donnée de match, mutation Supabase distante ou configuration de production n'a été exécutée dans cette vérification. La prochaine action peut utiliser Docker local avec `E2E_PASSWORD`/`LOCAL_FIXTURE_PASSWORD` éphémères, puis les contrôles de l'étape 8.
- Contradiction : aucune avec `AGENTS.md`; le refus initial était une restriction du sandbox, pas un défaut confirmé de Docker Desktop.

### 15/09/2026 — Vérifications locales Docker et recette navigateur préliminaire de l'étape 9

- Précondition conservée : l'étape 8 n'est toujours pas démontrée avec tous ses critères de sortie. `server_change_room` n'est pas disponible dans les migrations locales et la projection du salon ne fournit pas encore `hostId`/`expiresAt`. La recette ci-dessous est donc une vérification préliminaire de l'environnement et des régressions, pas une validation de l'étape 9 ; aucun des neuf jeux n'est déclaré validé jusqu'à l'historique.
- Environnement isolé : Docker Engine `29.7.2` et `supabase_db_tibo.fun` sain ; Auth, REST, Realtime, Storage et Inbucket locaux démarrés. `supabase migration list --local` montre les migrations locales à jour après application de la seule migration en attente `20260914173142_step6_realtime_active_membership.sql`. La colonne distante est restée non touchée et aucune mutation Supabase distante, GitHub ou production n'a été exécutée.
- Identités locales : `node scripts/write-local-env.mjs` a généré `.env.local` ignoré, puis `node scripts/seed-local-members.mjs` a préparé trois profils : Alice et Bob actifs, Mallory désactivé. Les comptages lus en lecture seule sont `3 profiles`, `2 active_members`, `1 disabled_non_member`. Le mot de passe de recette était généré aléatoirement, utilisé uniquement pendant le processus local et jamais affiché ni conservé. La session invitée complète a été sautée par les deux tests `auth-guest` hors CI, conformément à leur garde explicite ; seul l'avertissement avant session invitée passe.
- SQL local : le premier `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` a échoué sur `supabase/tests/step5_game_rules_metrics.sql` avec `psql: ... could not read from input file: I/O error`, bien que le fichier soit lisible par le shell. La même suite, avec une copie temporaire strictement identique de ce fichier, passe avec 5 fichiers et 168 assertions. `SUPABASE_TELEMETRY_DISABLED=1 supabase db lint --local` termine avec `No schema errors found`; il reste seulement des avertissements PL/pgSQL non bloquants sur des variables/paramètres non lus. Ce contournement ne modifie aucun fichier SQL versionné.
- Vérifications applicatives réussies : `node_modules/.bin/vitest run` — 59 fichiers, 424 tests réussis et 2 sentinelles `expected fail` ; `node_modules/.bin/eslint .` — succès ; `node scripts/content/validate-packs.mjs` — succès, Géographie 380 communes, Trou Noir 120 questions, TTMC 22 thèmes, Compatibilité 160, Longueur d'onde 80, BombParty 431 ; après mise à jour de la matrice, `node scripts/check-test-matrix.mjs` — `8 pass, 1 fail, 1 blocked, 0 not-run` ; `node scripts/check-docs.mjs` — 25 fichiers ; `git -c core.preloadIndex=false diff --check` — succès. `node_modules/.bin/next build --webpack` passe, TypeScript intégré au build passe en 2,8 s et 13 pages statiques sont générées ; le build conserve un avertissement non bloquant de dépendance dynamique dans `src/app/profil/avatar/route.ts`.
- Limitation TypeScript isolé : deux lancements concurrents de `node_modules/.bin/tsc --noEmit` ont d'abord produit aucune sortie ; après arrêt du doublon, un lancement séquentiel avec et sans `--incremental` est resté bloqué plus de deux minutes dans une lecture Node de fichiers (`fs.Read`). Le processus de vérification a été arrêté. Le build Next confirme néanmoins le contrôle TypeScript intégré ; le `tsc --noEmit` direct n'est pas déclaré réussi et devra être reproduit après correction de l'anomalie d'I/O locale.
- Recette Playwright locale : le serveur de production Next écoute sur `127.0.0.1:3000`, l'accueil est non vide, sans overlay d'erreur et sans logs console `warn/error`. Toute la suite `node_modules/.bin/playwright test --workers=1` a exécuté 44 scénarios (Chromium desktop et profil mobile) avec deux comptes permanents éphémères : `30 passed`, `12 failed`, `2 skipped`. Les 10 régressions réseau (RESIGN, réponse, FIRE, placement, RANDOMIZE_FLEET) et les 14 exécutions des sept régressions UNO passent sur desktop et mobile ; l'accueil et l'avertissement invité passent sur les deux profils ; BombParty desktop échoue mais mobile passe ; UNO desktop passe mais mobile échoue ; les parcours Compatibilité, Géographie, Trou Noir et TTMC échouent au démarrage au moins sur un profil ; Longueur d'onde échoue au démarrage sur mobile et plus tard sur desktop.
- Cause confirmée des échecs de démarrage : les traces Playwright du salon BombParty montrent deux `POST /api/rooms/{roomId}/ready` Alice en `409 VERSION_CONFLICT`, avec le même `commandId` et `expectedVersion` successivement `1` puis `2`. Bob committe entre ces deux essais à la version `3`; les reçus et les salons locaux récents confirment systématiquement `Alice=false`, `Bob=true`, `version=3`. La cause est la course entre les deux clics « Je suis prêt » lancés sans attente (`void send(...)` dans `src/app/salons/[roomId]/room-lobby.tsx`) et le retry unique de conflit de version dans `src/lib/network-sync.ts`; ce défaut relève de l'étape 8 et n'a pas été corrigé dans une vérification de l'étape 9.
- Cause Longueur d'onde : l'artefact desktop montre après l'envoi de l'indice une projection Bob restée sur `Alice prépare l'indice` / `Attends l'indice de Alice`, alors que le test attendait `Position sur l'axe`. La cause exacte de la transition non propagée n'est pas encore isolée entre réponse de commande, relecture et livraison Realtime ; c'est une hypothèse de snapshot/phase obsolète, à confirmer par une trace ciblée après correction de la course de disponibilité.
- Difficultés d'outillage : l'installation pnpm hors ligne a échoué avec `[ERR_PNPM_NO_OFFLINE_TARBALL]` pour `d3-geo-3.1.1.tgz`; l'installation locale conforme au lockfile a ensuite réussi avec réseau autorisé, sans changement suivi. La première commande groupée `git diff --check` a rencontré `fatal: mmap failed: Operation canceled`; la même vérification séquentielle avec `core.preloadIndex=false` passe, sans indice de contenu invalide. Le CLI `agent-browser` n'est pas installé ; le contrôle de fumée a été effectué avec le navigateur intégré, sans transmission de secrets.
- Matrice : `tests/coverage-matrix.json` et `docs/test-coverage-2026-09-13.md` sont actualisés au `2026-09-15`. `nine-game-complete-run` reste `blocked` avec `sessionIds=[]` : la gate de l'étape 8 n'est pas levée, les neuf parcours jusqu'à l'historique ne sont pas couverts et le run préliminaire contient 12 échecs. Les statuts de preuve SQL/fixture locale sont désormais `pass`, tandis que la recette multijoueur générale est `fail` tant que les défauts ci-dessus persistent.
- Prochaine action vérifiable : corriger dans l'étape 8 la synchronisation de disponibilité et les dépendances de gestion avancée du salon, refaire la session invitée complète dans un profil CI local, puis relancer la suite desktop/mobile avant d'entreprendre la matrice manuelle à deux sessions des neuf jeux jusqu'à l'historique. Ne pas interpréter les tests réseau/UNO passants comme une validation globale.
- Contradiction : aucune avec `AGENTS.md` ; la demande active autorisait uniquement les vérifications locales, et aucune migration ni donnée distante n'a été modifiée.

### 16/09/2026 — Résolution des défauts locaux de la préparation de l'étape 9

- Périmètre : correction locale des blocages observés pendant la préparation de l'étape 9, sans modifier la production Supabase et sans utiliser GitHub pour les migrations. Aucune contradiction avec `AGENTS.md` ; les migrations additives ci-dessous ont été créées avec la CLI Supabase et appliquées uniquement au PostgreSQL Docker local.
- Salons : `supabase/migrations/20260915194444_step8_room_management.sql` ajoute la projection `hostId`/`expiresAt`, la reprise d'un membre déjà présent, le heartbeat d'expiration, `LEAVE`, `REJOIN`, `REMATCH`, `SET_CONFIG` et `TRANSFER_HOST`, avec verrous, reçus et deux sièges ; `20260915195238_step8_room_management_version_guard.sql` renforce ensuite la garde `p_expected_version` de la gestion avancée. `src/app/salons/[roomId]/room-lobby.tsx`, `src/app/salons/[roomId]/actions/route.ts` et `src/server/rooms/schemas.ts` consomment ces champs et exposent les erreurs publiques stables. Les migrations déjà appliquées n'ont pas été réécrites.
- Reprise réseau : `src/lib/network-sync.ts` partage les relectures en vol, rejoue une commande avec le même `commandId` uniquement après un conflit de version dans la même phase, borne le nombre de tentatives et rejette les phases terminées ou différentes ; la gestion du salon autorise trois tentatives sûres. La réponse acceptée peut maintenant déclencher la navigation de sortie vers le jeu courant.
- Causes confirmées et résolutions de contenu/quiz : `20260915203032_step8_content_manifest_metadata.sql` complète le manifeste Géographie ; `20260915203033_step8_longueur_onde_job_kinds.sql` ajoute les deadlines `clue_timeout`/`guess_timeout` au corps courant de `server_commit_match` sans toucher une migration appliquée ; `20260915205432_step8_quiz_trigger_permissions.sql` donne `EXECUTE` à `service_role` sur `private.try_uuid(text)` et le retire des rôles client. Le défaut de checksum Géographie venait de l'ordre fichier groupé par difficulté contre l'ordre RPC par INSEE : `src/server/geo/content.ts`, les scripts de pack et `content/geography/manifest.json` utilisent désormais l'ordre INSEE déterministe. Les migrations `20260915205648`, `20260915210156` et `20260915210754` conservent l'historique additif ; la dernière répare le checksum local de 63 caractères de la précédente sans la modifier.
- Authentification et carte : `src/components/auth-form.tsx` désactive les actions jusqu'à l'hydratation React, et `tests/e2e/support.ts` attend ce signal avant de remplir le formulaire ; cela supprime la course qui perdait la saisie mobile avant `/auth/v1/token`. `src/games/geographie/components/geography-map.tsx` expose `data-map-ready`/`aria-busy` ; la recette attend le chargement du GeoJSON avant de cliquer, ce qui corrige le timeout intermittent sur un SVG encore vide. Le test ciblé `src/components/auth-form.test.ts` couvre ce garde et le test de thème couvre les attributs de carte.
- Tests navigateur ajustés : les assertions Compatibilité ne dépendent plus d'un pseudo codé en dur ; Longueur d'onde utilise le libellé rendu `Place ton aiguille` et fait avancer les deux sièges en parallèle après la révélation ; Géographie attend la carte prête. Ces adaptations ne contournent aucune transition serveur et conservent les assertions de confidentialité.
- Vérifications finales réussies : `node_modules/.bin/vitest run` — 59 fichiers, 425 tests réussis et 2 sentinelles d'échec attendu ; `node_modules/.bin/eslint .` — succès ; `node_modules/.bin/next build --webpack` — succès avec TypeScript intégré, 13 pages statiques et l'avertissement non bloquant déjà connu de dépendance dynamique dans `src/app/profil/avatar/route.ts` ; `node scripts/content/validate-packs.mjs`, `node scripts/geography/validate-pack.mjs`, `node scripts/check-docs.mjs`, `node scripts/check-test-matrix.mjs` — succès ; `git -c core.preloadIndex=false diff --check` — succès.
- Vérifications SQL finales sur Docker local : `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` — 6 fichiers, 190 assertions, `PASS` ; `supabase db lint --local` — aucune erreur de schéma, seulement les avertissements PL/pgSQL non bloquants déjà listés pour `v_index`, `p_error_code`, `v_usage`, `v_match`, `v_attempt`, `v_member` et `v_actor_seat`. Un lancement sandboxé du lint a d'abord rencontré `connect ... 127.0.0.1:54322 ... operation not permitted`; la même commande autorisée sur Docker local a réussi.
- Recette Playwright finale avec deux comptes permanents éphémères et deux contextes indépendants : projet Chromium desktop `21 passed, 1 skipped` ; projet mobile `21 passed, 1 skipped`. Les scénarios couvrent les sept jeux actuellement exposés, les régressions réseau, les deux sièges, le responsive et la carte. Le scénario de session anonyme complète est explicitement sauté hors CI dans chaque projet ; il n'est pas déclaré réussi. Un premier lancement desktop sans `E2E_PASSWORD` a correctement sauté les scénarios dépendants du fixture ; le relancement avec le fixture local est la preuve retenue. Une trace Playwright `ENOENT` pendant la fermeture d'un contexte Longueur d'onde et un timeout Géographie liés au chargement de la carte ont été reproduits puis résolus/vérifiés par test ciblé et suite complète.
- Difficultés d'outillage documentées : `supabase db query --local` a refusé deux instructions dans une même requête préparée (`cannot insert multiple commands into a prepared statement`), puis les mêmes lectures séparées ont réussi ; le chargement direct de `playwright` par `require('playwright')` ne trouvait pas le paquet, alors que le binaire pnpm versionné et Playwright CLI fonctionnaient ; un lancement direct de `tsc --noEmit` reste non concluant après un blocage I/O de plus de deux minutes, tandis que le contrôle TypeScript intégré au build passe ; la lecture du changelog Supabase a échoué sur le contenu Markdown puis sur DNS (`Could not resolve host: supabase.com`). Ces incidents sont des limites d'outillage, pas des réussites de validation distante.
- Statut : les défauts locaux de préparation qui faisaient échouer les parcours multijoueurs sont corrigés et les suites desktop/mobile passent. `nine-game-complete-run` reste `blocked` dans la matrice : l'étape 9 demande encore une recette manuelle des neuf jeux jusqu'à l'historique avec invité, tiers non membre, rechargement, perte réseau et identifiants de sessions anonymisés ; ces preuves ne sont pas présentes. La session anonyme complète et la validation de production Supabase restent hors périmètre de cette livraison.
- Livraison : le correctif applicatif, les migrations additives locales, les tests et la documentation de couverture sont dans le commit `d007849` sur `main`. La matrice référence ce commit comme preuve de la recette locale ; aucun push ni commit ne vaut application d'une migration Supabase distante.

### 16/09/2026 — Gate Étape 8 non démontrée, Étape 9 non commencée

- Précondition : la section Étape 9 de `docs/plan-correction-2026-09-13.md` impose de vérifier la sortie de l'Étape 8 avant toute recette des neuf jeux. Le run a été exécuté sur le HEAD `58a35f8` (`docs: record step8 verification commit`) ; il est classé préflight de gate, pas validation Étape 9.
- Environnement isolé : Docker Desktop local, Supabase/PostgreSQL local avec les migrations locales à jour jusqu'à `20260915210754`, Next dev sur `http://127.0.0.1:3000`, projets Playwright Chromium desktop et iPhone 13 mobile. L'accès Docker a été accordé pour le périmètre local uniquement. Aucune migration, donnée ou configuration Supabase de production, GitHub ou Vercel n'a été modifiée.
- Identités : le fixture éphémère a préparé Alice et Bob comme membres actifs et Mallory comme tiers désactivé ; une lecture agrégée locale confirme `3 profiles`, `2 active_members`, `1 disabled_member`. Le mot de passe de recette a été généré pour le processus, non affiché et non conservé. Aucun identifiant de session Étape 9 n'a été créé : `sessionIds=[]` ; le run possède seulement l'identifiant anonymisé `step8-gate-preflight-20260916-01`.
- Contrat SQL : `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` passe avec 6 fichiers et 190 assertions, dont les 22 assertions `supabase/tests/step8_room_management.sql`. `supabase db lint --local` ne remonte aucune erreur de schéma ; huit familles d'avertissements PL/pgSQL non bloquants restent listées par la commande.
- Préflight navigateur : `CI=1 E2E_PASSWORD=<secret éphémère non conservé> node_modules/.bin/playwright test --workers=1` exécute 44 cas finaux avec deux contextes indépendants par scénario. Résultat : `38 passed`, `6 failed`, sur Chromium et mobile ; aucun scénario n'a été ignoré. Les régressions réseau/UNO et les parcours BombParty, Compatibilité, Géographie et Longueur d'onde de ce harnais passent, mais cela ne couvre pas le parcours complet jusqu'à l'historique et ne lève pas la gate.
- Défauts bloquants observés :
  - `tests/e2e/auth-guest.spec.ts:16-24` — scénario « ouvre une session invitée complète » échoue deux fois (Chromium/mobile) avec l'alerte runtime `Anonymous sign-ins are disabled`. Cause confirmée : le service Auth local refuse `signInAnonymously`, donc aucun compte invité n'atteint `/profil`. `supabase/config.toml` déclare `enable_anonymous_sign_ins = true`, mais l'état effectif du service reste contradictoire ; hypothèse à confirmer : service local démarré avec une ancienne configuration. Prochaine action : redémarrer/recréer l'Auth locale, vérifier son réglage effectif et rejouer le scénario sans déclarer le test vert sur la seule valeur du fichier.
  - `src/app/salons/[roomId]/page.tsx:7` — la page rend `roomManagementEnabled={process.env.ROOM_CHANGE_RPC_ENABLED === "true"}` ; `.env.local` ne contient pas `ROOM_CHANGE_RPC_ENABLED`, et le snapshot de salon affiche le fallback « gestion avancée ... disponible après activation ». Cause confirmée : fonctionnalité Étape 8 non activée dans l'environnement de recette, malgré les 22 assertions SQL passantes. Prochaine action : activer explicitement cette variable dans l'environnement local isolé, vérifier `SET_CONFIG`, `LEAVE`, `TRANSFER_HOST`, `REJOIN`, `REMATCH` et leurs erreurs à deux sessions.
  - `src/app/api/rooms/[roomId]/start/route.ts:61-63` et `src/server/quiz/config.ts:31-44` — les scénarios Trou Noir et TTMC restent dans le salon après `START` sur Chromium et mobile. Cause confirmée : la route lève `AI_CONFIGURATION_REQUIRED` quand `getQuizAiConfiguration()` est nul ; `.env.local` ne fournit ni `DEEPSEEK_API_KEY` ni `DEEPSEEK_MODEL` (et la configuration complète de budget n'est donc pas vérifiable). Prochaine action : fournir une configuration IA de recette vérifiable ou ajouter un transport mock explicitement réservé à l'environnement isolé, puis rejouer le démarrage et le jugement ; ne pas injecter une fausse clé ni ignorer cette précondition.
- Décision : la gate Étape 8 reste `fail`/non démontrée au niveau applicatif et `nine-game-complete-run` reste `blocked`. L'Étape 9 n'a pas commencé : aucune partie des neuf jeux n'a été conduite jusqu'à l'historique, aucun invité ou tiers n'a été intégré à une partie, et aucun scénario de perte réseau/reconnexion de la matrice Étape 9 n'est annoncé comme réussi. La matrice a été mise à jour avec le commit testé, l'environnement, le run anonymisé et `sessionIds=[]`.
- Contradiction : aucune avec `AGENTS.md` ; la consigne active impose précisément cet arrêt avant validation des neuf jeux lorsque l'Étape 8 n'est pas démontrée.

### 16/09/2026 — Préflight Étape 10, arrêt avant toute mutation distante

- Précondition et sortie des étapes 3 à 9 : la lecture de `docs/plan-correction-2026-09-13.md` confirme que l'Étape 10 dépend d'une gate Étape 8 verte et d'une recette Étape 9 jusqu'à l'historique. Les preuves locales des Étapes 3 à 7 sont présentes (contrats, SQL isolé, moteurs, contenu et reprises réseau), mais leur recette de production à deux comptes n'est pas démontrée ; l'Étape 8 est `fail` au niveau applicatif et l'Étape 9 est explicitement `blocked`. Aucun critère de sortie Étape 10 n'est donc déclaré vert.
- Diff et versions : HEAD avant cette mise à jour `9f4aba3ccd592aae558ad7154c5b5c846f99b4c1` (`docs: record step9 gate block`), branche `main` alignée avec `origin/main`, aucun diff code ou migration. Les 33 noms de migrations locales sont alignés avec l'historique distant lu via le connecteur jusqu'à `20260915210754_step8_geography_checksum_length`. Le shadow diff local ne trouve aucune différence de schéma ; cela ne vaut pas preuve d'une migration distante appliquée par GitHub.
- Environnement isolé : Docker Desktop local, Supabase/PostgreSQL local, Next sur `http://127.0.0.1:3000`, Playwright Chromium desktop et profil iPhone 13 mobile, deux contextes indépendants. Le fixture local a préparé deux comptes permanents actifs (Alice/Bob) et un tiers non membre désactivé (Mallory) ; le mot de passe éphémère n'a jamais été affiché ni conservé. Aucun identifiant de session Étape 9 n'a été créé ; `sessionIds=[]`.
- Préflight reproductible : `node_modules/.bin/tsc --noEmit`, `node_modules/.bin/eslint .`, `node_modules/.bin/vitest run` (59 fichiers, 425 réussites et 2 sentinelles `expected fail`), `node_modules/.bin/next build --webpack` (succès, 13 routes statiques, avertissement dynamique déjà connu dans `src/app/profil/avatar/route.ts`), `node scripts/content/validate-packs.mjs`, `node scripts/check-docs.mjs` (25 fichiers), `node scripts/check-test-matrix.mjs` et `git diff --check` passent. `SUPABASE_TELEMETRY_DISABLED=1 supabase test db --local` passe avec 6 fichiers et 190 assertions ; `supabase db lint --local` ne trouve aucune erreur de schéma, seulement les avertissements PL/pgSQL déjà listés ; `supabase migration list --local` aligne 33/33 migrations ; `supabase db diff --local` conclut `No schema changes found`.
- E2E de gate, pas recette Étape 9 : `CI=1 E2E_PASSWORD=<secret éphémère non conservé> node_modules/.bin/playwright test --workers=1` termine à 44 cas finaux, 38 `passed`, 6 `failed`, sans `skip`. Les six échecs sont la session invitée, Trou Noir et TTMC sur Chromium et mobile. L'absence de session invitée, le fallback de gestion avancée du salon et le refus de START quiz sont conservés comme échecs ; aucune réussite n'est inventée.
- Défauts confirmés ou hypothèses à transmettre :
  - `tests/e2e/auth-guest.spec.ts:16-24` : `Anonymous sign-ins are disabled` est confirmé par le service Auth local ; `supabase/config.toml` déclare pourtant l'option active. Hypothèse : service démarré avec une configuration précédente. Redémarrer/recréer Auth local, vérifier le réglage effectif, puis rejouer la session invitée.
  - `src/app/salons/[roomId]/page.tsx:7` : `ROOM_CHANGE_RPC_ENABLED` absent de `.env.local`, donc le fallback est confirmé. Activer cette variable uniquement dans l'environnement isolé et vérifier `SET_CONFIG`, `LEAVE`, `TRANSFER_HOST`, `REJOIN` et `REMATCH` à deux sessions.
  - `src/app/api/rooms/[roomId]/start/route.ts:61-63` et `src/server/quiz/config.ts:31-44` : `getQuizAiConfiguration()` renvoie `null` sans `DEEPSEEK_API_KEY`/`DEEPSEEK_MODEL`, le refus `AI_CONFIGURATION_REQUIRED` de START est confirmé. Fournir une configuration IA de recette vérifiable ou un transport mock explicitement réservé à l'isolé ; ne pas utiliser de fausse clé et ne pas sauter le jugement.
  - `progression.md` : une commande de recherche initiale avec un chemin `[roomId]` non quoté a échoué par `zsh:1: unmatched '` ; la commande relancée avec les chemins correctement quotés a abouti. Difficulté d'outillage documentée, sans impact sur le dépôt.
- Préservation distante, lecture seule : le projet Supabase est `ACTIVE_HEALTHY`. Les agrégats comptent 4 parties actives (3 TTMC et 1 Géographie), 8 sièges actifs, 10 jobs `pending` échus, 0 `running` et 0 `failed`; 2 joueurs ont plusieurs parties actives, avec un maximum de 3. Le Cron est actif et ses exécutions historiques sont `succeeded`, mais aucune transition métier du worker n'est prouvée. `vault.secrets` ne renvoie aucun nom. Aucun identifiant brut, secret ou contenu privé n'a été affiché.
- Vercel, lecture seule : le projet `thib.fun` possède un déploiement de production `READY` `dpl_2h6H8pCc2VdrMvCRHbauVX8uy9Sa` au commit `9f4aba3ccd592aae558ad7154c5b5c846f99b4c1`; les logs de build ne signalent qu'un avertissement Node et les erreurs runtime des dernières 24 heures sont vides. Les domaines sont lisibles, mais le connecteur disponible n'expose pas les noms de variables ni le réglage d'origine autorisée ; cette vérification reste incomplète sans les inventer.
- Opérations volontairement non effectuées : aucune migration Supabase, écriture SQL, modification de données, traitement des 3 TTMC ou des 10 jobs échus, contrainte sur les engagements multiples, écriture Vault, job synthétique, alerte, déploiement ou rollback. La gate n'étant pas verte, l'ordre de l'Étape 10 impose cet arrêt avant mutation et avant reprise.
- Prochaine action vérifiable : lever les trois blocages locaux ci-dessus, refaire la gate Étape 8 puis exécuter réellement la recette Étape 9 des neuf jeux jusqu'à l'historique avec les deux comptes, l'invité, le tiers non membre, desktop/mobile, clavier/tactile, rechargement, perte réseau et reconnexion. Si cette preuve devient verte, réinspecter juste avant mutation les migrations, parties, jobs, versions et engagements, appliquer uniquement les migrations compatibles via le connecteur Supabase, vérifier leur historique, configurer ensuite Vault, déclencher un job synthétique et traiter les jobs de façon idempotente et explicable, sans suppression.
- Livraison documentaire : le commit `b27ce0f171580f3269d3983a9dd82d7163084517` a été poussé avec succès par `git push origin main` (`9f4aba3..b27ce0f main -> main`) ; `HEAD` et la référence locale `origin/main` correspondent. La vérification directe `git ls-remote origin refs/heads/main` a ensuite échoué avec `fatal: unable to access 'https://github.com/ThibaudFrancony/thib.fun.git/': Could not resolve host: github.com` ; cette preuve réseau négative est conservée, sans conclure à un échec du push accepté.
- Contradiction : aucune avec `AGENTS.md` ni avec la demande active ; l'arrêt résulte uniquement de la précondition non satisfaite de l'Étape 8 et de la règle de ne jamais présenter un scénario `blocked` comme réussi.

### 16/09/2026 — Correction et validation locale de la gate Étape 8

- Périmètre et environnement : correction effectuée sur `main`, arbre basé sur `4f3eeeb`, Docker Desktop et Supabase/PostgreSQL locaux, Next dev `127.0.0.1:3000`, Chromium desktop et profil iPhone 13 mobile. Deux comptes permanents actifs Alice/Bob, un tiers désactivé Mallory et des invités Auth anonymes ont été créés uniquement dans la base isolée ; les mots de passe étaient éphémères, non affichés et non conservés. Aucune migration, donnée, configuration, clé Vault ou ressource Supabase/Vercel distante n'a été lue ou modifiée dans cette correction.
- Dépendances de recette corrigées : `scripts/write-local-env.mjs` écrit désormais `ROOM_CHANGE_RPC_ENABLED=true`, les bornes de budget IA et `QUIZ_AI_LOCAL_FIXTURE=accept` dans le seul `.env.local` ignoré. `src/server/quiz/config.ts` refuse ce fixture lorsque `NODE_ENV=production` ou lorsque `APP_ORIGIN`/`NEXT_PUBLIC_SUPABASE_URL` ne sont pas loopback. Aucun faux secret DeepSeek n'est utilisé et le transport réel reste celui des environnements configurés.
- Juge local : `src/server/quiz/local-fixture.ts`, `src/server/quiz/judge.ts`, `src/server/ttmc/judge.ts` et `src/server/jobs/worker.ts` fournissent un verdict JSON déterministe et comptabilisent le provider réel (`local-fixture` ou `deepseek`). Les tests couvrent accept/reject/ambiguous et le refus du fixture en production, avec URL Supabase distante ou origine applicative distante.
- Gate reproductible : `scripts/run-step8-gate.mjs` prépare les comptes locaux avec un mot de passe généré, sans afficher les identifiants, force un worker Playwright et exécute par défaut l'invité, la gestion de salon, Trou Noir et TTMC sur desktop/mobile. Le script est exposé par `pnpm test:step8`; `--full` conserve la possibilité de lancer toute la suite.
- Salon réel : `tests/e2e/room-management.spec.ts` vérifie avec deux contextes la création/reprise, l'activation des contrôles avancés, les ready séquencés, la configuration qui annule les ready, le transfert d'hôte, le départ de l'hôte, la reprise par code, le départ du second joueur puis la fermeture par le dernier membre. Le test passe 2/2 desktop/mobile. `tests/e2e/compatibilite.spec.ts` attend désormais chaque projection ready avant la commande suivante, sans contourner la version serveur.
- Invité réel : `tests/e2e/auth-guest.spec.ts` attend et vérifie la réponse Auth anonyme avant `/profil`, puis laisse 20 secondes au rendu SSR lors d'une compilation froide. Après redémarrage de la pile locale pour prendre en compte `enable_anonymous_sign_ins`, la relance ciblée passe 4/4 sans retry. Ce fonctionnement correspond au modèle Supabase où l'utilisateur anonyme possède une session authentifiée, tout en restant identifié comme invité par le produit.
- Vérifications finales : `node_modules/.bin/vitest run` — 60 fichiers, 432 réussites et 2 sentinelles d'échec attendu ; `node_modules/.bin/eslint .`, `node_modules/.bin/tsc --noEmit`, `node_modules/.bin/next build --webpack`, validation des contenus, `docs:check`, matrice et `git diff --check` — succès ; pgTAP — 6 fichiers et 190 assertions ; lint DB — aucune erreur de schéma et les huit familles d'avertissements PL/pgSQL déjà connues. La suite Playwright complète a terminé en 13,6 minutes avec 44 succès directs et 2 succès après retry ; après correction des deux attentes, les relances invité/Compatibilité passent 6/6 sans retry et la gate finale `node scripts/run-step8-gate.mjs` passe 10/10 sans retry en 2,3 minutes. Run anonymisé : `step8-correction-20260916-01`.
- Difficultés rencontrées et résolues : (1) un premier Vitest ciblé a produit 9 échecs parce que les juges lisaient à tort la configuration budgétaire complète ; séparation de `getQuizJudgeTransportConfiguration()` et relance à 33/33 ; (2) le premier test salon échouait sur un locator Bob ambigu et une course de ready ; locator borné à `.geo-member-name` et attente de projection ajoutés, puis 2/2 ; (3) la première suite complète a révélé des retries Auth invité et Compatibilité ; leurs attentes réseau/projection ont été durcies ; (4) une relance ciblée interrompue a ensuite échoué pendant un redémarrage Docker : les logs Auth montrent des `context deadline exceeded`, des connexions DB en timeout puis un arrêt gracieux, et non un défaut applicatif reproductible ; après retour sain, les mêmes tests passent 6/6 ; (5) `supabase stop --help`/`start --help` ont d'abord échoué sur l'écriture de télémétrie hors sandbox (`EPERM`) et `supabase status` sur le socket Docker, puis les commandes locales autorisées ont réussi ; (6) une lecture `fetch` des réglages Auth a d'abord échoué dans le sandbox puis répondu 200 hors sandbox, sans exposer l'option anonyme, de sorte que seul le parcours E2E effectif sert de preuve.
- Statut : la gate Étape 8 passe localement et `nine-game-complete-run` devient `not-run`, pas `pass` ni `blocked`. L'Étape 9 n'a pas commencé dans ce changement : `sessionIds=[]`, aucune recette manuelle des neuf jeux jusqu'à l'historique n'est revendiquée. Les limites de production, le benchmark DeepSeek réel et le déploiement restent inchangés.
- Livraison : le correctif applicatif, le runner, les tests et la première mise à jour des preuves sont réunis dans le commit `173008d` sur `main`. Le commit documentaire `35a3735` inscrit ensuite cet identifiant exact dans la matrice. `git push origin main` a confirmé `4f3eeeb..35a3735 main -> main`, puis `HEAD`, `origin/main` et `git ls-remote origin refs/heads/main` ont tous retourné `35a37357c1732f976519d38b7cef56950785d53e`. Aucun de ces commits ne vaut déploiement ni mutation Supabase distante.
- Contradiction : aucune avec `AGENTS.md` ; la demande corrige la gate locale sans autoriser de mutation distante.

### 17/09/2026 — Durcissement multijoueur, reprise des parties bloquées et préparation worker

- Demande : « fait tout d'un coup si tu le peux » avec priorité au multijoueur réel (rejoindre un salon, forfait, jouer vraiment). Questions posées et réponses : URL canonique `https://thibfun.vercel.app` ; token Vercel fourni par l'utilisateur (attendu) ; autorisation explicite des mutations Supabase distantes ; clé DeepSeek fournie par l'utilisateur avec modèle le plus rapide et le moins cher ; budget `AI_DAILY_BUDGET_USD=1` ; clôture explicite des 4 parties et 10 jobs avant activation ; les parties multiples restent possibles mais une nouvelle partie clôt l'ancienne, la fermeture automatique sur déconnexion étant renvoyée à plus tard.
- Contradiction : aucune avec `AGENTS.md` ; la demande active autorise explicitement le périmètre distant, et les choix produit (clôture `superseded`, forfait visible partout) ne contredisent aucune règle non négociable.
- Code livré : `canClaimForfeit` (`src/lib/network-sync.ts`) et garde du bouton forfait dans les neuf jeux ; bouton Abandon Géo visible dans toutes les phases ; `src/lib/client-request.ts` et refonte des neuf `*-setup.tsx` plus `sign-out-button.tsx` (fin des boutons figés sur panne réseau) ; correction du jeton d'entraînement BombParty (`drawSequence`) ; refonte du salon (`src/app/salons/[roomId]/room-lobby.tsx`) avec aperçu de lien, CTA « Rejoindre le salon », panneau « Partie en cours » sans boucle, panneau salon fermé, copie du lien, brouillon préservé, défauts de config par jeu (`src/games/default-configs.ts`), bouton « Réessayer » d'une commande en attente ; route `GET /api/rooms/[roomId]/preview` ; codes publics `ROOM_EXPIRED`/`NOT_A_ROOM_MEMBER` et propagation des erreurs RPC du salon ; `maxDuration = 60` sur le worker ; raisons `superseded`/`worker_unreachable` (contrainte SQL, `record_match_result`, type `ResultReason`, libellés d'historique) ; libellés « revanche » sans action remplacés.
- Migration additive `supabase/migrations/20260917163900_step10_multiplayer_hardening.sql` (créée avec la CLI, appliquée au local puis au distant) : `server_get_room_preview` ; `server_change_room` autorise LEAVE sur salon fermé sans le rouvrir ; `private.finalize_match_abandoned` générique (raison paramétrée, événement `MATCH_ABANDONED`) et `finalize_match_technical_error` conservé en délégation ; `record_match_result` accepte les nouvelles raisons ; `server_fail_job` n'abandonne plus une partie pour un job de service ; `private.reap_exhausted_jobs()` et cron `tibo-fun-reap-exhausted-jobs` toutes les 30 s ; `server_start_match` relit le reçu sous verrou (acteur/type/hash) et clôt explicitement les parties actives précédentes (`superseded`).
- Tests : Vitest 61 fichiers / 438 réussis + 2 sentinelles d'échec attendu ; `eslint .` et `tsc --noEmit` propres ; `next build --webpack` réussi ; pgTAP 7 fichiers / 204 assertions (dont `step10_multiplayer_hardening.sql`, 14 assertions) après `supabase db reset --local` ; `content:validate`, `docs:check` (25 fichiers) et `check-test-matrix` passent ; gate Étape 8 `node scripts/run-step8-gate.mjs` 12/12 sans retry sur Chromium desktop et iPhone 13 mobile, incluant le nouveau scénario « rejoint un salon par son lien partagé » (4/4 ciblés desktop/mobile) et la session invitée.
- Actions distantes autorisées et vérifiées : `supabase migration up --linked` applique `20260917163900` (36/36 migrations distantes) ; inspection confirmant la présence de `server_get_room_preview`, `reap_exhausted_jobs` et du cron de reprise ; récupération idempotente exécutée en SQL : les 4 parties actives (3 TTMC, 1 Géographie) sont passées `abandoned` avec résultat `technical_error` et vues terminales, les 10 jobs `pending` et 4 jobs déjà annulés sont `cancelled`. État final : 0 match actif, 0 job en attente, 4 résultats techniques, aucune suppression de donnée. Aucun secret, aucune clé Vault, aucun déploiement Vercel et aucune donnée utilisateur n'a été touché.
- Difficultés rencontrées et résolues : (1) le nouveau `server_start_match` clôturait les parties actives mais `record_match_result` refusait la raison `superseded` (`INVALID_RESULT`) — la fonction a été redéfinie additivement avec les deux nouvelles raisons, démontrée par un test pgTAP dédié ; (2) le test pgTAP historique 11 lisait le corps de `finalize_match_technical_error`, devenu une délégation — le test vérifie désormais le délégué et l'implémentation générique sans affaiblir l'intention ; (3) `.next/types` contenait des doublons `* 2.ts` générés localement qui faisaient échouer `tsc` — supprimés comme artefacts ignorés ; (4) la première gate relançait les échecs quiz documentés, causés en réalité par `INVALID_RESULT` sur la clôture `superseded` — corrigé, gate repassée 10/10 puis 12/12.
- Limites restantes : la configuration Vercel n'a pas été vérifiée ni modifiée (token attendu) ; Vault reste vide, donc le worker durable et le jugement quiz réel ne tournent pas encore en production ; la recette manuelle des neuf jeux jusqu'à l'historique (Étape 9), le benchmark DeepSeek réel et le smoke de production à deux comptes restent à faire. Le token Vercel est le seul point bloquant externe.
- Livraison : commit et push de ce changement sur `main` après validation, puis configuration Vercel/Vault sous autorisation, job synthétique et smoke. Aucun déploiement n'est revendiqué à l'instant de cette entrée.

### 17/09/2026 — Configuration Vercel/Vault et validation du worker en production

- Périmètre : suite directe de l'entrée précédente, après fourniture du token Vercel par l'utilisateur. Autorisation explicite de muter la configuration Vercel et Vault. Aucun secret n'a été affiché, committé ou journalisé ; les fichiers temporaires qui les contenaient ont été supprimés après usage.
- Déploiement : le push de `371c070` sur `main` a déclenché automatiquement un déploiement de production Vercel (`dpl_A6n54mcjgG9hzzYXEgGXPzBvmq35`, `READY`, `main`, `371c070`). Les variables ont été appliquées puis un redéploiement forcé (`dpl_2bFyR7Eu2jbubNNegkTs4SA5F1vQ`) a été promu `READY` ; la production sert donc exactement ce commit.
- Variables Vercel production : `APP_ORIGIN` réécrit à `https://thibfun.vercel.app` (comparaison exacte avec l'en-tête Origin) ; `INTERNAL_JOB_SECRET` réécrit (valeur générée ≥ 32 caractères, non affichée) ; ajout de `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL=deepseek-flash`, `AI_DAILY_BUDGET_USD=1`, `AI_DAILY_CALL_LIMIT=1000` et `ROOM_CHANGE_RPC_ENABLED=true`. `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` et `SUPABASE_SERVICE_ROLE_KEY` étaient déjà présents.
- Clé et modèle IA : la clé fournie par l'utilisateur répond ; le compte expose `deepseek-flash` et `deepseek-v4-pro`. `deepseek-flash` a été retenu (le plus rapide et le moins cher) et testé sur un appel JSON réel : réponse JSON valide en ~2,6 s, compatible avec le timeout de 5 s par tentative et le budget de 12 s du juge.
- Vault : `worker_origin=https://thibfun.vercel.app` et `internal_job_secret` (même valeur que Vercel) écrits via `supabase db query --linked --file` ; l'inspection distante confirme les deux noms. Le dispatcher `tibo-fun-dispatch-due-jobs` peut désormais appeler le worker.
- Preuve de bout en bout : un job synthétique `check_absence` (clé `synthetic:step10:validation:1`) inséré sur une partie abandonnée est passé de `pending` à `cancelled` avec `attempts=1` et `last_error_code=MATCH_NOT_ACTIVE`. Cela prouve que la chaîne Cron → Vault → pg_net → worker Vercel authentifié fonctionne réellement, sans créer de partie. Production vérifiée en lecture HTTP : accueil 200, page salon 200, route `GET /api/rooms/[roomId]/preview` présente et répond 401 sans session (donc nouveau code déployé).
- État final : 36/36 migrations distantes, 0 partie active, 0 job en attente, Vault configuré, worker opérationnel. Les réglages de recette à deux comptes réels sur la production restent à faire par le propriétaire ; l'Étape 9 (neuf jeux jusqu'à l'historique) n'est pas exécutée.
- Contradiction : aucune avec `AGENTS.md` ; les mutations distantes (configuration Vercel, Vault, job synthétique) résultent de l'autorisation explicite de la tâche active.

### 17/09/2026 — Suppression du forfait réclamé et grâce de sortie de 30 secondes

- Demande : « on va fix le "réclamer le forfait" » ; « je veux le supprimer complètement du jeu : soit on abandonne, soit on reste » ; « enlève ce principe de pouvoir revenir dans la partie » précisé en « si on quitte la partie sans cliquer sur abandonner, on doit pouvoir rejoindre dans les 30 secondes, sinon la partie est comptée comme abandonnée par la personne qui a quitté ». Aucune contradiction utile à signaler : la demande précise un comportement produit, pas une règle non négociable de `AGENTS.md`.
- Modèle retenu : `CLAIM_FORFEIT` disparaît des neuf moteurs, des schémas d'action, des projections, des composants et du SQL (liste noire des commandes joueur) ; seuls `RESIGN` et la poursuite restent possibles. Un joueur qui cesse de donner signe a 30 secondes pour revenir ; passé ce délai, l'absence est attribuée à ce joueur : en compétitif l'adversaire gagne, en coopératif la partie se termine sans vainqueur. Si les deux sont absents, abandon sans gagnant. Le salon ne se rejoint plus par code une fois la partie lancée, et le panneau « Reprendre la partie » est retiré : la reprise se fait en rouvrant l'URL de la partie pendant la grâce.
- Code : seuils `30_000 ms` dans les neuf `shouldAbandonForXAbsence` ; `onXAbsence` calcule le siège parti depuis `ctx.actorId` et attribue la victoire au joueur resté (`resultFor`/`abandonResult`/`abandonTransition` adaptés) ; `src/server/jobs/worker.ts` ajoute `absentActorId` et transmet l'identité du partant aux neuf processeurs d'absence ; suppression du bouton et des champs `canClaimForfeit`/`CLAIM_FORFEIT` partout, y compris `src/lib/network-sync.ts` (helper retiré) ; `FORFEIT_NOT_AVAILABLE` retiré des codes publics.
- Migration additive `supabase/migrations/20260917172349_step10_remove_forfeit_and_leave_grace.sql` (créée avec la CLI) : `server_commit_match` blackliste `CLAIM_FORFEIT`, n'autorise que `RESIGN` après échéance, applique la grâce unique de 30 secondes et réarme l'observation à 5 s ; `server_finish_job` réarme à 5 s ; `server_join_room` refuse un salon dont la partie a démarré.
- Tests : Vitest 61 fichiers / 431 réussis + 2 sentinelles ; `eslint .` et `tsc --noEmit` propres ; `next build --webpack` réussi ; pgTAP 7 fichiers / 204 assertions, dont les assertions de source `transaction_commit.sql` réécrites (plus de 120/180 ni de `FORFEIT_NOT_AVAILABLE`) et le scénario runtime qui vérifie désormais que `CLAIM_FORFEIT` est refusé (`INVALID_COMMAND_TYPE`) et ne laisse aucun reçu ; tests moteurs mis à jour vers la grâce de 30 s et la victoire du joueur resté ; `geo-theme` vérifie l'abandon sans forfait.
- Limites : la cadence d'observation de 5 s n'est locale qu'après application de la migration ; le worker de production appliquera la nouvelle règle dès que la migration distante sera appliquée et le commit déployé. Le benchmark DeepSeek réel et la recette des neuf jeux restent inchangés.
- Livraison : commit et push sur `main`, puis application distante de la migration via la CLI Supabase (autorisation explicite permanente de la tâche). Aucun secret ni donnée utilisateur touché.

### 17/09/2026 — Refonte de l'accueil : hero néon et carrousel de jeux

- Demande : refaire la page d'accueil pour ressembler à la première maquette fournie, avec un carrousel dynamique (flèches gauche/droite, cartes qui montent au survol) ; vérifier si les assets fournis ensuite suffisent. Réponses utilisateur : les fichiers seront déposés dans `public/home/` ; cartes ré-exportées en fond transparent ; garder les noms des cartes (Trou noir, TTMC, Géographie…) ; laisser « DUEL » provisoirement sur Compatibilité/Longueur d'onde ; ajouter une police arrondie pour le titre.
- Contradiction : aucune avec `AGENTS.md` ; les noms d'affichage restent modifiables sans migration et les images sont des assets UI, pas du contenu de jeu.
- Réalisation : `src/games/registry.ts` gagne `cardName` (nom des cartes, sans toucher aux identifiants ni aux noms d'affichage existants) ; `src/components/home-game-selector.tsx` devient un carrousel (carte active centrée et agrandie, cartes latérales atténuées, boucle continue, flèches, points, balayage tactile, clavier Flèches/Début/Fin, `aria-live`, 9 cartes, repli gradient si l'image manque) ; `src/components/home-hero-background.tsx` affiche le fond attendu (`/home/hero.webp` puis `.png`) avec voile sombre appliqué seulement quand l'image est chargée ; `src/app/page.tsx` reconstruit en hero plein écran (titre dégradé « On joue à quoi ? », sous-titre) ; `src/app/layout.tsx` charge la police Fredoka via `next/font/google` (`--font-display`) ; `src/app/globals.css` remplace tout le bloc `.home-*` par les styles hero/carrousel (responsive, `prefers-reduced-motion`, `forced-colors`).
- Assets : les 9 cartes en fond transparent (`~/Downloads/tibo_fun_carousel_cards/*.png`) et le fond (`hero.png`) ont été copiés dans `public/home/` ; les cartes ont été réduites à 720×960 (9,7 Mo au total, servies optimisées par `next/image` sur l'accueil). Le composant essaie `.png` puis `.webp`.- Vérifications : `pnpm typecheck` et `eslint .` propres ; Vitest 61 fichiers / 431 réussis + 2 sentinelles ; `next build --webpack` réussi (seul l'avertissement préexistant `profil/avatar` subsiste) ; test Playwright `tests/e2e/home.spec.ts` réécrit et réussi sur Chromium desktop et iPhone 13 mobile (9 cartes, flèches/points/clavier changent la carte active, carte latérale recentrée puis jouable, aucun débordement horizontal) ; captures desktop et mobile inspectées, le fallback sans image est propre.
- Limites : le rendu final dépend du dépôt des 9 cartes et du fond par le propriétaire ; le libellé « DUEL » reste erroné sur les deux cartes coopératives ; la police Fredoka nécessite un accès réseau au build (Vercel et poste local OK), à surveiller en CI hors ligne.
- Livraison : commit et push sur `main` (déploiement Vercel automatique comme pour les changements précédents).

### 17/09/2026 — Carrousel d'accueil en coverflow 3D, glisser-déposer et aimantation

- Demande : transformer le carrousel en vrai coverflow 3D (carte centrale dominante, voisines inclinées en arc ±8–12°, cartes plus lointaines plus petites/reculées, perspective et profondeur, glows conservés, titres lisibles) et rendre le carrousel manipulable à la main (clic gauche/tactile), avec la carte centrale aimantée et une animation fluide (la flèche saccadait). Réponses aux questions : mix premium subtil (rotateY ≈12°, inclinaison ±9°), 7 cartes visibles sur grand écran, glissement multi-cartes puis aimantation, clic latéral = recentrer / clic central = jouer, mobile = centre + aperçus, bouton play centré.
- Réalisation : `src/components/home-game-selector.tsx` réécrit avec une position flottante (index continu), calcul par carte de la translation, `translateZ`, `rotateY`, `rotateZ`, échelle, opacité, `z-index` et filtre `drop-shadow` (glow rose sur la carte active) ; les noms d'affichage, les points, les flèches, le clavier (Flèches/Début/Fin) et le balayage tactile restent. Le DOM des cartes est désormais stable (un seul `<button>` par carte, jamais remonté) : fini la saccade à la flèche. Drag via écouteurs `window` (pointermove/up/cancel), seuil de 6 px pour distinguer clic et glissement, relâchement aimanté à l'entier le plus proche. `src/app/globals.css` : `perspective` sur le viewport, `transform-style: preserve-3d`, transition commune 560 ms, transitions coupées pendant le drag, survol avec `--card-lift`, bouton play centré, rail et `<li>` en `pointer-events: none` (sinon, en `preserve-3d`, le rail captait les clics des cartes latérales situées derrière son plan).
- Vérifications : `tsc` et `eslint` propres ; Vitest 61 fichiers / 431 réussis + 2 sentinelles ; `next build --webpack` OK ; test Playwright `tests/e2e/home.spec.ts` étendu (flèches, points, clavier, clic latéral, glissement de plusieurs cartes, aimantation, clic central → navigation) et réussi sur Chromium desktop et iPhone 13 mobile ; captures desktop/mobile inspectées (7 cartes, arc, titres lisibles).
- Limites : la disposition et les angles sont pilotés par une table de tailles dans le composant (à ajuster si besoin) ; le défaut « DUEL » sur les deux cartes coopératives reste inchangé ; l'intensité 3D peut être augmentée en une ligne si tu la veux plus marquée.
- Livraison : commit et push sur `main`.

### 17/09/2026 — Refonte visuelle de la page Géographie / HexaPoint

- Demande : redesigner uniquement la vue de `/jeux/geographie` pour l'aligner sur la nouvelle DA (fond illustré en couche, France animée séparément, header transparent en pills, colonne gauche de présentation, colonne droite « créer / rejoindre » en cartes glass), sans toucher à la logique. Précisions : header nouveau scopé à cette page, extension aux autres jeux plus tard.
- Analyse préalable conservée : `src/app/jeux/geographie/page.tsx` (serveur, sans state) ; `GeographySetup` (state `config`/`busy`/`error`, `update`, `createRoom` → `POST /api/rooms` puis `/salons/{roomId}`, 3 selects, radios `selection`) ; `RoomJoin` (state `code`/`busy`/`error`, `onSubmit` → `POST /api/rooms/join`, input 6 caractères). **`RoomJoin` est partagé par 7 autres pages** : sa logique et son markup n'ont pas été modifiés, tout le restyle est scopé à `.geo-hexapoint`.
- Réalisation : assets copiés dans `public/geographie/background.png` (1672×941) et `france.png` (1254×1254, transparent, glow bleu intégré). `page.tsx` restructuré (fond `next/image` `fill` en `absolute inset-0` `object-cover` `pointer-events:none` sans voile opaque, header, colonne gauche : retour, eyebrow, titre Fredoka avec « viser juste. » en dégradé rose→corail, description, 3 mini-cartes, France flottante ; colonne droite : `GeographySetup` puis `RoomJoin`). `globals.css` : nouveau bloc scopé `.geo-hexapoint` (header pills translucides, cartes glassmorphism, selects/inputs sombres avec focus cyan, radios Aléatoire/Défi avec état sélectionné cyan, CTA menthe, erreurs resetées) et animation CSS `geoFranceFloat` (translateY 0→-8 px, 7 s, `will-change`, `prefers-reduced-motion` désactivé).
- Vérifications : `tsc` et `eslint` propres ; Vitest 61 fichiers / 431 réussis + 2 sentinelles ; `next build --webpack` OK ; gate Étape 8 `node scripts/run-step8-gate.mjs` 12/12 (dont création/rejoindre réels depuis la nouvelle page) ; `tests/e2e/geographie.spec.ts` 2/2 desktop/mobile (sélection « Manches », création, connexion, manche complète) ; contrôle ciblé temporaire desktop+mobile : aucun message console d'erreur, aucun `pageerror`, pas de débordement horizontal, valeurs par défaut et changements des trois selects, bascule Aléatoire/Défi, boutons actifs, input code borné à 6, liens header et « Tous les jeux » intacts.
- Limites : le style du header et des cartes glass n'est appliqué qu'à Géographie (les 8 autres pages de jeu gardent l'ancien habillage) ; la page tient dans le viewport en 1080p, un léger scroll reste possible sur des hauteurs plus courtes.
- Livraison : commit et push sur `main` (déploiement Vercel automatique).
